# 회고록: 머신러닝_2026-07-01

오늘은 결정트리 문제풀이에서 출발해 SVM 분류 모델인 SVC까지 이어진 날이었다.

어제까지는 로지스틱 회귀, 오즈비, 결정트리를 통해 분류 모델의 기본 흐름과 해석 방법을 봤다면, 오늘은 모델이 데이터를 나누는 방식과 하이퍼파라미터가 경계에 주는 영향을 더 구체적으로 확인했다.

```text
결정트리 = 조건을 나누며 예측
SVC = 클래스 사이의 마진이 넓은 결정 경계를 찾음
```

오늘의 중심은 크게 두 가지였다.

```text
결정트리 사전 가지치기
SVC의 마진, 서포트 벡터, 커널, C, gamma
```

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
04_decision_tree_svm/practice/01_decision_tree.ipynb
04_decision_tree_svm/03_svc.ipynb
04_decision_tree_svm/practice/02_svc.ipynb
04_decision_tree_svm/practice/02_svc_추가문제.ipynb
04_decision_tree_svm/04_svr.ipynb
04_decision_tree_svm/practice/03_svr.ipynb
04_decision_tree_svm/dt_tree_wine.png
04_decision_tree_svm/dt_tree_wine_by_entropy.png
04_decision_tree_svm/dt_tree_iriis.png
```

파일 흐름을 보면 오전에는 결정트리 문제풀이와 가지치기 실습을 정리했고, 이후에는 SVM을 이용한 분류 모델인 SVC로 넘어갔다.
그리고 SVM을 회귀 문제에 적용하는 SVR까지 이어졌다.

## 오늘 커밋한 내용

```text
f4ec187 -record - 머신러닝 분류모델
         - 결정트리 문제풀이 업데이트
cb40aa0 -record - 머신러닝 분류모델
         - 결정트리 문제풀이 업데이트
873ad3d -record - 머신러닝 분류모델
         - SVM을 이용한 클래스 분류모델 중 SVC 업데이트
b16d5e6 -record - 머신러닝 분류모델
         - SVM을 이용한 클래스 분류모델 중 SVC 업데이트
6419f90 -record - 머신러닝 분류모델
         - 문제풀이 업데이트
2a37f07 -record - 머신러닝 분류모델
         - 문제풀이2 업데이트
35cc602 -record - 머신러닝 분류모델
         - update
aedc3e4 update
```

커밋 흐름을 보면 오늘의 학습 순서는 명확했다.

```text
결정트리 문제풀이
-> 결정트리 가지치기 비교
-> SVC 개념 정리
-> C, kernel, gamma 비교
-> 결정 경계 시각화
-> SVR 회귀
-> epsilon tube, kernel별 예측 곡선, 회귀 평가 지표
```

즉 오늘은 모델을 한 번 학습시키고 점수를 확인하는 단계에서 끝나지 않고, 모델의 복잡도를 조절하고 결정 경계 또는 예측 함수가 어떻게 바뀌는지 비교하는 데 초점이 있었다.

## 오늘 질문 흐름

현재 파일과 커밋 흐름을 기준으로 보면, 오늘의 질문은 다음 방향으로 이어졌다고 볼 수 있다.

```text
결정트리에서 max_depth를 제한하면 무엇이 달라지는가?
min_samples_split과 min_samples_leaf는 어떤 차이가 있는가?
평가셋 score가 가장 높은 모델이 항상 좋은 모델인가?
GridSearchCV로 고른 모델은 왜 평가셋 최고 모델과 다를 수 있는가?
SVM은 어떤 기준으로 클래스를 나누는가?
마진과 서포트 벡터는 무엇인가?
C 값이 작거나 크면 결정 경계가 어떻게 달라지는가?
linear kernel과 RBF kernel은 언제 다르게 쓰는가?
gamma 값이 너무 작거나 크면 왜 문제가 되는가?
PCA로 2차원 시각화를 하는 이유는 무엇인가?
SVC와 SVR은 무엇이 다른가?
SVR에서 epsilon tube는 무엇인가?
SVR에서 target y도 스케일링하는 이유는 무엇인가?
스케일링된 예측값은 왜 inverse_transform()으로 되돌려야 하는가?
R2, MAE, RMSE는 각각 어떻게 해석해야 하는가?
```

오늘 질문의 공통점은 "모델을 어떻게 실행하는가"보다 "모델의 판단 기준과 예측 함수가 어떻게 달라지는가"에 가까웠다.

## 결정트리 사전 가지치기

오늘 먼저 정리한 내용은 결정트리의 사전 가지치기였다.

결정트리는 조건을 계속 나누면서 데이터를 분류한다.
그런데 제한 없이 계속 나누면 학습 데이터에 너무 맞춰질 수 있다.
이것이 과대적합이다.

```text
트리가 너무 깊음
-> 학습 데이터는 잘 맞힘
-> 새 데이터에서는 성능이 떨어질 수 있음
```

그래서 오늘은 트리가 자라기 전에 미리 제한을 거는 방법을 비교했다.

```text
max_depth = 트리의 최대 깊이 제한
min_samples_split = 노드를 나누기 위한 최소 샘플 수
min_samples_leaf = leaf node에 남아야 하는 최소 샘플 수
```

`max_depth`는 트리가 얼마나 깊게 자랄 수 있는지 직접 제한한다.
값이 작으면 모델이 단순해지고, 값이 너무 작으면 과소적합될 수 있다.

`min_samples_split`은 노드를 다시 나누기 위한 최소 샘플 수이다.
값이 커질수록 작은 노드는 더 이상 나뉘기 어려워져 트리가 단순해진다.

`min_samples_leaf`는 마지막 leaf node에 최소 몇 개의 샘플이 남아야 하는지 정한다.
너무 적은 샘플만 남는 leaf를 막으면 예외적인 데이터 하나하나를 외우는 현상을 줄일 수 있다.

## 학습셋 점수와 평가셋 점수 비교

오늘 결정트리 실습에서 중요한 포인트는 학습셋 score와 평가셋 score를 함께 본 것이다.

```text
train score = 학습 데이터에서의 성능
test score = 새 데이터처럼 따로 둔 평가 데이터에서의 성능
```

학습셋 점수만 높다고 좋은 모델은 아니다.
학습셋 점수는 매우 높은데 평가셋 점수가 낮으면 과대적합을 의심해야 한다.

```text
train score 높음 + test score 낮음
-> 학습 데이터를 외웠을 가능성
```

반대로 둘 다 낮으면 모델이 데이터의 패턴을 충분히 못 잡은 것일 수 있다.

```text
train score 낮음 + test score 낮음
-> 과소적합 가능성
```

그래서 오늘은 score뿐 아니라 트리 깊이와 leaf node 개수도 함께 비교했다.

```text
score
tree_depth
leaf_count
```

이 세 가지를 같이 봐야 모델이 얼마나 복잡한지, 그리고 그 복잡도가 평가 성능에 도움이 되는지 판단할 수 있다.

## GridSearchCV

결정트리 가지치기 후보를 하나씩 직접 비교한 뒤에는 `GridSearchCV`로 여러 후보를 한 번에 비교했다.

```python
param_grid = {
    "max_depth": [2, 3, 4, 5, None],
    "min_samples_split": [2, 10, 20],
    "min_samples_leaf": [1, 5, 10]
}
```

`GridSearchCV`는 여러 하이퍼파라미터 조합을 만들고, 교차검증으로 평균 성능을 비교한다.

```text
직접 비교 = 정해진 train/test split에서 후보 비교
GridSearchCV = train 데이터를 여러 번 나눠 평균 성능 비교
```

오늘 중요한 점은 교차검증으로 고른 모델이 평가셋에서 항상 최고 점수를 내는 것은 아니라는 점이었다.

그 이유는 `GridSearchCV`가 평가셋을 직접 보고 고르는 것이 아니라, 학습 데이터 안에서 교차검증 평균을 기준으로 고르기 때문이다.
그래서 마지막에는 따로 남겨둔 평가셋으로 다시 확인해야 한다.

## 특성 중요도

결정트리에서는 `feature_importances_`를 통해 모델이 어떤 feature를 상대적으로 많이 사용했는지 볼 수 있다.

```python
best_tree.feature_importances_
```

이 값은 feature가 분기에 얼마나 기여했는지를 나타낸다.
값이 클수록 트리의 판단에 더 많이 사용된 feature라고 볼 수 있다.

다만 이것은 인과관계를 의미하지 않는다.
중요도가 높다고 해서 그 feature가 반드시 원인이라는 뜻은 아니다.
모델이 예측을 할 때 많이 활용한 변수라고 해석하는 것이 더 안전하다.

## SVM과 SVC

오늘 후반부에는 SVM을 이용한 분류 모델인 SVC를 정리했다.

SVM은 클래스를 나누는 결정 경계를 찾는 모델이다.
여기서 중요한 기준은 단순히 학습 데이터를 많이 맞히는 것이 아니라, 클래스 사이의 마진을 넓게 만드는 것이다.

```text
결정 경계 = 클래스를 나누는 기준선 또는 기준면
마진 = 결정 경계와 가까운 데이터 사이의 여유 공간
서포트 벡터 = 결정 경계와 가장 가까운 데이터
```

SVM은 여러 가능한 경계 중에서 마진이 넓은 경계를 선호한다.
마진이 넓으면 새 데이터가 조금 달라져도 같은 클래스로 예측될 가능성이 높기 때문이다.

```text
마진이 좁음 -> 경계가 불안정할 수 있음
마진이 넓음 -> 새 데이터에 더 안정적일 수 있음
```

## SVC에서 스케일링이 중요한 이유

SVC는 거리와 마진을 중요하게 보는 모델이다.
그래서 feature들의 스케일이 다르면 특정 feature가 모델 판단에 과하게 영향을 줄 수 있다.

예를 들어 한 feature는 0부터 1 사이이고, 다른 feature는 0부터 1000 사이이면 거리 계산에서 큰 단위를 가진 feature가 더 크게 작용할 수 있다.

그래서 오늘 SVC 실습에서는 `StandardScaler`를 사용했다.

```python
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

여기서도 중요한 원칙은 이전과 같았다.

```text
훈련 데이터에는 fit_transform()
평가 데이터에는 transform()
```

평가 데이터로 스케일링 기준을 다시 배우면 데이터 누수가 생길 수 있기 때문이다.

## C 값 비교

SVC에서 `C`는 오분류를 얼마나 강하게 벌점으로 줄지 정하는 값이다.

```text
C 작음 -> 오분류를 어느 정도 허용, 경계가 단순해질 수 있음
C 큼 -> 오분류를 줄이려 함, 경계가 복잡해질 수 있음
```

오늘 실습에서는 `C` 값을 바꿔가며 학습셋 accuracy, 평가셋 accuracy, support vector 개수를 비교했다.

이 비교의 핵심은 점수 하나만 보는 것이 아니었다.
`C`가 커질수록 학습 데이터에 더 맞추려는 경향이 생길 수 있으므로, 평가셋 성능과 support vector 개수의 변화도 함께 봐야 했다.

```text
accuracy
support_vector_count
```

support vector는 결정 경계에 직접 영향을 주는 중요한 데이터들이다.
따라서 support vector 개수를 보면 모델이 경계를 만들 때 얼마나 많은 데이터를 기준으로 삼았는지도 함께 확인할 수 있다.

## 커널과 비선형 분류

SVC에서는 `kernel`을 통해 결정 경계의 형태를 바꿀 수 있다.

```text
linear kernel = 직선 또는 초평면 경계
rbf kernel = 곡선형 비선형 경계
poly kernel = 다항식 형태 경계
```

Iris처럼 어느 정도 선형적으로 나뉘는 데이터에서는 linear kernel이 설명하기 쉽다.
하지만 `make_moons`나 `make_circles`처럼 직선 하나로 나누기 어려운 데이터에서는 RBF kernel이 더 적합할 수 있다.

오늘 추가 문제에서는 PCA로 데이터를 2차원으로 줄인 뒤 linear kernel과 RBF kernel의 결정 경계를 시각화했다.
또 `make_circles` 데이터에서 선형 경계와 비선형 경계의 차이를 확인했다.

```text
linear = 직선으로 나누려 함
rbf = 곡선으로 감싸듯이 나눌 수 있음
```

이 시각화 덕분에 kernel은 단순한 옵션명이 아니라, 모델이 만들 수 있는 경계의 모양을 바꾸는 설정이라는 점이 더 분명해졌다.

## gamma 값 비교

RBF kernel에서는 `gamma`가 중요하다.
`gamma`는 하나의 데이터 포인트가 주변에 미치는 영향 범위를 조절한다.

```text
gamma 작음 -> 영향 범위가 넓음, 경계가 부드럽고 단순해질 수 있음
gamma 큼 -> 영향 범위가 좁음, 경계가 데이터에 민감해질 수 있음
```

`gamma`가 너무 작으면 모델이 데이터의 복잡한 패턴을 충분히 따라가지 못해 과소적합될 수 있다.
반대로 너무 크면 훈련 데이터의 작은 변화에도 경계가 민감하게 흔들려 과대적합될 수 있다.

그래서 오늘은 `gamma` 후보를 바꿔가며 train accuracy, test accuracy, support vector count를 함께 확인했다.

```text
gamma
train_accuracy
test_accuracy
support_vector_count
```

이 과정에서 SVC도 결국 하이퍼파라미터에 따라 단순한 모델이 될 수도 있고, 복잡한 모델이 될 수도 있다는 점을 확인했다.

## PCA 시각화

Iris 데이터는 feature가 4개이다.
feature가 4개면 사람이 2차원 평면에서 결정 경계를 바로 보기 어렵다.

그래서 오늘은 PCA를 사용해 4개의 feature를 2개의 주성분으로 줄였다.

```text
4개 feature
-> PCA
-> 2개 주성분
-> 결정 경계 시각화 가능
```

단, PCA 시각화는 이해를 돕기 위한 것이다.
실제 모델 성능 평가는 원래 feature를 사용한 결과와 구분해서 봐야 한다.

이 점이 중요했다.
그림이 보기 좋다고 해서 그것이 실제 전체 feature 기반 모델의 성능을 그대로 의미하는 것은 아니다.

## SVR

SVC를 정리한 뒤에는 SVM을 회귀 문제에 적용하는 SVR도 다뤘다.

```text
SVC = 클래스를 나누는 분류 모델
SVR = 숫자를 예측하는 회귀 모델
```

SVC는 클래스 사이의 결정 경계와 마진을 중요하게 본다.
반면 SVR은 숫자를 예측하는 함수 주변에 허용 오차 범위를 둔다.

```text
SVC의 마진 = 분류 경계 주변의 여유 공간
SVR의 epsilon tube = 예측 함수 주변의 허용 오차 범위
```

즉 SVC는 어느 class에 속하는지를 판단하고, SVR은 연속형 숫자를 예측한다.

## epsilon tube

SVR에서 가장 중요한 개념은 `epsilon tube`였다.

SVR은 예측 함수 주변에 일정한 폭의 tube를 만든다.
실제값이 이 tube 안에 들어오면 그 오차는 크게 문제 삼지 않는다.

```text
tube 안쪽 오차 = 허용
tube 바깥 오차 = 벌점 부여
```

여기서 `epsilon`은 허용할 오차 폭을 의미한다.

```text
epsilon 큼 -> 더 많은 오차를 허용, 모델이 단순해질 수 있음
epsilon 작음 -> 작은 오차에도 민감, 모델이 복잡해질 수 있음
```

SVC에서 support vector가 결정 경계에 영향을 주는 중요한 데이터였던 것처럼, SVR에서도 tube 경계에 걸리거나 tube 밖에 있는 데이터들이 모델 학습에 중요하게 작용한다.

## SVR의 C, epsilon, gamma

SVR에서도 하이퍼파라미터가 중요했다.

```text
C = tube 밖 오차를 얼마나 강하게 벌줄지 결정
epsilon = 허용 오차 범위의 폭
gamma = RBF kernel에서 한 데이터가 주변에 영향을 주는 범위
```

`C`가 작으면 tube 밖 오차를 어느 정도 허용한다.
반대로 `C`가 크면 tube 밖 오차를 강하게 줄이려고 한다.

```text
C 작음 -> 오차를 더 허용, 예측 함수가 단순해질 수 있음
C 큼 -> 오차를 줄이려 함, 예측 함수가 복잡해질 수 있음
```

`gamma`는 RBF kernel에서 예측 곡선의 민감도와 연결된다.

```text
gamma 작음 -> 넓게 영향을 줌, 부드러운 예측 곡선
gamma 큼 -> 가까운 데이터에 민감, 복잡한 예측 곡선
```

이 흐름은 SVC에서 배운 `C`, `gamma`와 연결되었다.
분류에서는 결정 경계가 달라지고, 회귀에서는 예측 함수의 모양이 달라진다고 이해하면 된다.

## Kernel별 SVR 비교

오늘 SVR에서는 `linear`, `poly`, `rbf` kernel을 비교했다.

```text
linear = 직선에 가까운 예측
poly = 다항식 형태의 곡선 예측
rbf = 부드러운 비선형 곡선 예측
```

같은 데이터라도 kernel을 바꾸면 모델이 표현할 수 있는 예측 함수의 모양이 달라진다.

선형 관계가 강한 데이터라면 `linear`가 단순하고 해석하기 쉽다.
하지만 입력과 target 사이의 관계가 직선 하나로 설명되기 어렵다면 `rbf` 같은 비선형 kernel이 더 적합할 수 있다.

오늘은 사인 곡선 형태의 예제 데이터를 통해 kernel별 예측 곡선 차이를 직관적으로 확인했다.

## SVR에서 스케일링

SVR은 스케일링이 특히 중요했다.

SVC처럼 SVR도 거리와 kernel 계산의 영향을 받기 때문에 feature 스케일이 다르면 학습이 불안정해질 수 있다.

```python
X_train_scaled = X_scaler.fit_transform(X_train)
X_test_scaled = X_scaler.transform(X_test)
```

여기서도 훈련 데이터에는 `fit_transform()`, 평가 데이터에는 `transform()`만 사용한다.
평가 데이터로 스케일링 기준을 다시 학습하면 데이터 누수가 생길 수 있기 때문이다.

SVR에서는 target인 `y`도 함께 스케일링했다.
그 이유는 `epsilon`과 `C`가 target 값의 크기에 영향을 받기 때문이다.

```python
y_train_scaled = y_scaler.fit_transform(y_train.to_numpy().reshape(-1, 1))
```

다만 scikit-learn의 `SVR`은 `y`를 1차원 형태로 받기 때문에 학습할 때는 `ravel()`을 사용했다.

```python
model.fit(X_train_scaled, y_train_scaled.ravel())
```

이 부분도 중요했다.

```text
스케일링할 때 y = 2차원 필요
SVR에 fit할 때 y = 1차원 필요
```

## 예측값 복원

SVR에서 y를 스케일링하면 모델의 예측값도 스케일링된 단위로 나온다.
그 상태로는 사람이 원래 target 단위로 해석하기 어렵다.

그래서 예측 후에는 `inverse_transform()`으로 다시 원래 단위로 복원해야 한다.

```python
y_pred_scaled = model.predict(X_test_scaled)
y_pred = y_scaler.inverse_transform(y_pred_scaled.reshape(-1, 1))
```

California Housing 데이터에서는 target인 `MedHouseVal`이 주택 가격 단위이므로, 복원된 값으로 봐야 실제 가격 예측처럼 해석할 수 있다.

```text
스케일링된 예측값 = 모델 계산용 값
복원된 예측값 = 사람이 해석할 수 있는 원래 단위 값
```

## SVR 평가 지표

SVR은 회귀 모델이므로 분류의 accuracy가 아니라 회귀 평가 지표를 사용한다.

```text
R2 = 실제값의 변화를 모델이 얼마나 설명하는지
MAE = 절대 오차의 평균
RMSE = 제곱 오차 평균에 루트를 씌운 값
```

R2는 높을수록 좋다.
1에 가까우면 모델이 실제값의 변화를 잘 설명한다는 뜻이다.

```text
R2가 1에 가까움 -> 잘 예측
R2가 0에 가까움 -> 평균값으로 예측하는 것과 비슷
R2가 음수 -> 평균값으로 예측하는 것보다 못함
```

MAE와 RMSE는 낮을수록 좋다.

```text
MAE = |실제값 - 예측값|의 평균
RMSE = sqrt((실제값 - 예측값)^2의 평균)
```

RMSE는 큰 오차에 더 민감하다.
오차를 제곱하기 때문에 큰 실수를 더 크게 반영한다.

## GridSearchCV로 SVR 튜닝

마지막으로 RBF SVR의 하이퍼파라미터 후보를 `GridSearchCV`로 비교했다.

```text
C
epsilon
gamma
```

SVR은 이 값들에 따라 예측 곡선과 성능이 크게 달라질 수 있다.
그래서 후보를 정하고 교차검증으로 비교했다.

```python
GridSearchCV(
    estimator=SVR(kernel="rbf"),
    param_grid=param_grid,
    scoring="neg_root_mean_squared_error"
)
```

scikit-learn에서는 점수는 클수록 좋다는 규칙을 사용하기 때문에, RMSE처럼 낮을수록 좋은 지표는 음수 형태로 반환된다.

```text
neg_root_mean_squared_error = RMSE에 음수를 붙인 점수
값이 클수록 좋음
실제 RMSE는 낮을수록 좋음
```

교차검증으로 고른 모델도 마지막에는 따로 둔 평가셋에서 다시 확인해야 한다.
이 점은 결정트리의 GridSearchCV에서 배운 내용과 같았다.

## 오늘 느낀 점

오늘은 모델의 성능을 높이는 것보다 모델이 왜 그렇게 판단하는지를 더 많이 생각한 날이었다.

결정트리에서는 트리 깊이와 leaf 개수를 보면서 모델 복잡도를 확인했다.
SVC에서는 마진, 서포트 벡터, C, gamma, kernel을 통해 결정 경계가 어떻게 달라지는지 확인했다.
SVR에서는 epsilon tube, C, epsilon, gamma, kernel을 통해 예측 함수가 어떻게 달라지는지 확인했다.

```text
결정트리의 복잡도 = depth, split, leaf로 조절
SVC의 복잡도 = C, kernel, gamma로 조절
SVR의 복잡도 = C, epsilon, gamma, kernel로 조절
```

서로 다른 모델이지만 공통점도 있었다.

```text
너무 단순하면 과소적합
너무 복잡하면 과대적합
적절한 복잡도를 찾아야 함
```

오늘 가장 크게 남은 점은 모델을 비교할 때 하나의 점수만 보면 안 된다는 것이다.

```text
train score
test score
모델 복잡도
시각화
평가지표
```

이 요소들을 함께 봐야 모델을 더 정확하게 판단할 수 있다.

## 내일로 이어질 내용

오늘은 결정트리, SVC, SVR까지 이어지면서 단일 모델의 복잡도를 조절하고 성능을 평가하는 흐름을 봤다.
다음에는 여러 모델을 결합하는 앙상블로 넘어가면, 단일 모델의 판단을 비교하는 단계에서 여러 모델의 예측을 조합하는 단계로 확장될 수 있다.

오늘 배운 결정트리, SVC, SVR의 하이퍼파라미터 비교 경험은 이후 GridSearchCV와 앙상블 모델을 이해하는 데도 계속 연결될 것 같다.
