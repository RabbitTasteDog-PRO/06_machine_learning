# 회고록: 머신러닝_2026-06-29

오늘은 선형 회귀와 규제 선형 회귀를 다시 보면서, 코드가 돌아가는 것과 개념을 이해하는 것이 다르다는 걸 많이 느꼈다.

처음에는 `predict`, `score`, `coef_`, `Pipeline`, `StandardScaler` 같은 코드들이 각각 따로 보였는데, 질문을 이어가다 보니 결국 하나의 흐름이었다.

```text
데이터를 나눈다.
훈련 데이터 기준으로 전처리한다.
모델을 학습한다.
평가 데이터로 예측한다.
실제값과 예측값을 비교한다.
모델이 학습한 계수를 확인한다.
```

오늘은 특히 "왜 이렇게 써야 하는지"를 많이 확인했다.

---

## 오늘 커밋한 내용

```text
c173ff7 -record - ElasticNet 개념 및 예제 업데이트2
c10f9d9 -record - ElasticNet 개념 및 예제 업데이트
6a8c92a -record - 다항 feature
         - L2 : Ridge
         - L1 : Lasso 예제 및 값 확인
252515d -record - 다항 feature, 다항 선형의 LinerRegression 모델 평가지표 확인
c0dc539 -record - 하이퍼파라미터 기본 update
```

커밋 흐름을 보면 오늘은 기본 회귀에서 다항 feature와 규제 모델로 넘어갔다.
특히 Ridge, Lasso, ElasticNet을 다루면서 모델 성능뿐 아니라 회귀계수의 크기까지 같이 확인했다.

## train/test와 X/y 다시 정리

오늘 계속 헷갈렸던 부분 중 하나는 `X_train`, `X_test`, `y_train`, `y_test`였다.

```text
X_train = 학습용 문제
y_train = 학습용 정답

X_test = 평가용 문제
y_test = 평가용 정답
```

모델 학습은 다음처럼 한다.

```python
model.fit(X_train, y_train)
```

이 코드는 훈련 데이터를 정답 데이터에 넣는 것이 아니다.

```text
X_train 문제를 봤을 때
y_train 정답이 나오도록
모델이 규칙을 학습한다
```

예측할 때는 정답을 주지 않는다.

```python
y_pred = model.predict(X_test)
```

모델은 `X_test`만 보고 예측값을 만든다.
그리고 그 예측값을 실제 정답인 `y_test`와 비교한다.

```python
r2_score(y_test, y_pred)
mean_absolute_error(y_test, y_pred)
root_mean_squared_error(y_test, y_pred)
```

정리하면 다음과 같다.

```text
fit(X_train, y_train) = 문제와 정답을 같이 보고 학습
predict(X_test)       = 문제만 보고 예측
평가 지표              = y_test와 y_pred 비교
```

## 스케일링과 데이터 누수

스케일링에서도 중요한 차이를 다시 확인했다.

```python
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

`X_train`에는 `fit_transform()`을 쓴다.

```text
fit       = 스케일링 기준을 학습
transform = 학습한 기준으로 변환
```

즉 `X_train`으로 평균과 표준편차를 구하고, 그 기준으로 `X_train`을 변환한다.

반면 `X_test`에는 `transform()`만 쓴다.
평가 데이터는 시험 문제처럼 남겨야 하므로, 평가 데이터로 다시 평균과 표준편차를 구하면 안 된다.

```text
X_train = 기준을 배워도 되는 데이터
X_test  = 이미 배운 기준으로만 변환해야 하는 데이터
```

테스트 데이터까지 사용해서 전처리 기준을 배우면 평가 데이터 정보가 학습 과정에 섞인다.
이것을 데이터 누수라고 한다.

## 실제값, 예측값, 오차

평가 데이터 예측값은 다음처럼 만든다.

```python
y_pred = model.predict(X_test_scaled)
```

여기서 실제값은 `y_test`이고, 예측값은 `y_pred`이다.

```python
pred_df = pd.DataFrame({
    "actual": y_test,
    "pred": y_pred,
    "error": y_pred - y_test
})
```

오차는 실제값과 예측값의 차이다.
다만 방향은 정하기 나름이다.

```text
y_test - y_pred = 실제값 - 예측값
y_pred - y_test = 예측값 - 실제값
```

중요한 것은 어떤 기준으로 뺐는지 알고 해석하는 것이다.

```text
예측값 - 실제값 > 0  -> 모델이 실제보다 크게 예측
예측값 - 실제값 < 0  -> 모델이 실제보다 작게 예측
```

MAE와 RMSE는 오차 지표이기 때문에 낮을수록 좋다.
R2는 모델이 정답의 변화를 얼마나 잘 설명하는지 보는 지표라서 보통 높을수록 좋다.

## 회귀계수와 coef_

`trained_model.coef_`도 오늘 다시 정리했다.

`coef_`는 학습된 회귀 모델의 회귀계수이다.
선형 회귀식으로 보면 다음과 같다.

```text
예측값 = 계수1 * feature1 + 계수2 * feature2 + ... + 절편
```

여기서 각 feature 앞에 붙는 숫자가 회귀계수이다.

```python
trained_model.coef_
```

회귀계수는 각 feature가 예측값에 어떤 방향으로 얼마나 영향을 주는지 나타낸다.

```text
양수 계수 = feature가 커질수록 예측값 증가
음수 계수 = feature가 커질수록 예측값 감소
0에 가까운 계수 = 영향이 작음
```

단, feature들의 스케일이 다르면 계수 크기를 그대로 비교하면 안 된다.
스케일이 다르면 숫자 단위 때문에 계수 크기가 달라질 수 있다.

## Ridge, Lasso, ElasticNet

오늘 규제 선형 회귀에서 Ridge, Lasso, ElasticNet을 비교했다.

```text
Ridge      = L2 규제
Lasso      = L1 규제
ElasticNet = L1 + L2 규제
```

규제는 모델의 회귀계수가 너무 커지지 않게 제한하는 역할을 한다.
계수가 너무 크면 학습 데이터에 과하게 맞춰질 수 있기 때문이다.

Ridge의 `alpha` 값도 비교했다.

```python
alphas = [0.01, 0.1, 1, 10, 100]
```

각 alpha마다 Pipeline을 만들고 학습, 예측, 평가를 반복했다.

```python
ridge_result = []

for alpha in alphas:
    pipeline_ridge = Pipeline([
        ("scaler", StandardScaler()),
        ("model", Ridge(alpha=alpha))
    ])

    pipeline_ridge.fit(X_train, y_train)
    pred = pipeline_ridge.predict(X_test)

    trained_model = pipeline_ridge.named_steps["model"]

    ridge_result.append({
        "alpha": alpha,
        "R2": r2_score(y_test, pred),
        "RMSE": root_mean_squared_error(y_test, pred),
        "coef_square_sum": np.sum(trained_model.coef_ ** 2)
    })

ridge_result_df = pd.DataFrame(ridge_result)
ridge_result_df.round(6)
```

여기서 `coef_square_sum`은 회귀계수들을 제곱해서 더한 값이다.
Ridge는 계수 제곱합에 벌점을 주는 방식이므로, alpha가 커질수록 보통 계수 크기가 줄어드는 흐름을 확인할 수 있다.

## 주피터 노트북의 함정

오늘 가장 짜증났던 부분은 `trained_model` 변수였다.

노트북 파일에는 `trained_model = ...`을 정의한 코드가 없는데도 에러가 나지 않았다.
이유는 주피터 노트북의 커널 메모리 때문이다.

주피터는 셀을 실행하면 변수가 커널 메모리에 남는다.
그래서 어떤 셀을 지웠더라도, 커널을 재시작하지 않으면 이전에 만들어진 변수가 계속 살아 있을 수 있다.

```text
파일에는 trained_model이 없음
하지만 예전에 실행한 적 있음
커널 메모리에는 아직 있음
그래서 에러가 안 남
하지만 값은 엉뚱할 수 있음
```

이 문제를 피하려면 중요한 실습에서는 다음을 해봐야 한다.

```text
Kernel Restart & Run All
```

이렇게 했을 때 에러가 나면, 노트북 메모리에 남아 있던 변수에 의존하고 있었다는 뜻이다.

오늘의 경우에는 반복문 안에서 현재 모델을 명확히 꺼내야 했다.

```python
trained_model = pipeline_ridge.named_steps["model"]
```

## Pipeline

Pipeline은 전처리와 모델을 하나로 묶어서 실행하는 도구이다.

```python
pipeline = Pipeline([
    ("scaler", StandardScaler()),
    ("model", Ridge(alpha=alpha))
])
```

이렇게 만들면 학습할 때는 다음 순서로 동작한다.

```text
1. X_train으로 scaler 기준 학습
2. X_train 변환
3. 변환된 X_train으로 모델 학습
```

예측할 때는 다음 순서로 동작한다.

```text
1. X_test를 기존 scaler 기준으로 변환
2. 변환된 X_test로 모델 예측
```

그래서 전처리를 따로따로 처리할 때보다 실수를 줄일 수 있다.

## pandas DataFrame과 컬럼명 실수

오늘 `KeyError`도 다시 만났다.

```python
coef_df = pd.DataFrame({
    "feature": X_train.columns,
    "codf": multi_lin_reg.coef_
}).sort_values("coef", ascending=False)
```

여기서 문제는 컬럼을 `"codf"`로 만들고, 정렬할 때 `"coef"`를 찾았다는 것이다.
pandas는 없는 컬럼명을 찾으면 `KeyError`를 낸다.

수정하면 다음과 같다.

```python
coef_df = pd.DataFrame({
    "feature": X_train.columns,
    "coef": multi_lin_reg.coef_
}).sort_values("coef", ascending=False)
```

리스트를 DataFrame으로 바꾸는 것도 정리했다.

```python
results_df = pd.DataFrame(results)
```

`results`가 딕셔너리들의 리스트이면, 각 딕셔너리의 key가 DataFrame의 컬럼명이 된다.

## axis 정리

`axis=0`과 `axis=1`도 다시 확인했다.

```text
axis=0 = 행 방향으로 내려가며 계산, 결과는 열별로 나옴
axis=1 = 열 방향으로 옆으로 계산, 결과는 행별로 나옴
```

예를 들어 `df.mean(axis=0)`은 각 열의 평균을 구한다.
`df.mean(axis=1)`은 각 행의 평균을 구한다.

```text
axis=0 = 세로 계산, 열별 결과
axis=1 = 가로 계산, 행별 결과
```

## 오늘 자주 만난 실수

오늘 나온 실수들은 대부분 코드가 틀렸다기보다, 변수의 의미나 실행 상태를 놓쳐서 생겼다.

```text
shape를 shpae로 오타
coef 컬럼을 codf로 만든 뒤 coef로 정렬
trained_model을 반복문 안에서 새로 정의하지 않음
predict() 안에 평가용 X 데이터를 넣지 않음
results가 아니라 coef_df를 출력함
MSE, MAE, RMSE 컬럼에 서로 다른 함수를 잘못 넣음
노트북 커널에 남아 있는 변수 때문에 에러가 안 남
```

특히 오늘 배운 가장 큰 교훈은 다음이다.

```text
코드가 에러 없이 실행된다고 해서 항상 맞는 코드는 아니다.
```

노트북에서는 이전 실행 상태가 남아 있을 수 있으므로, 이상하면 커널을 재시작하고 위에서부터 다시 실행해봐야 한다.

## 오늘 정리

오늘은 회귀 모델을 평가하고, 규제 모델을 비교하는 데 집중했다.

핵심 흐름은 다음과 같다.

```text
X는 문제, y는 정답
train은 학습용, test는 평가용
fit은 학습
predict는 예측
score와 metrics는 평가
coef_는 모델이 학습한 회귀계수
Pipeline은 전처리와 모델을 묶는 도구
StandardScaler는 훈련 데이터 기준으로만 fit
Ridge/Lasso/ElasticNet은 회귀계수에 규제를 주는 모델
```

오늘은 단순히 코드를 채우는 것보다, 각 코드가 왜 필요한지 더 많이 물어본 날이었다.
특히 `fit(X_train, y_train)`이 문제와 정답을 같이 보고 규칙을 배우는 과정이라는 점, `X_test`에는 학습한 기준만 적용해야 한다는 점, `coef_`가 모델이 학습한 feature별 가중치라는 점이 중요했다.
