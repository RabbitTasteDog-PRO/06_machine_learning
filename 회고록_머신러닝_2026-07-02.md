# 회고록: 머신러닝_2026-07-02

오늘은 앙상블 중에서도 Voting을 중심으로 공부했다.

어제까지는 결정트리와 SVC처럼 하나의 모델이 데이터를 어떻게 나누는지 봤다면, 오늘은 여러 모델의 예측을 모아서 최종 판단을 만드는 방법을 배웠다.

```text
단일 모델 = 한 모델이 혼자 예측
앙상블 = 여러 모델의 예측을 합쳐 최종 예측
Voting = 여러 모델의 투표 또는 확률 평균으로 결정
```

오늘의 핵심은 단순히 `VotingClassifier`를 실행하는 것이 아니라, 모델이 무엇을 입력받고 무엇을 출력하는지 다시 정확히 구분하는 것이었다.

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
05_ensemble/01_ensemble_voting.ipynb
04_decision_tree_svm/practice/03_svr.ipynb
```

오늘 질문 흐름은 특히 `05_ensemble/01_ensemble_voting.ipynb`의 Voting 분류 코드와 연결되어 있었다.

## 오늘 커밋한 내용

```text
172621b -record - 머신러닝 분류모델
         - SVR문제 업데이트
817a988 -record - 머신러닝 분류모델
         - 앙상블 업데이트
8d1db1c -record - 머신러닝 분류모델
         - 앙상블
         - Voting 개념 정리
```

커밋 흐름을 보면 오늘은 SVR 문제풀이 이후 앙상블과 Voting 개념 정리로 넘어갔다.

```text
SVR 문제풀이
-> 앙상블 개념
-> VotingClassifier
-> Hard Voting
-> Soft Voting
-> VotingRegressor
```

## 오늘 질문 흐름

오늘 직접 정리한 질문은 다음과 같았다.

```text
X_train은 학습 데이터이고 X_test는 학습된 정답 데이터인가?
predict()로 y 예측값을 보는데 왜 X_test_scaled를 넣는가?
Hard Voting과 Soft Voting 코드에서 문법 에러가 난 것 같은데 어디가 문제인가?
R2와 RMSE는 각각 무엇을 의미하는가?
```

질문의 공통점은 모델 이름보다 머신러닝 코드의 기본 구조를 정확히 이해하는 데 있었다.

```text
X = 문제 데이터
y = 정답 데이터
predict(X) = 문제를 보고 정답을 예측
y_pred = 모델이 만든 예측 정답
y_test = 실제 정답
```

## X_train, X_test, y_train, y_test

오늘 가장 먼저 다시 잡은 개념은 `X_train`, `X_test`, `y_train`, `y_test`였다.

```text
X_train = 학습용 문제 데이터
y_train = 학습용 정답 데이터

X_test = 평가용 문제 데이터
y_test = 평가용 정답 데이터
```

처음에는 `X_test`를 학습된 정답 데이터처럼 생각할 수 있었지만, 실제로는 정답이 아니라 시험 문제에 해당한다.

모델 학습은 다음처럼 한다.

```python
model.fit(X_train, y_train)
```

이 코드는 모델에게 학습용 문제와 정답을 함께 보여주는 과정이다.

```text
X_train 문제를 봤을 때
y_train 정답이 나오도록
규칙을 학습한다
```

예측할 때는 정답을 주지 않는다.

```python
y_pred = model.predict(X_test)
```

모델은 `X_test`만 보고 예측값을 만든다.
그리고 그 예측값을 실제 정답인 `y_test`와 비교한다.

```python
accuracy_score(y_test, y_pred)
```

정리하면 다음과 같다.

```text
X_train = 공부할 문제
y_train = 공부할 문제의 정답

X_test = 시험 문제
y_test = 시험 문제의 실제 정답
```

## predict()에는 왜 X가 들어가는가?

오늘 보팅 코드에서 헷갈렸던 부분은 다음 코드였다.

```python
hard_y_pred = hard_voting_clf.predict(voting_X_test_scaled)
```

`hard_y_pred`는 분명히 y의 예측값이다.
그런데 `predict()` 안에는 `voting_y_test`가 아니라 `voting_X_test_scaled`가 들어간다.

이유는 간단하다.
예측은 항상 문제를 보고 정답을 맞히는 과정이기 때문이다.

```text
voting_X_test_scaled = 스케일링된 시험 문제
hard_voting_clf.predict(...) = 이 문제들을 보고 y를 예측
hard_y_pred = 모델이 만든 y 예측값
```

정답인 `voting_y_test`는 예측할 때 넣는 것이 아니라, 예측이 끝난 뒤 비교할 때 사용한다.

```python
hard_y_pred = hard_voting_clf.predict(voting_X_test_scaled)
accuracy_score(voting_y_test, hard_y_pred)
```

비유하면 다음과 같다.

```text
voting_X_test_scaled = 시험지 문제
hard_y_pred = 내가 쓴 답
voting_y_test = 실제 정답지
```

이 구분이 오늘 중요한 포인트였다.
`predict()`는 정답을 받는 함수가 아니라, 문제를 받아 정답을 만들어내는 함수이다.

## 앙상블과 Voting

앙상블은 여러 모델을 함께 사용해 최종 예측을 만드는 방법이다.

```text
앙상블 = 여러 모델의 판단을 합침
```

하나의 모델은 특정 패턴에는 강하지만 다른 패턴에는 약할 수 있다.
서로 다른 모델을 함께 사용하면 한 모델의 실수를 다른 모델이 보완할 수 있다.

오늘 Voting에서는 다음 세 모델을 함께 사용했다.

```text
LogisticRegression = 선형 분류 모델
KNeighborsClassifier = 거리 기반 분류 모델
DecisionTreeClassifier = 조건 분기 기반 분류 모델
```

이렇게 서로 다른 방식의 모델을 섞는 이유는 예측의 다양성을 만들기 위해서이다.
비슷한 모델만 여러 개 모으면 투표를 해도 결과가 크게 달라지지 않을 수 있다.

## Hard Voting

Hard Voting은 각 모델이 예측한 최종 class를 모아 다수결로 결정한다.

```text
모델 A 예측: 1
모델 B 예측: 1
모델 C 예측: 0

Hard Voting 최종 예측: 1
```

코드로는 다음처럼 설정한다.

```python
hard_voting_clf = VotingClassifier(
    estimators=[
        ("lr", lr_clf),
        ("knn", knn_clf),
        ("dt", dt_clf)
    ],
    voting="hard"
)
```

Hard Voting은 확률을 평균내지 않는다.
각 모델이 최종적으로 고른 class만 보고 다수결을 한다.

```text
Hard Voting = class 예측값의 다수결
```

그래서 `predict()`의 결과는 최종 class이다.

```python
hard_y_pred = hard_voting_clf.predict(voting_X_test_scaled)
```

여기서도 입력은 X이고, 출력은 예측된 y이다.

## Soft Voting

Soft Voting은 각 모델이 계산한 class별 확률을 평균낸다.

```text
모델 A의 class 1 확률: 0.6
모델 B의 class 1 확률: 0.7
모델 C의 class 1 확률: 0.8

평균 확률: 0.7
```

평균 확률이 더 높은 class를 최종 예측으로 선택한다.

```text
Soft Voting = class 확률의 평균
```

코드로는 다음처럼 설정한다.

```python
soft_voting_clf = VotingClassifier(
    estimators=[
        ("lr", LogisticRegression(max_iter=5000, random_state=42)),
        ("knn", KNeighborsClassifier(n_neighbors=5)),
        ("dt", DecisionTreeClassifier(max_depth=4, random_state=42))
    ],
    voting="soft"
)
```

Soft Voting을 사용하려면 개별 모델들이 `predict_proba()`를 제공해야 한다.
확률을 평균내야 하기 때문이다.

```text
predict() = 최종 class 예측
predict_proba() = class별 확률 예측
```

오늘 확인한 코드에서 문법 자체는 문제가 없었다.
다만 결과표를 만들 때 컬럼명이 서로 달라질 수 있었다.

기존 hard voting 결과표의 컬럼명은 `model`이었다.
그런데 soft voting 결과를 추가할 때 `'모델'`이라고 쓰면 새 컬럼이 따로 생긴다.

```python
pd.DataFrame([{
    "model": "soft_voting",
    "train_accuracy": soft_voting_clf.score(voting_X_train_scaled, voting_y_train),
    "test_accuracy": soft_voting_clf.score(voting_X_test_scaled, voting_y_test)
}])
```

이처럼 같은 표에 붙일 때는 컬럼명을 맞춰야 한다.

```text
기존 컬럼: model
추가 행 컬럼: model
```

## Hard Voting과 Soft Voting의 차이

오늘 정리한 Hard Voting과 Soft Voting의 차이는 다음과 같다.

```text
Hard Voting
= 각 모델의 최종 class를 다수결

Soft Voting
= 각 모델의 class별 확률을 평균
```

예를 들어 세 모델이 class 1에 대해 다음 확률을 냈다고 하자.

```text
모델 A: 0.4
모델 B: 0.3
모델 C: 0.2
```

Soft Voting에서는 평균이 0.3이므로 class 1보다는 class 0으로 갈 가능성이 높다.

반대로 다음과 같다면:

```text
모델 A: 0.6
모델 B: 0.7
모델 C: 0.8
```

평균이 0.7이므로 class 1로 예측할 가능성이 높다.

Hard Voting은 확률의 크기를 보지 않고 최종 선택지만 본다.
Soft Voting은 각 모델이 얼마나 확신했는지까지 반영한다.

## VotingRegressor

오늘 Voting은 분류뿐 아니라 회귀에도 연결되었다.

분류에서는 class를 투표하지만, 회귀에서는 숫자 예측값을 평균낸다.

```text
VotingClassifier = class를 투표 또는 확률 평균으로 결정
VotingRegressor = 숫자 예측값을 평균
```

예를 들어 세 회귀 모델의 예측이 다음과 같다면:

```text
모델 A 예측: 100
모델 B 예측: 110
모델 C 예측: 90
```

VotingRegressor의 최종 예측은 평균인 100이 된다.

```text
(100 + 110 + 90) / 3 = 100
```

분류는 class를 맞히는 문제이고, 회귀는 숫자를 예측하는 문제이므로 평가 지표도 달라진다.

## R2와 RMSE

오늘 마지막에는 회귀 평가 지표인 R2와 RMSE도 다시 정리했다.

```text
R2 = 모델이 실제값의 변화를 얼마나 잘 설명하는지 보는 지표
RMSE = 예측값과 실제값의 오차 크기를 보는 지표
```

R2는 예상치라기보다는 설명력 점수에 가깝다.

```text
R2가 1에 가까움 -> 잘 예측함
R2가 0에 가까움 -> 평균값으로 찍는 것과 비슷함
R2가 음수 -> 평균값으로 찍는 것보다 못함
```

RMSE는 평균의 제곱이 아니라, 오차를 제곱한 값들의 평균에 다시 루트를 씌운 값이다.

```text
오차 = 실제값 - 예측값
MSE = 오차^2의 평균
RMSE = sqrt(MSE)
```

정리하면 다음과 같다.

```text
R2 = 높을수록 좋음
RMSE = 낮을수록 좋음
```

RMSE는 원래 target과 같은 단위로 해석할 수 있어서, 평균적으로 예측이 실제값과 얼마나 떨어져 있는지 이해하기 좋다.

## 오늘 느낀 점

오늘은 Voting 자체도 중요했지만, 그보다 `X`와 `y`의 역할을 다시 분명하게 잡은 것이 컸다.

```text
X = 문제
y = 정답
predict(X) = 문제를 보고 정답을 예측
y_pred = 모델이 만든 답
y_test = 실제 답
```

이 구조를 이해하니 hard voting과 soft voting도 더 자연스럽게 연결되었다.

```text
Hard Voting = 여러 모델이 낸 답을 다수결
Soft Voting = 여러 모델이 낸 확률을 평균
VotingRegressor = 여러 모델이 낸 숫자 예측값을 평균
```

오늘 가장 중요한 정리는 이것이었다.

```text
모델은 y를 입력받아 예측하는 것이 아니라,
X를 입력받아 y를 예측한다.
```

이 문장이 앞으로 분류, 회귀, 앙상블 코드를 읽을 때 계속 기준이 될 것 같다.

## 내일로 이어질 내용

오늘 Voting을 배우면서 여러 모델의 예측을 단순히 합치는 방법을 봤다.
이후에는 Bagging, Random Forest, Boosting, Stacking처럼 더 발전된 앙상블 방식으로 이어질 수 있다.

```text
Voting = 서로 다른 모델의 예측을 투표 또는 평균
Bagging = 데이터를 다르게 뽑아 여러 모델 학습
Boosting = 틀린 데이터를 다음 모델이 더 신경 쓰게 학습
Stacking = 여러 모델의 예측을 다시 다른 모델이 학습
```

오늘 배운 Voting은 앙상블의 가장 직관적인 출발점이었다.
