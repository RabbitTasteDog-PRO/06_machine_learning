
이번주부터 시작된 머신러닝....
어렵다...
어려워....
그래도.....

>머신러닝 기본 흐름부터 회귀 분석까지 한 번에 이어서 정리해보려고 한다.

---
> 감사합니다 CODEXS
---

처음에는 `train`, `test`, `X`, `y`, `fit`, `predict`, `score` 같은 단어들이 따로따로 보였다.
그런데 분류와 회귀 실습을 같이 해보니까 결국 머신러닝의 흐름은 비슷했다.

```text
데이터를 준비한다.
feature와 target을 나눈다.
학습용과 테스트용으로 나눈다.
필요한 전처리를 한다.
모델을 학습한다.
예측한다.
평가한다.
```

처음에는 코드 한 줄씩 따라가는 느낌이 컸다면, 
뭔진 알겠는데 연결이 잘 안되는 느낌... 

## 머신러닝 기본 흐름

머신러닝에서는 입력 데이터와 정답 데이터를 나누는 것이 먼저였다.

```text
X = 입력 데이터, 문제
y = 정답 데이터, 답
```

모델은 `X_train`과 `y_train`을 보고 학습한다.
그리고 처음 보는 데이터인 `X_test`를 보고 예측한 뒤, 실제 정답인 `y_test`와 비교해서 성능을 확인한다.

```text
fit()     = 학습
predict() = 예측
score()   = 평가
```

이 세 가지가 가장 기본이 되는 흐름이었다.
처음에는 함수 이름을 외우는 느낌이었지만, 지금은 역할로 이해하는 것이 더 중요하다고 느꼈다.

`fit()`은 모델이 데이터를 보고 규칙을 배우는 과정이다.
`predict()`는 학습된 모델이 새로운 입력을 보고 값을 예측하는 과정이다.
`score()`는 그 예측이 얼마나 괜찮았는지 확인하는 과정이다.

기본 구조는 다음처럼 볼 수 있다.

```python
# 모델이 학습용 문제와 정답을 보고 규칙을 배운다.
model.fit(X_train, y_train)

# 학습된 모델에게 테스트 문제만 주고 예측값을 만든다.
pred = model.predict(X_test)

# 예측값과 실제 정답을 비교해서 모델 성능을 확인한다.
score = model.score(X_test, y_test)

# 예측 결과 일부와 평가 점수를 출력한다.
print("예측:", pred[:5])
print("평가 점수:", score)
```

처음에는 `fit()`과 `predict()`가 비슷해 보였는데, 정답을 같이 넣는지 아닌지를 보면 구분하기 쉬웠다.

```text
fit(X_train, y_train) = 문제와 정답을 같이 보고 학습
predict(X_test)       = 문제만 보고 정답을 예측
score(X_test, y_test) = 예측 결과와 실제 정답을 비교
```

## train과 test를 나누는 이유

전체 데이터를 모두 학습에 사용하면 모델이 진짜로 배운 것인지, 그냥 외운 것인지 알기 어렵다.
그래서 데이터를 학습용과 테스트용으로 나눈다.

```python
# 데이터를 학습용과 테스트용으로 나누기 위한 함수
from sklearn.model_selection import train_test_split

# X는 입력 데이터, y는 정답 데이터이다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.2,     # 전체 데이터 중 20%를 테스트용으로 사용
    random_state=42    # 실행할 때마다 같은 결과가 나오도록 고정
)
```

`X_train`은 모델이 공부할 문제이고, `y_train`은 그 문제의 정답이다.
`X_test`는 모델이 처음 보는 시험 문제이고, `y_test`는 시험 문제의 실제 정답이다.

이 구조를 이해하고 나니까 이후에 나오는 분류, 회귀, 평가 지표도 같은 흐름 위에 있다는 것을 알 수 있었다.

예를 들어 붓꽃 데이터로 나누면 다음과 같다.

```python
# 붓꽃 예제 데이터와 train/test 분리 함수 불러오기
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split

# 붓꽃 데이터 불러오기
iris = load_iris()

# X는 꽃잎/꽃받침 길이와 너비, y는 붓꽃 종류이다.
X = iris.data
y = iris.target

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.2,     # 20%는 테스트 데이터로 사용
    stratify=y,        # 정답 비율이 한쪽으로 몰리지 않게 유지
    random_state=42    # 결과를 고정해서 다시 실행해도 같게 만듦
)

# 나눠진 데이터의 크기를 확인한다.
print(X_train.shape, X_test.shape)
print(y_train.shape, y_test.shape)
```

`stratify=y`는 정답 비율을 비슷하게 유지하면서 데이터를 나누는 옵션이다.
분류 문제에서는 특정 클래스가 한쪽에 몰리지 않게 해주는 역할을 한다.

## 스케일링과 데이터 전처리

전처리는 모델이 데이터를 잘 학습할 수 있도록 데이터를 정리하는 과정이다.

이번에는 결측치 처리, 이상치 탐지, 범주형 데이터 인코딩, 스케일링을 정리했다.

```text
SimpleImputer  = 비어 있는 값을 평균, 중앙값, 최빈값 등으로 채움
IsolationForest = 다른 데이터와 다른 패턴을 보이는 값을 이상치로 찾음
LabelEncoder    = 문자 범주를 숫자 라벨로 바꿈
OneHotEncoder   = 범주를 여러 개의 0과 1 컬럼으로 바꿈
StandardScaler  = 평균 0, 표준편차 1 기준으로 스케일링
MinMaxScaler    = 값을 0과 1 사이 범위로 스케일링
RobustScaler    = 중앙값과 사분위 범위를 기준으로 스케일링
```

특히 스케일링은 KNN에서 중요했다.
KNN은 가까운 이웃을 찾는 모델이기 때문에 feature의 숫자 크기가 다르면 거리 계산이 한쪽으로 치우칠 수 있다.

예를 들어 길이와 무게를 같이 사용할 때, 무게 값의 숫자 범위가 훨씬 크면 모델이 무게를 더 중요하게 보는 것처럼 계산될 수 있다.
그래서 feature의 단위를 맞춰주는 과정이 필요했다.

중요한 점은 `fit()`은 학습 데이터에만 해야 한다는 것이다.

```python
# X_train의 평균과 표준편차를 기준으로 스케일링 기준을 학습한다.
scaler.fit(X_train)

# 학습 데이터는 학습한 기준으로 변환한다.
X_train_scaled = scaler.transform(X_train)

# 테스트 데이터도 같은 기준으로만 변환한다.
X_test_scaled = scaler.transform(X_test)
```

테스트 데이터는 모델이 처음 보는 데이터처럼 남겨야 한다.
그래서 `X_train`으로만 기준을 만들고, 그 기준을 `X_test`에도 똑같이 적용한다.

전처리 코드는 다음처럼 정리할 수 있었다.

```python
# numpy는 결측치(np.nan)를 만들 때 사용했다.
import numpy as np

# pandas는 표 형태의 데이터를 만들고 다룰 때 사용했다.
import pandas as pd

# 결측치 처리 도구
from sklearn.impute import SimpleImputer

# 범주형 데이터 인코딩과 수치 데이터 스케일링 도구
from sklearn.preprocessing import OneHotEncoder, StandardScaler

# 예시용 데이터프레임 생성
df = pd.DataFrame({
    "age": [20, 25, np.nan, 30],
    "city": ["Seoul", "Busan", "Seoul", "Incheon"],
    "score": [70, 80, 90, 100]
})

# 결측치 처리
# age 컬럼의 비어 있는 값을 평균값으로 채우도록 설정한다.
imputer = SimpleImputer(strategy="mean")

# age 컬럼에 결측치 처리 결과를 다시 넣는다.
df[["age"]] = imputer.fit_transform(df[["age"]])

# 범주형 데이터 인코딩
# city처럼 문자로 된 범주를 0과 1 컬럼으로 바꾸기 위한 인코더이다.
encoder = OneHotEncoder(sparse_output=False)

# city 컬럼을 원핫 인코딩한다.
city_encoded = encoder.fit_transform(df[["city"]])

# 인코딩 결과를 다시 보기 쉬운 표 형태로 바꾼다.
city_df = pd.DataFrame(
    city_encoded,
    columns=encoder.get_feature_names_out(["city"])
)

# 수치 데이터 스케일링
# age와 score의 숫자 범위를 표준점수 기준으로 맞춘다.
scaler = StandardScaler()
scaled_data = scaler.fit_transform(df[["age", "score"]])

# 인코딩 결과와 스케일링 결과를 확인한다.
print(city_df)
print(scaled_data)
```

실제 학습 흐름에서는 스케일러도 모델처럼 학습 데이터 기준으로만 `fit()`해야 한다.

## KNN 분류

KNN은 가까운 이웃을 보고 정답을 판단하는 모델이다.

도미와 빙어 데이터를 사용했을 때, 모델은 물고기의 길이와 무게를 보고 가까운 데이터가 어떤 종류인지 확인했다.
가까운 이웃 중 도미가 많으면 도미로 예측하고, 빙어가 많으면 빙어로 예측한다.

```text
KNN 분류 = 가까운 이웃들의 다수결로 정답을 예측
```

`n_neighbors`는 몇 개의 이웃을 볼지 정하는 값이다.
이 값이 너무 작으면 가까운 데이터 하나하나에 민감해질 수 있고, 너무 크면 전체적인 평균에 가까워져서 둔해질 수 있다.

그래서 KNN에서는 이웃 수와 스케일링이 같이 중요했다.
가까운 이웃을 찾는 기준 자체가 거리이기 때문이다.

도미와 빙어를 분류하는 흐름은 다음과 같다.

```python
# 배열 계산을 위해 numpy를 사용한다.
import numpy as np

# 데이터를 나누고, KNN 분류 모델과 스케일러를 불러온다.
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier
from sklearn.preprocessing import StandardScaler

# 도미 데이터: 길이와 무게
bream_length = [25.4, 26.3, 26.5, 29.0, 29.0, 29.7, 29.7, 30.0, 30.0, 30.7]
bream_weight = [242.0, 290.0, 340.0, 363.0, 430.0, 450.0, 500.0, 390.0, 450.0, 500.0]

# 빙어 데이터: 길이와 무게
smelt_length = [9.8, 10.5, 10.6, 11.0, 11.2, 11.3, 11.8, 11.8, 12.0, 12.2]
smelt_weight = [6.7, 7.5, 7.0, 9.7, 9.8, 8.7, 10.0, 9.9, 9.8, 12.2]

# 도미와 빙어의 길이, 무게 데이터를 하나로 합친다.
fish_length = bream_length + smelt_length
fish_weight = bream_weight + smelt_weight

# 길이와 무게를 하나의 입력 데이터 X로 묶는다.
X = np.column_stack((fish_length, fish_weight))

# 도미는 1, 빙어는 0으로 정답 데이터를 만든다.
y = np.array([1] * len(bream_length) + [0] * len(smelt_length))

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    stratify=y,        # 도미와 빙어 비율을 비슷하게 유지
    random_state=42    # 결과 고정
)

# KNN은 거리 기반 모델이라 스케일링을 먼저 해준다.
scaler = StandardScaler()

# 학습 데이터 기준으로 평균과 표준편차를 학습한다.
scaler.fit(X_train)

# 학습 데이터와 테스트 데이터를 같은 기준으로 변환한다.
X_train_scaled = scaler.transform(X_train)
X_test_scaled = scaler.transform(X_test)

# 가까운 이웃 3개를 보는 KNN 분류 모델 생성
kn_clf = KNeighborsClassifier(n_neighbors=3)

# 스케일링된 학습 데이터로 모델을 학습한다.
kn_clf.fit(X_train_scaled, y_train)

# 테스트 데이터 기준 점수와 예측 결과를 확인한다.
print("테스트 점수:", kn_clf.score(X_test_scaled, y_test))
print("예측:", kn_clf.predict(X_test_scaled[:5]))
```

여기서 `1`은 도미, `0`은 빙어로 두었다.
모델은 길이와 무게를 보고 가까운 이웃을 찾은 뒤, 더 많은 쪽의 정답을 예측한다.

## KNN 회귀

KNN 회귀도 가까운 이웃을 본다는 점은 분류와 같았다.
다만 분류처럼 종류를 맞히는 것이 아니라 숫자를 예측한다.

농어 데이터를 예로 들면, 농어의 길이를 보고 무게를 예측했다.
예측하려는 농어와 길이가 비슷한 농어들을 찾고, 그 이웃들의 무게 평균을 사용해서 값을 예측한다.

```text
KNN 회귀 = 가까운 이웃들의 target 값을 평균내서 예측
```

여기서 KNN 회귀의 한계도 확인했다.
학습 데이터 범위 밖의 값을 예측할 때는 기존 이웃들의 평균을 사용하기 때문에, 입력값이 더 커져도 예측값이 충분히 커지지 못할 수 있다.

즉, KNN은 학습 데이터 안에서는 직관적이지만, 데이터 범위 밖으로 확장해서 예측하는 데에는 약점이 있었다.

KNN 회귀 코드는 다음처럼 작성했다.

```python
# 배열 데이터를 만들기 위해 numpy를 사용한다.
import numpy as np

# 데이터를 나누고 KNN 회귀 모델을 사용하기 위한 import
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsRegressor

# 농어 길이 데이터
perch_length = np.array([
    8.4, 13.7, 15.0, 16.2, 17.4, 18.0, 18.7, 19.0,
    19.6, 20.0, 21.0, 21.0, 21.0, 21.3, 22.0, 22.0,
    22.0, 22.0, 22.0, 22.5, 22.5, 22.7, 23.0, 23.5,
    24.0, 24.0, 24.6, 25.0, 25.6, 26.5, 27.3, 27.5,
    27.5, 27.5, 28.0, 28.7, 30.0, 32.8, 34.5, 35.0,
    36.5, 36.0, 37.0, 37.0, 39.0, 39.0, 39.0, 40.0,
    40.0, 40.0, 40.0, 42.0, 43.0, 43.0, 43.5, 44.0
])

# 농어 무게 데이터
perch_weight = np.array([
    5.9, 32.0, 40.0, 51.5, 70.0, 100.0, 78.0, 80.0,
    85.0, 85.0, 110.0, 115.0, 125.0, 130.0, 120.0, 120.0,
    130.0, 135.0, 110.0, 130.0, 150.0, 145.0, 150.0, 170.0,
    225.0, 145.0, 188.0, 180.0, 197.0, 218.0, 300.0, 260.0,
    265.0, 250.0, 250.0, 300.0, 320.0, 514.0, 556.0, 840.0,
    685.0, 700.0, 700.0, 690.0, 900.0, 650.0, 820.0, 850.0,
    900.0, 1015.0, 820.0, 1100.0, 1000.0, 1100.0, 1000.0, 1000.0
])

# sklearn 모델은 입력 데이터를 2차원 형태로 받는다.
X = perch_length.reshape(-1, 1)

# 예측해야 하는 정답은 농어 무게이다.
y = perch_weight

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    random_state=42    # 결과 고정
)

# 가까운 이웃 3개의 평균으로 값을 예측하는 KNN 회귀 모델
kn_reg = KNeighborsRegressor(n_neighbors=3)

# 길이와 무게의 관계를 학습한다.
kn_reg.fit(X_train, y_train)

# 학습 점수, 테스트 점수, 새 농어 길이에 대한 예측값을 확인한다.
print("학습 점수:", kn_reg.score(X_train, y_train))
print("테스트 점수:", kn_reg.score(X_test, y_test))
print("50cm 농어 예측:", kn_reg.predict([[50]]))
```

이 코드를 통해 KNN 회귀는 가까운 이웃의 평균으로 예측한다는 것을 확인할 수 있었다.
다만 50cm처럼 학습 데이터 범위를 넘어가는 값을 넣으면 한계가 보인다.

---

## 선형 회귀

선형 회귀는 입력값과 정답 사이의 관계를 가장 잘 설명하는 직선을 찾는 모델이다.

feature가 하나라면 다음과 같이 볼 수 있다.

```text
y = w * x + b
```

여기서 `w`는 기울기이고, `b`는 절편이다.
`x`가 변할 때 `y`가 얼마나 변하는지를 직선으로 표현한다.

feature가 여러 개라면 다음처럼 여러 입력값을 함께 사용한다.

```text
y = (w1 * x1) + (w2 * x2) + (w3 * x3) + b
```

농어 데이터에서는 `length`, `height`, `width`를 feature로 사용하고, `weight`를 target으로 사용했다.
모델은 각 feature마다 가중치를 학습하고, 그 값을 조합해서 무게를 예측했다.

KNN 회귀와 비교했을 때 선형 회귀는 입력값이 커졌을 때
예측값도 직선 관계에 따라 같이 변할 수 있다.... 라고 한다....


### 선형회기 
```python
# 선형 회귀 모델과 데이터 분리 함수 불러오기
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

# 농어 길이는 입력 데이터, 농어 무게는 정답 데이터이다.
X = perch_length.reshape(-1, 1)
y = perch_weight

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    random_state=42    # 결과 고정
)

# 선형 회귀 모델 생성
lin_reg = LinearRegression()

# 학습 데이터로 직선의 기울기와 절편을 학습한다.
lin_reg.fit(X_train, y_train)

# 모델이 학습한 기울기와 절편, 평가 점수, 예측값을 확인한다.
print("기울기:", lin_reg.coef_)
print("절편:", lin_reg.intercept_)
print("학습 점수:", lin_reg.score(X_train, y_train))
print("테스트 점수:", lin_reg.score(X_test, y_test))
print("50cm 농어 예측:", lin_reg.predict([[50]]))
```

여기서 `coef_`는 기울기, `intercept_`는 절편이다.
선형 회귀는 학습을 통해 이 두 값을 찾는다.

## 다항 회귀

선형 회귀는 기본적으로 직선 관계를 학습한다.
하지만 현실 데이터가 항상 직선처럼 움직이지는 않는다.

농어의 길이와 무게도 길이가 커질수록 무게가 더 빠르게 증가할 수 있다.
이럴 때는 기존 feature를 그대로 쓰는 것보다, feature를 확장해서 곡선적인 관계를 표현할 수 있게 만든다.

```python
# 기존 feature를 다항 feature로 확장하는 도구
from sklearn.preprocessing import PolynomialFeatures

# degree=2는 x와 x^2 형태로 feature를 확장한다.
poly = PolynomialFeatures(degree=2, include_bias=False)

# 학습 데이터는 확장 기준을 학습하면서 변환한다.
X_train_poly = poly.fit_transform(X_train)

# 테스트 데이터는 학습된 기준으로만 변환한다.
X_test_poly = poly.transform(X_test)
```

`degree=2`는 기존 feature에 제곱항과 feature끼리 곱한 항을 추가하는 것이다.
즉, 모델 자체는 선형 회귀를 사용하지만 입력 feature를 확장해서 더 복잡한 관계를 학습할 수 있게 만든다.

여기서도 학습 데이터에는 `fit_transform()`을 사용하고, 테스트 데이터에는 `transform()`만 사용한다.
테스트 데이터에 `fit()`을 하면 평가 데이터의 정보를 미리 본 것이 되기 때문이다.

다항 회귀까지 연결하면 다음과 같다.

```python
# 선형 회귀 모델, 데이터 분리 함수, 다항 feature 생성 도구
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import PolynomialFeatures

# 농어 길이와 무게 데이터 준비
X = perch_length.reshape(-1, 1)
y = perch_weight

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    random_state=42    # 결과 고정
)

# 길이 x를 x, x^2 형태로 확장한다.
poly = PolynomialFeatures(degree=2, include_bias=False)

# 학습 데이터는 fit_transform, 테스트 데이터는 transform만 사용한다.
X_train_poly = poly.fit_transform(X_train)
X_test_poly = poly.transform(X_test)

# 확장된 feature를 선형 회귀 모델에 학습시킨다.
lin_reg = LinearRegression()
lin_reg.fit(X_train_poly, y_train)

# 50cm 농어도 같은 방식으로 다항 feature로 변환한다.
input_data = [[50]]
input_poly = poly.transform(input_data)

# 만들어진 feature 이름과 평가 점수, 예측값을 확인한다.
print("변환된 feature:", poly.get_feature_names_out())
print("학습 점수:", lin_reg.score(X_train_poly, y_train))
print("테스트 점수:", lin_reg.score(X_test_poly, y_test))
print("50cm 농어 예측:", lin_reg.predict(input_poly))
```

`degree=2`를 사용하면 `x`와 `x^2`가 함께 들어간다.
그래서 직선 하나로 설명하기 어려운 관계를 조금 더 자연스럽게 표현할 수 있었다.

## 보스턴 주택가격 회귀

보스턴 주택가격 데이터에서는 집값인 `MEDV`를 target으로 두고, 나머지 컬럼들을 feature로 사용했다.

```text
feature = 집값을 예측할 때 참고하는 정보
target  = 모델이 맞춰야 하는 실제 집값
```

범죄율, 방 개수, 세금, 저소득층 비율 같은 여러 feature가 집값과 어떤 관계를 가지는지 확인했다.
단순히 모델을 바로 학습시키는 것이 아니라, 먼저 데이터의 구조와 feature의 의미를 보는 과정이 필요했다.

그 다음 `LinearRegression()`으로 학습하고, 학습셋과 평가셋의 점수를 비교했다.
학습셋 점수만 높다고 좋은 모델이라고 말하기는 어렵다.
모델이 이미 본 데이터를 잘 맞히는 것과 처음 보는 데이터를 잘 맞히는 것은 다르기 때문이다.

보스턴 주택가격 데이터는 다음 흐름으로 학습했다.

```python
# 표 형태 데이터를 읽기 위해 pandas를 사용한다.
import pandas as pd

# 선형 회귀 모델과 train/test 분리 함수 불러오기
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

# 보스턴 주택가격 데이터 불러오기
boston_housing_df = pd.read_csv("./02_regression/data/boston_housing_train.csv")

# MEDV는 정답이므로 제외하고 나머지 컬럼을 feature로 사용한다.
X = boston_housing_df.drop(columns="MEDV").to_numpy()

# MEDV는 모델이 맞춰야 하는 집값이다.
y = boston_housing_df["MEDV"].to_numpy()

# 학습용 데이터와 테스트용 데이터를 나눈다.
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.2,     # 전체 데이터 중 20%를 테스트용으로 사용
    random_state=42    # 결과 고정
)

# 선형 회귀 모델 생성 및 학습
lin_reg = LinearRegression()
lin_reg.fit(X_train, y_train)

# 학습셋과 평가셋 점수를 비교하고, 일부 예측값을 실제값과 비교한다.
print("학습셋 R^2:", lin_reg.score(X_train, y_train))
print("평가셋 R^2:", lin_reg.score(X_test, y_test))
print("예측값:", lin_reg.predict(X_test[:5]))
print("실제값:", y_test[:5])
```

`MEDV`는 정답으로 따로 빼고, 나머지 컬럼들을 입력 데이터로 사용했다.
이렇게 해야 모델이 집값을 맞히기 위해 다른 feature들을 참고하게 된다.

## 회귀 평가 지표

회귀에서는 `score()`가 기본적으로 `R^2` 값을 반환한다.
하지만 회귀 모델을 볼 때는 오차 지표도 같이 확인해야 한다.

```text
R^2  = target의 변화를 모델이 얼마나 잘 설명하는지 보는 지표
MSE  = 오차를 제곱해서 평균낸 값
MAE  = 오차의 절댓값을 평균낸 값
RMSE = MSE에 제곱근을 씌운 값
```

`R^2`는 높을수록 좋고, `MSE`, `MAE`, `RMSE`는 낮을수록 좋다.

특히 `MAE`와 `RMSE`는 실제 예측값이 정답과 얼마나 차이 나는지 이해하는 데 도움이 됐다.
점수 하나만 보고 끝내는 것이 아니라, 모델이 어느 정도의 오차를 내는지 같이 확인해야 한다.

평가 지표는 다음처럼 계산했다.

```python
# RMSE 계산을 위해 numpy를 사용한다.
import numpy as np

# 회귀 평가 지표 함수들
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

# 학습 데이터와 테스트 데이터에 대한 예측값을 각각 만든다.
y_train_pred = lin_reg.predict(X_train)
y_test_pred = lin_reg.predict(X_test)

# R^2는 모델이 target의 변화를 얼마나 설명하는지 확인하는 지표이다.
train_r2 = r2_score(y_train, y_train_pred)
test_r2 = r2_score(y_test, y_test_pred)

# MSE는 오차를 제곱해서 평균낸 값이다.
test_mse = mean_squared_error(y_test, y_test_pred)

# MAE는 오차의 절댓값을 평균낸 값이다.
test_mae = mean_absolute_error(y_test, y_test_pred)

# RMSE는 MSE에 제곱근을 씌운 값이다.
test_rmse = np.sqrt(test_mse)

# 계산한 평가 지표들을 출력한다.
print("학습셋 R^2:", train_r2)
print("평가셋 R^2:", test_r2)
print("MSE:", test_mse)
print("MAE:", test_mae)
print("RMSE:", test_rmse)
```

`R^2`만 보면 모델이 어느 정도 설명하는지는 알 수 있지만, 실제로 얼마나 틀렸는지는 바로 느껴지지 않을 수 있다.
그래서 `MAE`, `RMSE`처럼 오차 크기를 같이 보는 것이 좋았다.

## 이번에 정리된 흐름

이번 학습을 통해 분류와 회귀가 완전히 다른 흐름이 아니라는 것을 알게 됐다.
문제의 종류가 다를 뿐, 전체 과정은 비슷했다.

```text
분류 = 종류를 예측
회귀 = 숫자를 예측
```

분류에서는 정확도를 많이 보고, 회귀에서는 `R^2`, `MSE`, `MAE`, `RMSE` 같은 지표를 함께 본다.
하지만 둘 다 결국 feature를 보고 target을 예측한다는 점은 같다.

처음에는 `X_train`, `X_test`, `y_train`, `y_test` 같은 변수 
이름이 복잡하게 느껴져 다시 한번 정리해본다

```text
X_train = 모델이 공부할 문제
y_train = 공부할 문제의 정답
X_test  = 모델이 시험 볼 문제
y_test  = 시험 문제의 실제 정답
```

그리고 모델 학습의 흐름은 다음처럼 이어진다.

```text
fit()으로 학습하고
predict()로 예측하고
score()와 평가 지표로 확인한다.
```

## 느낀 점

> 코드보다는 흐름을 인지하자.

각 함수가 무엇을 하는지 외우려고 했으나....
하지만 실습을 반복하면서 중요한 것은 함수 이름 자체보다 데이터가 어떤 상태로 이동하는지 보는 것이라는 생각이 들었고,

`X`와 `y`를 나누고, `train`과 `test`를 나누고, 
전처리를 학습 데이터 기준으로 적용하고, 모델을 학습한 뒤 평가한다.

이 흐름을 이해하는게 먼저 인것 같다

KNN은 가까운 데이터를 기준으로 판단한다는 점에서 직관적이었다.
선형 회귀는 데이터의 관계를 식으로 표현한다는 점에서 
모델이 무엇을 학습하는지 더 명확하게 보였다.

다항 회귀는 feature를 확장하면 같은 선형 회귀 모델로도 
더 복잡한 관계를 표현할 수 있다는 점

아직 모든 개념이 완전히 익숙한 것은 아니지만, 
이제 머신러닝 실습을 볼 때 어디서부터 어디까지가 준비이고, 
어디서부터가 학습과 평가인지 구분할 수 있게 됐다.
