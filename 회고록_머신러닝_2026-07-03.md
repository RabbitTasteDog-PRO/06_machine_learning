# 회고록: 머신러닝_2026-07-03

오늘은 앙상블 모델의 확장 흐름과 함께, 머신러닝을 공부하면서 자주 부딪히는 Python 문법과 개발 도구 사용법을 정리했다.

어제 Voting을 통해 여러 모델의 예측을 합치는 방식을 봤다면, 오늘은 Boosting, Stacking, XGBoost, LightGBM, CatBoost처럼 앙상블을 더 강하게 활용하는 모델들로 범위가 넓어졌다.

```text
Voting = 여러 모델의 예측을 투표 또는 평균으로 합침
Boosting = 앞 모델의 실수를 다음 모델이 보완하며 순차적으로 학습
Stacking = 여러 모델의 예측 결과를 다시 다른 모델이 학습
```

그리고 오늘 질문을 통해 L1, L2 규제와 Python의 문법 특징도 함께 점검했다.

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
05_ensemble/03_boosting.ipynb
05_ensemble/04_stacking.ipynb
05_ensemble/05_xgboost.ipynb
05_ensemble/06_lightgbm_optional.ipynb
05_ensemble/07_catboost_optional.ipynb
05_ensemble/practice/03_boosting_stacking.ipynb
05_ensemble/data/adult_income.csv
05_ensemble/data/california_housing.csv
```

파일 흐름을 보면 오늘은 기본 앙상블에서 한 단계 더 나아가 Boosting 계열 모델과 Stacking 실습을 진행한 날이었다.

```text
Boosting
-> Stacking
-> XGBoost
-> LightGBM
-> CatBoost
-> Boosting/Stacking 문제풀이
```

## 오늘 커밋한 내용

오늘 날짜 기준으로 확인된 커밋은 없었다.

```text
오늘 커밋 없음
```

## 오늘 질문 흐름

오늘 직접 정리한 질문은 다음과 같았다.

```text
L1, L2 규제가 무엇인가?
L1은 라쏘, L2는 릿지인가?
PyCharm에서도 F12가 함수 찾기인가?
True, False처럼 대문자로 입력하는 언어가 있는가?
SQL은 개발 언어라고 보기 애매하지 않은가?
Ruby의 true, false 표기는 어떤가?
Python에서 없는 변수를 실행해도 에러가 안 나는 것처럼 보이는 이유는 무엇인가?
```

오늘 질문의 공통점은 머신러닝 모델 자체뿐 아니라, 모델을 다루는 개발 환경과 언어 문법의 헷갈리는 지점을 정리하는 데 있었다.

## L1 규제와 L2 규제

오늘 먼저 확인한 개념은 L1, L2 규제였다.

규제는 모델의 가중치가 너무 커지지 않게 제한해서 과대적합을 줄이는 방법이다.

```text
L1 규제 = Lasso
L2 규제 = Ridge
```

L1 규제는 가중치의 절댓값 합에 벌점을 준다.

```text
L1 penalty = lambda * sum(|w|)
```

특징은 일부 가중치를 정확히 0으로 만들 수 있다는 점이다.
그래서 중요하지 않은 변수를 제거하는 변수 선택 효과가 있다.

L2 규제는 가중치의 제곱합에 벌점을 준다.

```text
L2 penalty = lambda * sum(w^2)
```

L2는 가중치를 전체적으로 작게 만들지만, 보통 0으로 만들지는 않는다.
그래서 모든 변수를 유지하면서 모델을 안정화하는 데 강하다.

정리하면 다음과 같다.

```text
Lasso = L1 규제 = 변수 선택 효과
Ridge = L2 규제 = 계수 안정화 효과
Elastic Net = L1 + L2
```

## PyCharm에서 함수나 변수 정의로 이동하기

오늘 PyCharm 단축키도 확인했다.

VS Code에서는 `F12`가 정의 이동으로 자주 쓰이지만, PyCharm에서는 기본적으로 다음 단축키를 많이 사용한다.

```text
Windows/Linux: Ctrl + B
macOS: Cmd + B
```

마우스로는 `Ctrl + Click` 또는 macOS의 `Cmd + Click`으로 정의 위치로 이동할 수 있다.

`Ctrl + F12`는 함수 정의 이동이라기보다는 현재 파일의 구조를 보는 기능에 가깝다.

따라서 PyCharm에서 함수나 변수 선언 위치를 찾을 때는 `Ctrl + B` 또는 `Cmd + B`를 먼저 기억하면 된다.

## 언어별 True, False 표기

오늘 헷갈렸던 부분은 Python의 불리언 표기였다.

Python은 다음처럼 첫 글자만 대문자를 쓴다.

```python
True
False
None
```

반면 많은 범용 프로그래밍 언어는 소문자를 사용한다.

```text
JavaScript = true / false / null
Java = true / false / null
C# = true / false / null
Go = true / false / nil
Ruby = true / false / nil
```

대문자 표기를 자주 쓰는 쪽도 있다.

```text
R = TRUE / FALSE
SQL = TRUE / FALSE
Excel = TRUE / FALSE
```

Python에서 특히 주의할 오타는 다음과 같다.

```python
Ture   # 틀림
True   # 맞음
```

정리하면 Python에서는 `True`, `False`, `None`을 하나의 세트로 외우는 것이 좋다.

## SQL은 프로그래밍 언어인가?

오늘 SQL에 대해서도 정리했다.

SQL은 Python, Java, JavaScript 같은 범용 프로그래밍 언어라기보다는 데이터베이스에 질의하는 언어로 보는 것이 자연스럽다.

```text
Python, Java, JavaScript = 범용 프로그래밍 언어
SQL = 데이터 질의 언어
HTML = 마크업 언어
CSS = 스타일시트 언어
```

SQL도 조건, 함수, 프로시저 등을 사용하면 프로그래밍처럼 다룰 수 있지만, 기본 목적은 데이터를 조회하고 조작하는 것이다.

```sql
SELECT name, age
FROM users
WHERE age >= 20;
```

따라서 SQL은 실무에서 보통 프로그래밍 언어라기보다 쿼리 언어라고 부르는 것이 더 정확하다.

## Python에서 없는 변수가 실행되는 것처럼 보이는 이유

오늘 Python에 대해 가장 답답했던 부분은 없는 변수를 실행했는데도 에러가 안 나는 것처럼 보이는 상황이었다.

Python에서 정말로 없는 변수를 읽으면 원래는 에러가 난다.

```python
print(x)
```

`x`를 만든 적이 없다면 다음과 같은 에러가 발생하는 것이 정상이다.

```text
NameError: name 'x' is not defined
```

그런데 Jupyter Notebook, Colab, PyCharm 콘솔에서는 이전에 실행한 변수가 메모리에 남아 있을 수 있다.

```python
x = 10
```

이 코드를 예전에 실행해두면, 이후 코드에서 다시 `x`를 정의하지 않아도 다음 코드가 실행될 수 있다.

```python
print(x)
```

이 경우 코드 파일만 보면 없는 변수처럼 보이지만, 실제 실행 환경에는 이전 값이 남아 있는 것이다.

그래서 노트북 환경에서는 다음 습관이 중요하다.

```text
Kernel Restart
Run All
처음부터 다시 실행해서 에러 확인
```

정리하면 Python 자체가 없는 변수를 마음대로 실행하는 것은 아니다.
다만 대화형 실행 환경에서는 이전 실행 상태가 남아 있어서 코드가 정상처럼 보일 수 있다.

## 오늘 느낀 점

오늘은 머신러닝 개념과 개발 환경의 불편함이 같이 드러난 날이었다.

L1, L2 규제는 단순히 이름을 외우는 것이 아니라, 모델의 계수를 어떻게 줄이는지에 따라 Lasso와 Ridge의 성격이 달라진다는 점이 중요했다.

또 Python은 쉬운 언어라고 하지만 실제로는 대소문자 규칙, 동적 타입, 이전 실행 상태 유지 때문에 처음 공부할 때 헷갈리는 부분이 많다.
특히 노트북이나 콘솔에서는 코드만 믿지 말고, 커널을 재시작한 뒤 처음부터 다시 실행해보는 습관이 필요하다.

오늘의 핵심 정리는 다음과 같다.

```text
L1 = Lasso = 일부 계수를 0으로 만들어 변수 선택
L2 = Ridge = 계수를 전체적으로 줄여 모델 안정화
PyCharm 정의 이동 = Ctrl + B 또는 Cmd + B
Python 불리언 = True / False / None
없는 변수는 원래 NameError가 정상
노트북에서는 이전 실행 상태가 남을 수 있음
```

머신러닝을 공부할 때 모델 공식이나 성능 점수만 보는 것이 아니라, 코드를 실행하는 환경과 언어 문법까지 같이 정확히 잡아야 한다는 점을 다시 확인했다.
