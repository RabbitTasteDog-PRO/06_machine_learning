# 6주차 머신러닝

## 6주차를 마치며

이번주는 머신러닝 세 번째 주.

5주차에는 분류 모델과 앙상블 모델을 한 번에 많이 보면서, 모델들이 데이터를 어떻게 판단하는지 따라가는 데 집중했다.

6주차는 그 다음 단계였다.

이번주는 단순히 모델을 하나 더 배우는 느낌이 아니라, 머신러닝 흐름 전체를 조금 더 넓게 보는 주였다.

```text
차원축소
-> 비지도학습
-> 군집화
-> 고객 세분화
-> 교차검증
-> 하이퍼파라미터 튜닝
-> 모델 저장
-> 평가지표
-> 추천 시스템
```

정리해보니 생각보다 많은 내용을 지나왔다.

어렵다.
아직도 어렵다.

그래도 이번주는 모델을 실행하는 것보다, 모델을 어떻게 비교하고 해석하고 다시 사용할 수 있게 만들지에 대해 더 많이 생각한 주였다.

5주차까지는 이런 질문을 많이 했다.

```text
이 모델은 어떤 모델인가?
fit()과 predict()는 어떻게 쓰는가?
분류와 회귀는 무엇이 다른가?
```

이번주는 질문이 조금 바뀌었다.

```text
이 데이터는 어떤 구조를 가지고 있는가?
이 모델의 결과를 어떻게 믿을 수 있는가?
평가 지표는 무엇을 봐야 하는가?
추천 결과는 어떤 기준으로 만들어지는가?
```

머신러닝이 단순히 모델을 고르고 학습시키는 작업이 아니라는 것을 다시 느꼈다.

---

## 이번 주 학습 흐름

이번주 학습은 크게 다음 흐름으로 이어졌다.

```text
PCA / LDA
-> KMeans
-> DBSCAN
-> RFM 고객 세분화
-> KFold / StratifiedKFold
-> cross_val_score / cross_validate
-> GridSearchCV / RandomizedSearchCV
-> HyperOpt
-> joblib 모델 저장
-> 분류 평가지표
-> 회귀 평가지표
-> 회귀 기반 추천
-> 코사인 유사도
-> 콘텐츠 기반 영화 추천
```

이번 주 회고에서는 기록된 학습 흐름을 기준으로, 차원축소와 비지도학습을 시작점으로 묶어 정리했다.

이번주는 앞에서 배운 지도학습 모델을 넘어서, 정답 없이 데이터 구조를 찾는 비지도학습과 모델을 더 안정적으로 평가하는 방법을 배웠다.

그리고 마지막에는 추천 시스템으로 넘어가면서 지금까지 배운 전처리, 회귀, 벡터화, 유사도 계산이 실제 서비스 형태로 어떻게 연결될 수 있는지도 확인했다.

---

## 차원축소

이번주 초반에는 PCA와 LDA를 정리했다.

차원축소는 feature 수를 줄이면서도 데이터의 중요한 패턴은 최대한 유지하는 방법이다.

처음에는 feature가 많으면 정보가 많아서 무조건 좋은 것 아닌가 싶었다.

하지만 feature가 너무 많아지면 거리 기반 알고리즘에서 계산이 어려워지고, 노이즈가 늘어나서 오히려 모델이 데이터 구조를 잘 못 볼 수 있다.

```text
차원축소 = feature 수를 줄이되 중요한 정보는 최대한 유지하는 방법
```

PCA와 LDA는 비슷해 보이지만 기준이 다르다.

```text
PCA = 데이터가 많이 퍼진 방향을 찾음
LDA = class가 잘 나뉘는 방향을 찾음
```

PCA는 target을 사용하지 않는다.
데이터 자체의 분산이 큰 방향을 찾는다.

반면 LDA는 target class를 사용한다.
그래서 분류 문제에서 class를 잘 구분하는 방향을 찾는 데 초점이 있다.

정리하면 다음과 같다.

```text
PCA = 비지도 방식의 차원축소
LDA = 지도 정보를 사용하는 차원축소
```

또 중요한 점은 스케일링이었다.

PCA는 분산이 큰 방향을 찾기 때문에 feature마다 숫자 범위가 다르면 값이 큰 feature가 결과를 지배할 수 있다.

그래서 `StandardScaler`로 평균 0, 표준편차 1 수준으로 맞춘 뒤 PCA를 적용했다.

```text
거리 기반 모델
분산을 보는 모델
좌표와 방향을 보는 모델

이런 모델들은 스케일링 영향을 많이 받는다.
```

이 부분은 KMeans와 DBSCAN에서도 계속 반복됐다.

---

## 비지도학습과 KMeans

비지도학습은 정답 없이 데이터 안의 구조를 찾는 방식이다.

지도학습에서는 `X`와 `y`가 있었다.

```text
X = 문제
y = 정답
```

그런데 비지도학습에서는 보통 `y`를 사용하지 않는다.

```text
비지도학습 = 정답 없이 X만 보고 데이터의 패턴을 찾음
```

KMeans는 대표적인 군집화 알고리즘이다.

```text
KMeans = 데이터를 k개의 군집으로 나누는 알고리즘
```

KMeans는 중심점을 기준으로 데이터를 묶는다.

```text
1. k개의 중심점을 정한다.
2. 각 데이터가 가장 가까운 중심점에 배정된다.
3. 배정된 데이터의 평균으로 중심점을 다시 옮긴다.
4. 이 과정을 반복한다.
```

처음에는 군집 번호가 실제 class 번호와 같은 의미라고 착각하기 쉬웠다.

하지만 KMeans가 만든 `cluster 0`, `cluster 1`, `cluster 2`는 모델이 임의로 붙인 그룹 이름일 뿐이다.

```text
cluster 번호 != 실제 class 번호
```

Iris 데이터처럼 실제 target이 있는 경우에는 군집 결과와 실제 품종을 비교할 수 있다.
하지만 그 target은 학습에 사용한 것이 아니라, 결과를 해석하기 위해 나중에 비교한 것이다.

KMeans에서 특히 기억할 점은 두 가지였다.

```text
KMeans는 k를 사람이 먼저 정해야 한다.
KMeans는 거리 기반 모델이라 스케일링이 중요하다.
```

`Elbow Method`로 k 후보를 비교하면서 inertia가 어떻게 줄어드는지도 확인했다.

```text
inertia = 각 데이터가 자신이 속한 중심점에서 얼마나 떨어져 있는지의 합
```

k가 커지면 군집이 많아지기 때문에 inertia는 줄어들 수밖에 없다.
그래서 단순히 inertia가 가장 낮은 k를 고르는 것이 아니라, 감소 폭이 꺾이는 지점을 봐야 한다.

이 부분은 모델 성능을 볼 때 숫자 하나만 보면 안 된다는 점과도 연결됐다.

---

## DBSCAN

KMeans 다음에는 DBSCAN을 정리했다.

KMeans가 중심점과 거리를 기준으로 군집을 나눈다면, DBSCAN은 밀도를 기준으로 군집을 찾는다.

```text
KMeans = 중심점 기준
DBSCAN = 밀도 기준
```

DBSCAN에서 중요한 값은 `eps`와 `min_samples`였다.

```text
eps = 이웃으로 볼 반경
min_samples = 핵심 데이터로 판단하기 위한 최소 이웃 개수
```

그리고 데이터 포인트는 크게 세 가지로 나뉜다.

```text
core point = 주변에 이웃이 충분히 많은 점
border point = core point 근처에 있지만 스스로는 조건을 만족하지 못하는 점
noise point = 어떤 군집에도 속하기 어려운 점
```

DBSCAN은 반달 모양 데이터처럼 KMeans가 잘 나누기 어려운 구조에서 차이를 보기 좋았다.

KMeans는 중심점을 기준으로 나누기 때문에 둥근 모양의 군집에는 잘 맞지만, 곡선형 구조에서는 어색하게 나뉠 수 있다.

반면 DBSCAN은 밀도가 이어지는 영역을 따라 군집을 확장하기 때문에 곡선형 군집을 더 자연스럽게 찾을 수 있었다.

하지만 DBSCAN도 쉬운 모델은 아니었다.

`eps`를 조금만 바꿔도 결과가 크게 달라졌다.

```text
eps가 너무 작음 = 대부분 noise가 될 수 있음
eps가 너무 큼 = 여러 군집이 하나로 합쳐질 수 있음
min_samples가 커짐 = core point 조건이 엄격해짐
```

이번에 `plt.subplots(2, 2)`로 여러 파라미터 결과를 비교하면서 `axes.ravel()`도 사용했다.

처음에는 axes가 왜 바로 반복문에서 안 맞는지 헷갈렸다.
알고 보니 `plt.subplots(2, 2)`의 axes는 2차원 배열 형태였다.

```python
for ax, params in zip(axes.ravel(), param_candidates):
    ...
```

이런 작은 문법도 시각화할 때 자주 발목을 잡는다.

그래도 이번에는 단순히 에러만 해결한 것이 아니라, 왜 2차원 배열을 1차원으로 펴야 하는지까지 같이 정리했다.

---

## RFM 고객 세분화

이번주에 가장 실무적인 느낌이 났던 부분은 RFM 고객 세분화였다.

RFM은 고객의 구매 행동을 세 가지 feature로 요약하는 방법이다.

```text
R = Recency, 최근 구매일
F = Frequency, 구매 빈도
M = Monetary, 구매 금액
```

거래 데이터는 처음에는 주문 한 건이 한 행으로 되어 있다.
하지만 고객 세분화를 하려면 고객 한 명이 한 행이 되도록 다시 집계해야 한다.

```text
거래 단위 데이터
-> 고객 단위 RFM feature
-> 스케일링
-> KMeans 군집화
-> 군집별 RFM 평균 해석
```

여기서 전처리도 중요했다.

```text
CustomerID 결측치 제거
취소 주문 제거
Quantity가 0 이하인 행 제거
UnitPrice가 0 이하인 행 제거
```

처음에는 모델에 넣는 코드가 더 중요해 보였는데, RFM에서는 모델보다 데이터 정리가 더 중요하게 느껴졌다.

정상 구매 행동만 남기고 고객 단위로 집계해야 그 다음 군집화가 의미를 가진다.

그리고 KMeans가 만든 군집 번호만 보고 끝내면 안 된다.

```text
cluster 0
cluster 1
cluster 2
```

이 숫자 자체에는 VIP, 이탈 위험, 신규 고객 같은 의미가 없다.

군집별 RFM 평균을 보고 사람이 해석해야 한다.

```text
Recency가 낮고 Frequency와 Monetary가 높음
-> 최근에도 자주 많이 구매한 고객

Recency가 높고 Frequency가 낮음
-> 오래전에 한두 번 구매한 고객
```

즉 모델은 비슷한 고객을 묶어줄 뿐이고, 그 묶음에 이름을 붙이는 것은 사람이 해야 한다.

이 부분이 인상적이었다.

머신러닝 결과는 자동으로 의미까지 만들어주는 것이 아니라, 사람이 도메인 기준으로 해석해야 한다.

---

## 교차검증

다음으로는 모델 평가 흐름으로 넘어갔다.

교차검증은 모델을 한 번만 train/test로 나눠 평가했을 때 생길 수 있는 불안정함을 줄이기 위한 방법이다.

```text
교차검증 = 학습 데이터를 여러 fold로 나누어 여러 번 검증하는 방법
```

한 번만 나누면 운 좋게 쉬운 test 데이터가 걸릴 수도 있고, 반대로 너무 어려운 데이터가 걸릴 수도 있다.

그래서 학습 데이터를 여러 조각으로 나누고, 각 조각을 한 번씩 검증용으로 사용한다.

```text
KFold = 데이터를 K개 fold로 나눔
StratifiedKFold = class 비율을 유지하면서 fold를 나눔
```

분류 문제에서는 특히 StratifiedKFold가 중요했다.

class 비율이 fold마다 크게 달라지면 검증 점수가 불안정해질 수 있기 때문이다.

교차검증을 할 때도 테스트 데이터는 마지막까지 남겨둔다.

```text
train 데이터 내부에서 교차검증
-> 모델 후보 비교
-> 선택한 모델을 전체 train 데이터로 다시 학습
-> test 데이터로 마지막 평가
```

이 흐름이 중요했다.

그리고 스케일링이 필요한 모델은 `Pipeline`으로 묶어야 한다.

```text
Pipeline = 전처리와 모델을 하나의 흐름으로 묶음
```

교차검증 바깥에서 전체 데이터를 먼저 스케일링하면 검증 데이터 정보가 학습 과정에 들어갈 수 있다.
이것이 데이터 누수로 이어질 수 있다.

그래서 교차검증 안에서 fold별로 스케일링과 학습이 같이 진행되도록 Pipeline을 사용한다.

이번주에는 `cross_val_score()`와 `cross_validate()`도 같이 봤다.

```text
cross_val_score() = 교차검증 점수 배열을 간단히 반환
cross_validate() = 여러 지표, 학습 시간, 검증 시간까지 반환
```

회귀 모델에서는 RMSE 점수가 음수로 나오는 것도 확인했다.

scikit-learn에서는 점수 체계가 보통 높을수록 좋은 방향으로 맞춰져 있어서, 오차처럼 낮을수록 좋은 값은 음수 형태로 반환될 수 있다.

처음에는 이상해 보였지만, 기준을 알면 이해할 수 있었다.

---

## 하이퍼파라미터 튜닝

교차검증 다음에는 하이퍼파라미터 튜닝으로 넘어갔다.

하이퍼파라미터는 모델이 학습으로 직접 찾는 값이 아니라, 사람이 먼저 정해야 하는 설정값이다.

```text
parameter = 모델이 학습 과정에서 찾는 값
hyperparameter = 사람이 먼저 정하는 모델 설정값
```

`GridSearchCV`는 후보 조합을 모두 확인한다.

```text
GridSearchCV = 후보 조합을 전부 비교
```

후보가 적을 때는 이해하기 쉽지만, 후보가 많아지면 시간이 오래 걸린다.

그래서 `RandomizedSearchCV`도 사용했다.

```text
RandomizedSearchCV = 후보 중 일부를 무작위로 뽑아 비교
```

오늘 중요한 점은 `best_params_`만 보는 것이 아니라는 점이었다.

```text
best_params_ = 가장 좋았던 하이퍼파라미터 조합
best_estimator_ = 그 조합으로 다시 학습된 최종 모델
cv_results_ = 후보별 교차검증 결과
```

튜닝을 하면 가장 좋은 조합이 나오지만, 그 결과를 최종 test 데이터로 다시 확인해야 한다.

즉 튜닝도 학습 과정의 일부이고, test 데이터는 마지막 평가용으로 남겨둬야 한다.

이 흐름은 계속 반복된다.

```text
학습 데이터 안에서 비교한다.
검증 점수로 후보를 고른다.
마지막에 test 데이터로 확인한다.
```

---

## HyperOpt

HyperOpt는 하이퍼파라미터 후보를 더 효율적으로 찾기 위한 도구였다.

GridSearchCV나 RandomizedSearchCV보다 구조가 조금 더 복잡했다.

```text
search_space 정의
-> objective 함수 정의
-> fmin으로 탐색
-> Trials에 결과 저장
-> best parameter 확인
```

여기서 핵심은 `objective()` 함수였다.

`objective()` 함수는 파라미터 후보를 받아 모델을 만들고, 교차검증 점수를 계산한 뒤, HyperOpt가 최소화할 `loss`를 반환한다.

```text
accuracy = 높을수록 좋음
loss = 낮을수록 좋음
```

그래서 accuracy를 그대로 반환하지 않고 음수로 바꾸어 loss로 반환했다.

```python
return {
    'loss': -mean_score,
    'mean_score': mean_score,
    'status': STATUS_OK,
    'params': params
}
```

처음에는 왜 굳이 음수로 바꾸는지 헷갈렸다.

하지만 HyperOpt는 최소화하는 방향으로 움직이기 때문에, 높은 accuracy를 낮은 loss처럼 보이게 바꿔야 한다.

이번에 `pprint()` 사용에서도 에러가 있었다.

```python
pprint('objective 함수 테스트', sample_result)
```

이 코드는 `pprint()`가 두 번째 인자를 출력할 객체가 아니라 다른 인자로 해석해서 문제가 생겼다.

그래서 제목과 딕셔너리를 나누어 출력하는 방식으로 정리했다.

```python
print('objective 함수 테스트')
pprint(sample_result)
```

작은 부분이지만, 함수마다 인자를 받는 방식이 다르다는 것을 다시 확인했다.

---

## joblib 모델 저장

이번주에는 학습한 모델을 저장하고 다시 불러오는 흐름도 정리했다.

모델을 학습하고 평가하는 것에서 끝나는 것이 아니라, 나중에 다시 사용할 수 있도록 파일로 저장하는 과정이다.

```text
joblib.dump() = Python 객체를 파일로 저장
joblib.load() = 저장된 객체를 다시 불러옴
```

특히 `Pipeline`으로 전처리와 모델을 묶은 뒤 저장했다.

```text
Pipeline 저장 = 전처리 기준과 모델을 함께 보관
```

모델만 저장하면 나중에 어떤 스케일러를 썼는지, 어떤 feature 순서로 학습했는지 헷갈릴 수 있다.

그래서 모델 파일과 메타데이터를 함께 관리했다.

```text
wine_logistic_pipline.joblib
wine_logistic_pipline_metadata.csv
```

메타데이터에는 target 이름, feature 정보, 선택된 파라미터 같은 설명을 남길 수 있다.

이번에 느낀 점은 모델 저장도 단순 백업이 아니라는 것이다.

나중에 다시 예측하려면 학습 당시의 전처리 흐름과 입력 feature 구조를 정확히 알아야 한다.

그리고 `joblib.load()`는 내부적으로 pickle 계열 로딩을 사용하기 때문에, 신뢰할 수 없는 파일을 함부로 열면 안 된다는 점도 확인했다.

---

## 분류 평가지표

분류 평가지표에서는 Titanic 데이터를 사용해 생존 여부를 예측하고 평가했다.

5주차에도 accuracy, precision, recall, f1-score를 봤지만, 이번주에는 조금 더 체계적으로 정리했다.

```text
accuracy = 전체 중 맞춘 비율
precision = Positive라고 예측한 것 중 실제 Positive 비율
recall = 실제 Positive 중 모델이 찾아낸 비율
f1-score = precision과 recall의 균형
ROC-AUC = 확률 기준으로 두 class를 구분하는 능력
```

accuracy는 직관적이지만 항상 충분하지는 않다.

데이터가 불균형하면 대부분의 class만 맞혀도 accuracy가 높게 나올 수 있다.

그래서 혼동행렬도 같이 봐야 한다.

```text
TN = 실제 Negative를 Negative로 예측
FP = 실제 Negative를 Positive로 예측
FN = 실제 Positive를 Negative로 예측
TP = 실제 Positive를 Positive로 예측
```

이번에 threshold를 바꿔가며 precision과 recall이 어떻게 달라지는지도 확인했다.

```text
threshold 낮춤 = Positive 예측 증가, recall 증가 가능
threshold 높임 = Positive 예측 감소, precision 증가 가능
```

이 부분이 중요했다.

모델의 예측 결과는 단순히 0 또는 1로 끝나는 것이 아니라, 확률을 어떤 기준으로 자를 것인지에 따라 달라질 수 있다.

그리고 그 기준은 문제 상황에 따라 달라진다.

예를 들어 놓치면 안 되는 문제라면 recall이 중요할 수 있고, 잘못 Positive로 판단하면 비용이 큰 문제라면 precision이 중요할 수 있다.

---

## 회귀 평가지표

회귀 평가지표에서는 Diabetes 데이터를 사용해 연속형 target 예측 결과를 평가했다.

주요 지표는 다음과 같았다.

```text
MAE = 오차 절댓값의 평균
MSE = 오차 제곱의 평균
RMSE = MSE의 제곱근
R2 = 평균 예측 대비 모델이 얼마나 설명력을 가지는지 보는 지표
```

MAE는 오차 크기를 직관적으로 보기 좋다.

MSE는 오차를 제곱하기 때문에 큰 오차에 더 민감하다.

RMSE는 MSE에 루트를 씌워 target 단위에 가깝게 다시 해석할 수 있다.

```text
MAE = 평균적으로 얼마나 틀렸는가
RMSE = 큰 오차를 더 강하게 반영하면 얼마나 틀렸는가
R2 = 평균만 예측하는 것보다 얼마나 나은가
```

이번에 작은 예제로 마지막 예측값 하나만 크게 틀렸을 때 MAE와 RMSE가 어떻게 달라지는지 비교했다.

그 결과 RMSE가 큰 오차에 더 민감하다는 점이 눈에 더 잘 들어왔다.

회귀에서도 마찬가지로 점수 하나만 보면 안 된다.

```text
오차의 평균 크기를 보고 싶은가?
큰 오차를 더 중요하게 볼 것인가?
모델의 설명력을 보고 싶은가?
```

이 질문에 따라 지표 선택이 달라진다.

---

## 추천 시스템

이번주 마지막에는 추천 시스템으로 넘어갔다.

추천 시스템은 처음에는 완전히 새로운 분야처럼 느껴졌다.

하지만 정리해보니 지금까지 배운 것들이 많이 연결되어 있었다.

```text
회귀 모델
전처리
벡터화
유사도 계산
정렬
평가와 해석
```

먼저 회귀 기반 추천에서는 여행 데이터를 사용해 만족도를 예측했다.

```text
사용자 조건과 여행지 정보
-> 만족도 예측
-> 만족도가 높을 것으로 예상되는 여행지 추천
```

여기서는 CatBoostRegressor를 사용했다.

즉 추천이라고 해서 꼭 특수한 알고리즘만 쓰는 것이 아니라, 회귀 모델로 점수를 예측하고 그 점수를 기준으로 추천할 수도 있다.

다음으로는 벡터와 코사인 유사도를 정리했다.

```text
코사인 유사도 = 벡터의 방향이 얼마나 비슷한지 보는 값
```

크기보다 방향을 보기 때문에, 문서나 장르처럼 어떤 성분을 가지고 있는지 비교할 때 유용하다.

영화 추천에서는 TMDB 데이터의 장르와 키워드를 사용했다.

처음에는 `genres` 컬럼이 그냥 문자열처럼 보였지만, 실제로는 딕셔너리 리스트 형태였다.

```python
[
    {'id': 28, 'name': 'Action'},
    {'id': 12, 'name': 'Adventure'}
]
```

여기서 `name`만 꺼내 문자열로 합쳤다.

```python
movie_df['genres'] = movie_df['genres'].apply(
    lambda x: ' '.join([genre['name'] for genre in x])
)
```

이 코드는 한 줄이지만 안에는 여러 단계가 들어 있다.

```text
각 행을 꺼낸다.
딕셔너리 리스트를 반복한다.
name 값을 꺼낸다.
공백으로 합친다.
같은 컬럼에 다시 저장한다.
```

그 다음 `CountVectorizer`로 텍스트를 벡터로 바꿨다.

```text
CountVectorizer = 텍스트를 단어 빈도 기반 벡터로 변환
```

영화 하나가 모든 장르를 가지는 것은 아니기 때문에 결과는 대부분 0인 희소 행렬이 된다.

```text
원핫 인코딩 = 범주형 값을 0과 1로 표현하는 방법
희소 행렬 = 대부분의 값이 0인 행렬 또는 저장 방식
```

둘은 완전히 같은 말은 아니지만, 실제 전처리에서는 자주 연결된다.

---

## 콘텐츠 기반 영화 추천

콘텐츠 기반 추천에서는 장르 벡터를 만든 뒤 영화 간 코사인 유사도를 계산했다.

```python
genre_sim = cosine_similarity(movie_genres_mat, movie_genres_mat)
```

결과 shape는 다음과 같은 의미를 가진다.

```text
genre_sim.shape = (영화 개수, 영화 개수)
```

즉 모든 영화와 모든 영화 사이의 유사도를 계산한 것이다.

추천 흐름은 다음과 같았다.

```text
기준 영화 제목 입력
-> 해당 영화의 행 인덱스 찾기
-> 장르 유사도가 높은 영화 후보 추출
-> 후보 영화의 vote_count, weighted_vote 기준 정렬
-> 최종 추천 목록 반환
```

여기서 가장 많이 헷갈린 부분은 인덱스였다.

처음에는 조건에 맞는 영화의 행 전체 값을 가져오면서 문제가 생길 수 있었다.

```python
index = movie_df[movie_df['title'] == title].values[0]
```

이렇게 하면 정수 인덱스 하나가 아니라 행 전체 배열이 들어갈 수 있다.

그래서 유사도 행렬의 인덱스로 쓰기에 적절하지 않다.

올바른 흐름은 행 번호를 가져오는 것이다.

```python
index = movie_df[movie_df['title'] == title].index.values[0]
```

또 하나 중요한 점은 원본 `movie_df`를 추천 결과로 덮어쓰지 않는 것이다.

```python
recommended_movies = movie_df.iloc[recommended_idx]
```

추천 결과를 `movie_df`에 다시 저장해버리면 전체 영화 목록이 사라져서 다음 추천에서 인덱스가 꼬일 수 있다.

이번 추천 시스템에서 가장 크게 느낀 점은, 에러가 났을 때 함수만 의심하면 안 된다는 것이다.

변수 안에 지금 무엇이 들어 있는지 봐야 한다.

```text
문자열
-> 리스트/딕셔너리
-> 전처리된 텍스트
-> 희소 행렬
-> 유사도 행렬
-> 정렬된 인덱스
-> 추천 결과 데이터프레임
```

데이터가 여러 단계를 거치면서 형태가 계속 바뀌기 때문에 `type`, `shape`, `head()`를 확인하는 습관이 중요하다.

---

## 이번 주에 계속 반복된 핵심

이번주에 계속 반복된 핵심은 다음이었다.

```text
모델을 학습시키는 것만으로 끝나지 않는다.
데이터 구조를 확인해야 한다.
평가 기준을 정해야 한다.
결과를 해석해야 한다.
다시 사용할 수 있게 관리해야 한다.
```

특히 이번주는 `스케일링`, `검증`, `해석`이 계속 반복됐다.

```text
스케일링 = 거리, 분산, 좌표를 보는 모델에서 중요
검증 = 모델 성능을 안정적으로 추정하기 위해 중요
해석 = 모델 결과에 실제 의미를 붙이기 위해 중요
```

KMeans와 DBSCAN에서는 스케일링이 중요했다.

교차검증과 튜닝에서는 검증 방식이 중요했다.

RFM과 추천 시스템에서는 결과를 어떻게 해석할지가 중요했다.

그리고 모든 과정에서 데이터프레임의 컬럼, 변수의 타입, 행렬의 shape를 확인해야 했다.

```python
print(type(data))
print(data.shape)
display(data.head())
```

이런 기본 확인이 결국 디버깅의 시작이라는 것을 다시 느꼈다.

---

## 어려웠던 점

이번주에 어려웠던 점은 내용의 범위가 넓었다는 것이다.

차원축소, 군집화, 교차검증, 튜닝, 평가 지표, 추천 시스템이 모두 다른 주제처럼 느껴졌다.

하지만 정리하다 보니 완전히 따로 떨어진 내용은 아니었다.

그래도 여전히 어려운 부분은 많다.

```text
PCA와 LDA의 차이를 실제 문제에서 바로 판단하기
DBSCAN의 eps와 min_samples를 적절히 고르기
교차검증과 test 평가의 역할을 헷갈리지 않기
평가지표를 문제 상황에 맞게 선택하기
추천 시스템에서 인덱스와 행렬 shape를 정확히 추적하기
```

특히 추천 시스템에서는 변수의 형태가 계속 바뀌어서 정신을 놓으면 바로 에러가 났다.

`movie_df`, `movie_genres_mat`, `genre_sim`, `recommended_idx`가 각각 무엇인지 계속 확인해야 했다.

그리고 평가 지표도 말로는 이해한 것 같지만, 실제 문제에서 precision을 봐야 할지 recall을 봐야 할지 판단하려면 더 많은 예제가 필요할 것 같다.

---

## 좋았던 점

이번주에 좋았던 점은 머신러닝의 전체 흐름이 조금 더 넓게 보였다는 것이다.

처음에는 모델을 하나씩 배우는 느낌이었다.

하지만 이번주는 모델 전후의 과정이 더 중요하게 다가왔다.

```text
데이터를 준비한다.
필요하면 차원을 줄인다.
모델을 학습한다.
교차검증으로 비교한다.
하이퍼파라미터를 튜닝한다.
평가지표로 해석한다.
모델을 저장한다.
서비스 목적에 맞게 추천 결과를 만든다.
```

특히 RFM과 추천 시스템은 실무와 연결되는 느낌이 있었다.

RFM에서는 고객 거래 데이터를 고객 단위 feature로 바꾸고, 군집별 특징을 보고 고객 유형을 해석했다.

추천 시스템에서는 영화 장르와 키워드를 벡터화하고, 유사도를 기준으로 비슷한 영화를 추천했다.

이 과정을 보면서 머신러닝은 모델 자체보다 데이터를 어떤 관점으로 바꾸고 결과를 어떻게 사용하는지가 중요하다는 생각이 들었다.

---

## 다음 주 목표

다음 주에는 지금까지 배운 흐름을 더 안정적으로 적용하는 연습을 해야겠다.

```text
문제를 보면 분류, 회귀, 군집화, 추천 중 무엇인지 먼저 구분하기
데이터 전처리 순서를 스스로 정리하기
스케일링이 필요한 모델인지 판단하기
교차검증과 test 평가를 구분해서 사용하기
평가지표를 문제 상황에 맞게 선택하기
모델 결과를 표와 그래프로 해석하기
추천 시스템에서는 변수 type과 shape를 계속 확인하기
```

그리고 노트북을 작성할 때는 중간 변수에 너무 의존하지 않도록 해야겠다.

이전 셀에 남아 있는 변수가 있어서 코드가 되는 것처럼 보일 수 있다.

```text
Kernel Restart & Run All
```

이걸 자주 해서 실제 파일만으로도 처음부터 끝까지 실행되는지 확인해야겠다.

---

## 마무리

6주차는 머신러닝을 더 넓게 본 주였다.

5주차에는 여러 모델을 배우면서 모델별 특징을 구분하는 데 집중했다면, 6주차에는 그 모델을 어떻게 평가하고, 고르고, 저장하고, 실제 추천 문제에 연결할지까지 봤다.

이번주를 지나면서 머신러닝에서 중요한 질문이 조금 더 선명해졌다.

```text
이 데이터는 어떤 형태인가?
이 모델은 무엇을 기준으로 판단하는가?
이 성능을 어떻게 믿을 수 있는가?
이 지표는 무엇을 의미하는가?
이 결과를 어떻게 사용할 것인가?
```

아직 개념이 완전히 편한 것은 아니다.

DBSCAN 파라미터도 어렵고, 평가 지표도 문제마다 선택 기준이 다르고, 추천 시스템에서는 인덱스 하나만 꼬여도 결과가 이상해진다.

그래도 이번주는 머신러닝이 단순히 `fit()`과 `predict()`만으로 끝나지 않는다는 것을 확실히 느꼈다.

```text
전처리
학습
검증
튜닝
평가
저장
해석
활용
```

이 전체 흐름을 조금씩 연결하기 시작한 주였다.

어렵다.
아직 어렵다.

그래도 이제는 에러가 나면 함수 이름만 보는 것이 아니라, 데이터의 모양과 흐름을 먼저 확인하려고 한다.

일단 여기까지.

6주차 끝.
