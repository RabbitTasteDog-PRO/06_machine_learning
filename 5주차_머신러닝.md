# 5주차 머신러닝

## 5주차를 마치며

이번주는 머신러닝 두 번째 주.

4주차에는 머신러닝의 기본 흐름과 회귀를 따라가는 데 집중했다면, 5주차는 본격적으로 분류 모델과 앙상블 모델로 넘어간 한 주였다.

어렵다...
어려워....

그래도 이번주는 단순히 모델 이름을 외우는 것보다, 모델이 데이터를 어떻게 보고 판단하는지 조금씩 구분하기 시작한 주였다.

처음에는 `LogisticRegression`, `DecisionTreeClassifier`, `SVC`, `VotingClassifier`, `RandomForest`, `XGBoost`, `LightGBM`, `CatBoost` 같은 이름들이 그냥 모델 목록처럼 보였다.

그런데 수업과 실습을 이어가다 보니 결국 흐름은 비슷했다.

```text
데이터를 준비한다.
feature와 target을 나눈다.
학습용과 테스트용으로 나눈다.
필요하면 스케일링한다.
모델을 학습한다.
예측한다.
평가한다.
결과를 해석한다.
```

4주차에 계속 헷갈렸던 `X_train`, `X_test`, `y_train`, `y_test`도 이번주에 반복해서 보니까 조금씩 익숙해졌다.

물론 아직 완전히 편한 것은 아니다.
그래도 이제는 코드를 볼 때 어디가 문제 데이터이고, 어디가 정답 데이터인지 먼저 보려고 한다.

---

## 이번 주 학습 흐름

이번주 학습은 크게 다음 흐름으로 이어졌다.

```text
로지스틱 회귀
-> 오즈와 오즈비
-> 결정트리
-> 결정트리 가지치기
-> SVC
-> SVR
-> Voting
-> Bagging / RandomForest
-> Boosting
-> Stacking
-> XGBoost / LightGBM / CatBoost
```

회귀에서 분류로 넘어가면서 처음에는 또 새로운 걸 시작하는 느낌이었다.

하지만 정리해보면 회귀와 분류는 완전히 다른 세계라기보다, 예측하려는 `target`의 성격이 다른 것이다.

```text
회귀 = 숫자를 예측
분류 = 클래스를 예측
```

예를 들어 집값이나 무게를 예측하면 회귀이고, 붓꽃 종류나 와인 품질 등급처럼 어떤 종류를 맞히면 분류이다.

이번주에는 분류 문제를 중심으로, 모델이 어떤 방식으로 클래스를 나누는지를 많이 봤다.

---

## 로지스틱 회귀

처음에는 로지스틱 회귀라는 이름 때문에 헷갈렸다.

이름에는 회귀가 들어가는데 실제로는 분류 모델이다.

```text
Logistic Regression = 분류 모델
```

로지스틱 회귀는 먼저 선형 회귀처럼 점수 `z`를 만든다.

```text
z = w1*x1 + w2*x2 + ... + b
```

그리고 이 값을 확률로 바꿔서 클래스를 판단한다.

이진분류에서는 `sigmoid`를 사용하고, 다중분류에서는 `softmax`를 사용한다.

```text
sigmoid = 두 클래스 중 하나를 고르는 확률
softmax = 여러 클래스 각각의 확률
```

이번에 `predict()`와 `predict_proba()`도 다시 정리했다.

```text
predict() = 최종 예측 클래스
predict_proba() = 각 클래스에 속할 확률
```

`predict()`는 결과만 보여준다.

반면 `predict_proba()`는 모델이 어느 정도 확신하고 있는지 보여준다.

처음에는 그냥 둘 다 예측 아닌가 싶었는데, 확률을 확인할 수 있다는 점에서 `predict_proba()`가 모델 해석에 더 도움이 됐다.

---

## 오즈와 오즈비

오즈와 오즈비는 솔직히 처음에 말부터 어려웠다.

확률도 헷갈리는데 오즈라니...

그래도 정리하면 다음과 같다.

```text
확률 = 발생 / 전체
오즈 = 발생 / 미발생
오즈비 = 두 오즈의 비율
```

오즈비는 어떤 feature가 결과의 가능성을 얼마나 움직이는지 해석할 때 사용한다.

```text
오즈비 > 1 -> 사건 발생 오즈 증가
오즈비 = 1 -> 거의 차이 없음
오즈비 < 1 -> 사건 발생 오즈 감소
```

중요한 점은 오즈비가 확률의 배수가 아니라 오즈의 배수라는 것이다.

예를 들어 오즈비가 3이라고 해서 확률이 3배라는 뜻은 아니다.
사건이 발생할 오즈가 3배라는 의미이다.

이 부분은 아직도 말로 설명하려면 조심해야 할 것 같다.

---

## 결정트리

결정트리는 비교적 직관적이었다.

조건을 하나씩 나누면서 최종 클래스를 판단한다.

```text
sugar <= 3.74 ?
alcohol <= 10.25 ?
volatile acidity <= 0.42 ?
```

스무고개처럼 질문을 던져서 데이터를 나누는 방식이다.

결정트리의 좋은 점은 모델의 판단 흐름을 눈으로 볼 수 있다는 것이다.
`plot_tree()`로 시각화하면 어떤 feature를 기준으로 데이터를 나눴는지 확인할 수 있다.

트리에서 봐야 하는 값은 다음과 같다.

```text
조건 = 데이터를 나누는 기준
gini / entropy = 노드의 불순도
samples = 해당 노드에 들어온 데이터 수
value = 클래스별 데이터 수
class = 대표 예측 클래스
```

처음에는 gini와 entropy가 무슨 차이인지 어려웠다.

하지만 둘 다 결국 한 노드 안에 클래스가 얼마나 섞여 있는지 보는 값이다.

```text
불순도 낮음 = 한 클래스가 많이 모여 있음
불순도 높음 = 여러 클래스가 섞여 있음
```

결정트리는 조건을 계속 나누면 학습 데이터에 너무 잘 맞을 수 있다.
이것이 과대적합이다.

그래서 가지치기 개념도 같이 배웠다.

```text
max_depth = 트리 최대 깊이 제한
min_samples_split = 노드를 나누기 위한 최소 샘플 수
min_samples_leaf = leaf node에 남아야 하는 최소 샘플 수
```

이번에 느낀 점은 학습 점수만 높다고 좋은 모델이 아니라는 것이다.

```text
train score 높음 + test score 낮음
-> 학습 데이터를 외웠을 가능성
```

그래서 모델을 볼 때는 학습셋 점수와 평가셋 점수를 같이 봐야 한다.

---

## SVM, SVC, SVR

SVM은 처음에 개념이 조금 어려웠다.

결정트리는 조건으로 나누는 느낌이라면, SVM은 클래스 사이의 경계를 찾는 느낌이다.

```text
SVM = 클래스를 나누는 결정 경계를 찾는 모델
SVC = SVM을 분류에 사용
SVR = SVM을 회귀에 사용
```

SVM에서 중요한 단어는 마진과 서포트 벡터였다.

```text
결정 경계 = 클래스를 나누는 기준선 또는 기준면
마진 = 결정 경계와 가까운 데이터 사이의 여유 공간
서포트 벡터 = 결정 경계와 가장 가까운 데이터
```

SVM은 단순히 데이터를 많이 맞히는 것보다, 클래스 사이의 마진을 넓게 만드는 경계를 찾는다.

마진이 넓으면 새로운 데이터가 들어와도 조금 더 안정적으로 분류할 수 있기 때문이다.

SVC에서는 `C`, `kernel`, `gamma`도 중요했다.

```text
C 작음 = 오분류를 어느 정도 허용, 단순한 경계
C 큼 = 오분류를 줄이려 함, 복잡한 경계

linear kernel = 직선 경계
rbf kernel = 곡선형 경계

gamma 작음 = 경계가 부드러움
gamma 큼 = 가까운 데이터에 민감함
```

여기서도 스케일링이 중요했다.

SVC는 거리와 마진을 보는 모델이기 때문에 feature의 숫자 범위가 다르면 판단이 한쪽으로 치우칠 수 있다.

```text
훈련 데이터에는 fit_transform()
평가 데이터에는 transform()
```

이 원칙은 이번주에도 계속 반복됐다.

처음에는 그냥 외우는 느낌이었지만, 이제는 테스트 데이터로 기준을 다시 배우면 데이터 누수가 생긴다는 말이 조금 이해된다.

---

## 앙상블

이번주 후반에는 앙상블로 넘어갔다.

앙상블은 여러 모델의 판단을 합쳐 최종 예측을 만드는 방법이다.

```text
단일 모델 = 한 모델이 혼자 예측
앙상블 = 여러 모델의 예측을 합쳐 최종 예측
```

처음에는 여러 모델을 섞으면 무조건 좋아지는 건가 싶었는데, 중요한 것은 서로 다른 관점의 모델을 잘 조합하는 것이었다.

비슷한 모델만 여러 개 모으면 결과가 크게 달라지지 않을 수 있다.

---

## Voting

Voting은 앙상블 중에서도 가장 직관적인 방식이었다.

여러 모델의 예측을 모아서 투표하거나 확률 평균을 낸다.

```text
Hard Voting = class 예측값의 다수결
Soft Voting = class별 확률의 평균
```

Hard Voting은 각 모델이 고른 최종 class만 본다.

```text
모델 A 예측: 1
모델 B 예측: 1
모델 C 예측: 0

최종 예측: 1
```

Soft Voting은 각 모델의 확률을 평균내서 결정한다.

```text
모델 A의 class 1 확률: 0.6
모델 B의 class 1 확률: 0.7
모델 C의 class 1 확률: 0.8

평균 확률: 0.7
```

여기서 다시 헷갈렸던 부분이 있었다.

`predict()`로 y 예측값을 만드는데 왜 `X_test`를 넣는가?

정리하면 간단하다.

```text
X_test = 시험 문제
y_pred = 내가 쓴 답
y_test = 실제 정답지
```

예측은 항상 문제를 보고 정답을 맞히는 과정이다.
그래서 `predict()`에는 정답인 `y_test`가 아니라 문제인 `X_test`가 들어간다.

이 부분은 계속 반복해서 정리할 필요가 있다.

---

## Bagging과 RandomForest

Bagging은 여러 모델을 병렬로 학습시키고 결과를 합치는 방식이다.

대표적으로 RandomForest가 있다.

```text
Bagging = 데이터를 여러 번 뽑아 여러 모델을 학습
RandomForest = 여러 결정트리를 모아 예측
```

결정트리 하나는 학습 데이터에 과하게 맞춰질 수 있다.
하지만 여러 트리를 만들고 결과를 합치면 개별 트리의 흔들림을 줄일 수 있다.

RandomForest는 결정트리를 여러 개 사용하지만, 각 트리가 보는 데이터와 feature를 조금씩 다르게 만든다.

그래서 하나의 트리가 외운 결과에 덜 의존하게 된다.

이번에 앙상블을 보면서 느낀 점은, 모델 하나를 강하게 만드는 방법도 있지만 여러 약점을 서로 보완하게 만드는 방법도 있다는 것이다.

---

## Boosting

Boosting은 Bagging과 다르게 순차적으로 학습한다.

앞 모델이 틀린 부분을 다음 모델이 보완하는 방식이다.

```text
Boosting = 앞 모델의 실수를 다음 모델이 보완하며 순차적으로 학습
```

이 흐름은 직관적이면서도 조금 무섭다.

잘 쓰면 성능이 좋아질 수 있지만, 너무 학습 데이터에 맞춰지면 과대적합될 수도 있기 때문이다.

이번주에는 Boosting 계열로 다음 모델들을 봤다.

```text
Gradient Boosting
XGBoost
LightGBM
CatBoost
```

이름만 보면 다 비슷해 보이지만, 각각 학습 속도나 범주형 처리, 트리 생성 방식에서 차이가 있었다.

아직 내부 원리를 전부 이해했다고 말하기는 어렵다.
하지만 큰 흐름은 이렇게 잡았다.

```text
RandomForest = 여러 트리를 병렬로 모음
Boosting = 틀린 부분을 보완하며 트리를 순차적으로 쌓음
```

---

## Stacking

Stacking은 여러 모델의 예측 결과를 다시 다른 모델이 학습하는 방식이다.

```text
Stacking = 여러 모델의 예측 결과를 meta model이 다시 학습
```

처음에는 모델 위에 모델을 또 올린다는 말이 복잡하게 느껴졌다.

하지만 생각해보면 여러 모델이 낸 답을 하나의 새로운 feature처럼 보고, 최종 모델이 다시 판단하는 구조이다.

```text
모델 A 예측
모델 B 예측
모델 C 예측
        ↓
meta model이 최종 예측
```

Voting은 단순히 투표하거나 평균을 내는 방식이라면, Stacking은 그 예측 결과를 다시 학습한다는 점이 다르다.

그래서 더 유연할 수 있지만, 구조가 복잡해지는 만큼 검증도 더 중요하다고 느꼈다.

---

## 이번 주에 계속 반복된 핵심

이번주에 여러 모델을 배웠지만, 결국 계속 반복된 핵심은 비슷했다.

```text
X는 문제
y는 정답
fit()은 학습
predict()는 예측
score()는 평가
```

그리고 평가할 때는 모델 이름보다 다음을 먼저 봐야 한다.

```text
학습 데이터에서만 잘 맞는가?
평가 데이터에서도 잘 맞는가?
어떤 클래스를 잘 맞히고 어떤 클래스를 못 맞히는가?
오차가 어느 정도인가?
모델이 너무 복잡해지지는 않았는가?
```

분류에서는 accuracy만 보면 위험할 수 있다.

특정 클래스가 대부분이면 그 클래스만 찍어도 accuracy가 높게 나올 수 있기 때문이다.

그래서 `classification_report()`로 precision, recall, f1-score를 같이 확인해야 한다.

```text
precision = 예측한 것 중 실제로 맞은 비율
recall = 실제 정답 중 모델이 찾아낸 비율
f1-score = precision과 recall의 균형
```

회귀에서는 `R2`, `MAE`, `RMSE`를 같이 봤다.

```text
R2 = target의 변화를 얼마나 설명하는지
MAE = 오차의 절댓값 평균
RMSE = 큰 오차에 더 민감한 지표
```

점수 하나만 보고 끝내면 안 된다는 것을 계속 확인한 주였다.

---

## 어려웠던 점

이번주에 어려웠던 점은 모델이 많아지면서 머릿속에서 정리가 잘 안 됐다는 것이다.

로지스틱 회귀까지는 어느 정도 따라갔는데, 결정트리, SVM, Voting, RandomForest, Boosting, Stacking까지 나오니까 이름과 역할이 섞이기 시작했다.

특히 SVM의 `C`, `gamma`, `kernel`은 아직 더 연습이 필요하다.

```text
C는 오분류 벌점
gamma는 가까운 데이터에 대한 민감도
kernel은 경계의 형태
```

이렇게 정리하면 알 것 같은데, 실제 코드에서 값을 바꿨을 때 결정 경계가 어떻게 달라지는지 바로 떠올리는 수준은 아니다.

또 앙상블도 단어는 이해했지만 모델별 차이를 설명하려면 더 정리가 필요하다.

```text
Voting
Bagging
RandomForest
Boosting
Stacking
```

이 다섯 개를 그냥 외우는 것이 아니라, 어떤 상황에서 왜 쓰는지까지 이해해야 한다.

아직 갈 길이 멀다.

---

## 좋았던 점

이번주에 좋았던 점은 모델을 그냥 실행만 한 것이 아니라 계속 질문하면서 흐름을 다시 잡았다는 것이다.

특히 `predict()`에 왜 `X_test`가 들어가는지, `X_test`가 정답 데이터가 아닌지, `Hard Voting`과 `Soft Voting`이 무엇이 다른지 같은 부분을 다시 확인했다.

예전 같으면 그냥 코드가 돌아가면 넘어갔을 수도 있는데, 이번에는 왜 그렇게 쓰는지 계속 물어보면서 정리했다.

또 결정트리나 SVM처럼 모델의 판단 기준을 시각화하거나 비교하면서, 모델이 단순히 숫자 점수만 내는 도구가 아니라는 것도 느꼈다.

모델을 볼 때는 결과 점수뿐 아니라, 어떤 기준으로 판단했는지 같이 봐야 한다.

```text
결정트리 = 조건으로 나누는 흐름
SVC = 마진이 넓은 경계
RandomForest = 여러 트리의 판단
Boosting = 이전 모델의 실수 보완
Stacking = 여러 모델의 예측을 다시 학습
```

이렇게 정리하니 모델들이 조금씩 다른 성격을 가진다는 것이 보였다.

---

## 다음 주 목표

다음 주에는 지금까지 배운 모델들을 문제에 맞게 고르는 연습을 더 해보고 싶다.

```text
분류 문제인지 회귀 문제인지 먼저 구분하기
데이터 전처리 흐름을 먼저 설계하기
스케일링이 필요한 모델인지 확인하기
평가 지표를 문제에 맞게 선택하기
여러 모델을 비교할 때 기준을 명확히 하기
```

그리고 노트북 실습을 할 때는 중간중간 커널 메모리에 기대지 않도록 조심해야겠다.

주피터는 이전에 실행한 변수가 남아 있어서 코드가 되는 것처럼 보일 수 있다.

```text
Kernel Restart & Run All
```

이걸 자주 해봐야 실제 파일만으로도 코드가 돌아가는지 확인할 수 있다.

---

## 마무리

5주차는 분류 모델과 앙상블 모델을 한 번에 많이 본 주였다.

처음에는 모델 이름이 너무 많아서 머리가 복잡했다.

하지만 정리해보면 모든 모델은 결국 같은 질문으로 돌아온다.

```text
이 데이터에서 어떤 feature를 보고
어떤 target을 예측할 것인가?
그리고 그 예측을 어떻게 평가할 것인가?
```

이번주를 지나면서 머신러닝은 모델을 많이 아는 것보다, 문제를 잘 나누고 흐름을 정확히 보는 것이 먼저라는 생각이 더 강해졌다.

아직 개념이 완전히 익숙한 것은 아니지만, 이제는 적어도 모델을 볼 때 다음을 먼저 생각하려고 한다.

```text
이 모델은 어떤 방식으로 판단하는가?
스케일링이 필요한가?
과대적합을 어떻게 막는가?
평가 지표는 무엇을 봐야 하는가?
```

어렵다.
아직 어렵다.

그래도 4주차에는 머신러닝의 기본 흐름을 잡았다면, 5주차에는 여러 모델이 그 흐름 위에서 어떻게 다르게 판단하는지 보기 시작했다.

일단 여기까지.

5주차 끝.
