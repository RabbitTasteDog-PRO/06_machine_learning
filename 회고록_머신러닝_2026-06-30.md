# 회고록: 머신러닝_2026-06-30

오늘은 회귀에서 분류로 학습 흐름이 넘어간 날이었다.

어제까지는 선형 회귀, 다항 회귀, Ridge, Lasso, ElasticNet처럼 숫자를 예측하는 회귀 문제를 중심으로 봤다면, 오늘은 정답이 클래스인 문제를 다뤘다.

```text
회귀 = 숫자를 예측
분류 = 클래스를 예측
```

오늘 수업의 중심은 로지스틱 회귀, 오즈와 오즈비, 결정트리였다.
이 세 가지는 모두 분류 문제를 다루지만, 바라보는 관점이 조금씩 달랐다.

```text
로지스틱 회귀 = 확률을 기준으로 클래스 분류
오즈비 = feature가 결과 가능성을 몇 배 움직이는지 해석
결정트리 = 조건 분기로 클래스를 나누고 판단 흐름을 시각화
```

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
03_classification/01_logistic_regression.ipynb
03_classification/practice/01_logistic_regression.ipynb
03_classification/02_odds_ratio.ipynb
03_classification/practice/02_odds_ratio (1).ipynb
04_decision_tree_svm/01_decision_tree_classifier.ipynb
04_decision_tree_svm/practice/01_decision_tree.ipynb
04_decision_tree_svm/data/wine_simple.csv
04_decision_tree_svm/dt_tree_wine.png
04_decision_tree_svm/dt_tree_wine_by_entropy.png
04_decision_tree_svm/dt_tree_iriis.png
```

파일 흐름을 보면 오전에는 로지스틱 회귀와 오즈비를 정리했고, 오후에는 결정트리와 가지치기 실습으로 이어졌다.

## 오늘 커밋한 내용

```text
cac5d44 -record - 머신러닝 분류모델
         - 로지스틱
         - 소프트맥스
         - 와인 퀄리티
e486e62 -record - 머신러닝 분류모델
         - 로지스틱 회귀 문제풀이
175549e -record - 머신러닝 분류모델
         - 오즈와 오즈비
7b8b06e -record - 머신러닝 분류모델
         - 오즈와 오즈비
cc3f142 -record - 머신러닝 분류모델
         - 결정트리 업데이트
3374bd8 -record - 머신러닝 분류모델
         - 결정트리 업데이트
```

커밋 메시지를 보면 오늘의 흐름은 명확했다.

```text
로지스틱 회귀
-> 로지스틱 회귀 문제풀이
-> 오즈와 오즈비
-> 결정트리
```

즉 단순히 모델을 실행하는 데서 끝난 것이 아니라, 모델의 예측 결과를 어떻게 해석할지까지 확장해서 공부했다.

## 오늘 질문 흐름

현재 대화에서 직접 확인 가능한 질문은 오늘 회고록 작성 요청이다.
다만 오늘 생성된 노트북과 커밋 흐름을 기준으로 보면, 오늘의 질문은 다음 방향으로 이어졌다고 볼 수 있다.

```text
로지스틱 회귀는 이름에 회귀가 들어가는데 왜 분류 모델인가?
predict()와 predict_proba()는 무엇이 다른가?
이진분류에서 sigmoid는 어떤 역할을 하는가?
다중분류에서 softmax는 어떤 역할을 하는가?
accuracy만 보면 왜 위험할 수 있는가?
오즈와 확률은 무엇이 다른가?
오즈비는 어떻게 해석해야 하는가?
결정트리는 어떤 기준으로 데이터를 나누는가?
gini와 entropy는 무엇을 의미하는가?
가지치기는 왜 필요한가?
feature_importances_는 어떻게 해석해야 하는가?
```

오늘 질문의 공통점은 "코드가 무엇을 실행하는가"보다 "그 결과를 어떻게 읽어야 하는가"에 가까웠다.

## 로지스틱 회귀

오늘 가장 먼저 정리한 내용은 로지스틱 회귀였다.

로지스틱 회귀는 이름에는 회귀가 들어가지만 실제로는 분류 모델이다.
내부적으로는 선형 회귀처럼 점수 `z`를 계산한 뒤, 그 값을 확률로 바꿔 클래스를 결정한다.

```text
z = w1*x1 + w2*x2 + ... + b
```

이진분류에서는 이 `z` 값을 sigmoid 함수에 넣는다.

```text
sigmoid(z) = 0과 1 사이의 확률
```

확률이 기준값보다 크면 양성 클래스, 작으면 음성 클래스로 분류한다.
보통 기준값은 0.5이다.

```text
확률 >= 0.5 -> 양성 클래스
확률 < 0.5  -> 음성 클래스
```

오늘 생선 데이터에서는 `Bream`과 `Smelt`를 나누는 이진분류를 실습했다.
이 과정에서 `predict()`와 `predict_proba()`의 차이를 확인했다.

```text
predict() = 최종 예측 클래스
predict_proba() = 각 클래스에 속할 확률
```

이 차이는 중요하다.
`predict()`는 이미 결정된 답만 보여주지만, `predict_proba()`는 모델이 얼마나 확신하고 있는지를 보여준다.

## 이진분류와 다중분류

로지스틱 회귀는 이진분류뿐 아니라 다중분류에도 사용할 수 있다.

이진분류에서는 sigmoid를 사용해 하나의 양성 클래스 확률을 계산한다.
반면 다중분류에서는 softmax를 사용해 여러 클래스의 확률을 동시에 계산한다.

```text
sigmoid = 두 클래스 중 하나를 고르는 확률
softmax = 여러 클래스 각각의 확률
```

생선 전체 종류를 분류하거나 와인 품질 등급을 예측하는 문제는 다중분류에 해당한다.

와인 품질 데이터에서는 `quality` 값이 여러 등급으로 나뉘어 있었다.
이때 단순 accuracy만 보면 모델을 잘못 판단할 수 있다.

```text
특정 클래스가 대부분이면
모델이 그 클래스만 찍어도 accuracy가 높게 나올 수 있음
```

그래서 오늘은 `classification_report()`를 통해 precision, recall, f1-score도 함께 확인했다.

## Accuracy와 classification_report

분류 모델에서 `score()`는 기본적으로 accuracy를 반환한다.

```text
accuracy = 맞힌 개수 / 전체 개수
```

accuracy는 직관적이지만 항상 충분하지는 않다.
특히 와인 품질처럼 특정 품질 등급에 데이터가 몰려 있으면 accuracy만으로는 모델 성능을 제대로 보기 어렵다.

이럴 때는 다음 지표를 함께 봐야 한다.

```text
precision = 예측한 것 중 실제로 맞은 비율
recall = 실제 정답 중 모델이 찾아낸 비율
f1-score = precision과 recall의 균형
```

오늘 배운 중요한 포인트는 "점수가 높다"보다 "어떤 클래스를 잘 맞히고 어떤 클래스를 못 맞히는지"를 봐야 한다는 점이었다.

## 오즈와 오즈비

오즈와 오즈비는 로지스틱 회귀 결과를 해석하기 위해 배웠다.

확률은 전체 중 사건이 일어날 비율이다.

```text
확률 = 발생 / 전체
```

오즈는 사건이 일어날 가능성과 일어나지 않을 가능성의 비율이다.

```text
오즈 = 발생 / 미발생
```

오즈비는 두 그룹의 오즈를 비교한 값이다.

```text
오즈비 = 그룹 A의 오즈 / 그룹 B의 오즈
```

오즈비 해석은 다음처럼 정리할 수 있다.

```text
오즈비 > 1 -> 기준 그룹보다 사건 발생 오즈가 높음
오즈비 = 1 -> 기준 그룹과 차이가 거의 없음
오즈비 < 1 -> 기준 그룹보다 사건 발생 오즈가 낮음
```

중요한 점은 오즈비가 확률의 배수가 아니라 오즈의 배수라는 것이다.
예를 들어 오즈비가 3이라고 해서 확률이 3배라는 뜻은 아니다.
사건 발생 오즈가 3배라는 뜻이다.

## 로지스틱 회귀 계수와 오즈비

로지스틱 회귀의 `coef_`는 그대로 해석하기 어렵다.
이 계수는 로그 오즈의 변화량이기 때문이다.

그래서 `np.exp(coef_)`를 사용해 오즈비로 바꿔 해석한다.

```python
odds_ratio = np.exp(model.coef_)
```

이렇게 바꾸면 feature가 1 증가할 때 사건 발생 오즈가 몇 배로 변하는지 볼 수 있다.

```text
np.exp(coef_) > 1 -> 사건 발생 오즈 증가
np.exp(coef_) < 1 -> 사건 발생 오즈 감소
```

오늘 수업에서 이 부분은 모델 해석의 관점에서 중요했다.
모델을 단순히 맞히는 도구로만 보는 것이 아니라, 어떤 feature가 결과를 어떤 방향으로 움직이는지 설명하는 도구로 본 것이다.

## 결정트리

오후에는 결정트리를 학습했다.

결정트리는 조건을 반복해서 데이터를 나누는 모델이다.
스무고개처럼 질문을 하나씩 던지며 최종 클래스를 결정한다.

```text
sugar <= 3.74 ?
alcohol <= 10.25 ?
volatile acidity <= 0.42 ?
```

결정트리의 장점은 모델의 판단 흐름을 눈으로 볼 수 있다는 것이다.
`plot_tree()`로 트리를 시각화하면 어떤 조건으로 데이터가 나뉘는지 확인할 수 있다.

트리 그림에서 봐야 할 정보는 다음과 같다.

```text
조건 = 데이터를 나누는 기준
gini 또는 entropy = 노드의 불순도
samples = 해당 노드에 들어온 데이터 수
value = 클래스별 데이터 수
class = 해당 노드의 대표 예측 클래스
```

결정트리는 스케일링이 필수는 아니다.
KNN이나 선형 모델처럼 거리나 계수 크기에 직접 의존하지 않고, feature의 기준값으로 데이터를 나누기 때문이다.

## 지니 불순도와 엔트로피

결정트리는 데이터를 나눌 때 더 잘 섞이지 않도록 만드는 기준을 찾는다.
이때 사용하는 값이 불순도이다.

지니 불순도는 한 노드 안에 클래스가 얼마나 섞여 있는지 나타낸다.

```text
Gini = 1 - Σ(pk^2)
```

한 클래스만 있으면 지니 불순도는 0에 가까워진다.
여러 클래스가 비슷하게 섞여 있으면 값이 커진다.

엔트로피도 비슷하게 노드의 불확실성을 측정한다.

```text
Entropy = -Σ(pk * log2(pk))
```

오늘 수업에서는 `criterion='gini'`와 `criterion='entropy'`를 비교했다.
같은 데이터라도 기준이 달라지면 트리의 분기 순서나 구조가 달라질 수 있다.

## 가지치기

결정트리는 제한을 주지 않으면 훈련 데이터를 너무 세밀하게 나눌 수 있다.
그러면 학습셋 점수는 높지만 평가셋 점수가 낮아질 수 있다.
이것이 과적합이다.

가지치기는 트리가 너무 복잡해지는 것을 막기 위한 방법이다.

오늘 비교한 사전 가지치기 파라미터는 다음과 같다.

```text
max_depth = 트리의 최대 깊이 제한
min_samples_split = 노드를 나누기 위한 최소 샘플 수
min_samples_leaf = leaf node에 남아야 하는 최소 샘플 수
```

각 파라미터는 트리 복잡도를 줄이는 방식이 다르다.

```text
max_depth가 작을수록 트리가 얕아짐
min_samples_split이 클수록 작은 노드는 잘 나누지 않음
min_samples_leaf가 클수록 너무 작은 leaf가 생기지 않음
```

오늘 실습에서는 단순히 평가 점수만 보는 것이 아니라, 트리 깊이와 leaf node 개수도 함께 확인했다.
이 부분이 중요했다.

```text
점수만 높다고 좋은 모델은 아님
너무 복잡하면 과적합일 수 있음
너무 단순하면 과소적합일 수 있음
```

## GridSearchCV

결정트리 실습 마지막에는 `GridSearchCV`를 사용했다.

앞에서는 `max_depth`, `min_samples_split`, `min_samples_leaf`를 각각 따로 비교했다.
`GridSearchCV`는 여러 하이퍼파라미터 조합을 한 번에 비교한다.

```text
여러 후보 조합 생성
교차검증으로 평균 성능 계산
가장 좋은 조합 선택
```

다만 중요한 점은 `GridSearchCV`가 고른 모델이 항상 테스트셋에서 최고 점수를 내는 것은 아니라는 것이다.
교차검증은 훈련 데이터 안에서 후보를 비교하는 과정이고, 테스트셋은 최종 확인용으로 남겨야 한다.

## Feature Importance

결정트리에서는 `feature_importances_`로 모델이 어떤 feature를 많이 사용했는지 확인할 수 있다.

```python
model.feature_importances_
```

이 값은 트리가 분기할 때 불순도 감소에 얼마나 기여했는지를 나타낸다.

하지만 주의할 점도 있다.

```text
중요도가 높다 = 이 모델이 분기 기준으로 많이 사용했다
중요도가 높다 != 실제 원인이다
```

feature importance는 모델 해석의 출발점이지 인과관계의 증거는 아니다.
오늘 이 차이를 구분하는 것이 중요했다.

## 오늘 수업 내용 분석

오늘 수업은 단순히 새로운 모델을 배운 날이 아니라, 모델을 보는 관점이 바뀐 날이었다.

어제까지는 회귀 모델에서 실제값과 예측값의 차이를 보고 성능을 평가했다.
오늘은 분류 모델에서 정답 클래스, 예측 클래스, 예측 확률, 클래스별 평가 지표를 같이 봤다.

```text
회귀 수업의 핵심 = 예측값이 실제 숫자와 얼마나 가까운가
분류 수업의 핵심 = 어떤 클래스를 얼마나 잘 구분하는가
```

또한 오늘은 해석 가능한 모델의 중요성이 많이 나왔다.

로지스틱 회귀에서는 `coef_`를 오즈비로 바꿔 feature의 영향을 해석했다.
결정트리에서는 트리 그림과 feature importance를 통해 모델의 판단 흐름을 해석했다.

```text
로지스틱 회귀 해석 = 계수 -> 오즈비
결정트리 해석 = 조건 분기 -> 트리 구조
```

오늘 내용은 앞으로 RandomForest, Gradient Boosting, XGBoost, LightGBM 같은 트리 기반 앙상블 모델로 넘어가기 위한 기반이 된다.
결정트리를 먼저 이해해야 앙상블 모델이 여러 트리를 어떻게 조합하는지도 이해할 수 있다.

## 오늘 헷갈리기 쉬운 부분

오늘 내용에서 특히 헷갈릴 수 있는 부분은 다음과 같다.

```text
로지스틱 회귀는 회귀가 아니라 분류 모델이다.
predict()는 최종 클래스, predict_proba()는 확률이다.
오즈비는 확률의 배수가 아니라 오즈의 배수이다.
accuracy가 높아도 클래스 불균형이 있으면 좋은 모델이라고 단정할 수 없다.
결정트리는 스케일링이 필수는 아니다.
feature_importances_는 인과관계가 아니라 모델이 사용한 정도이다.
GridSearchCV 결과가 테스트셋 최고 점수를 항상 보장하지는 않는다.
```

이 내용들은 코드보다 해석에서 실수하기 쉬운 부분이다.
앞으로 분류 모델을 사용할 때 계속 다시 확인해야 할 기준이다.

## 오늘의 정리

오늘은 분류 모델의 기본기를 한 번에 많이 다뤘다.

처음에는 로지스틱 회귀가 왜 분류인지부터 시작했고, sigmoid와 softmax를 통해 확률 기반 분류 과정을 확인했다.
그다음 오즈와 오즈비를 통해 로지스틱 회귀 계수를 해석하는 방법을 배웠다.
마지막으로 결정트리를 통해 조건 분기, 불순도, 가지치기, GridSearchCV, feature importance까지 연결했다.

오늘 배운 내용을 하나로 정리하면 다음과 같다.

```text
분류 모델은 클래스를 맞히는 모델이다.
하지만 단순히 맞혔는지만 보면 부족하다.
확률, 클래스별 지표, 모델의 판단 기준까지 함께 봐야 한다.
```

오늘의 핵심은 모델을 실행하는 것보다 모델이 왜 그렇게 판단했는지 읽는 연습이었다.
