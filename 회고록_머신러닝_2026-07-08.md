# 회고록: 머신러닝_2026-07-08

오늘은 모델 성능 평가 흐름을 조금 더 넓게 정리했다.

전날 교차검증을 통해 모델을 안정적으로 평가하는 방법을 봤다면, 오늘은 하이퍼파라미터 튜닝, HyperOpt, 학습된 모델 저장, 그리고 분류/회귀 평가지표까지 이어서 정리했다.

```text
교차검증
-> 하이퍼파라미터 튜닝
-> GridSearchCV / RandomizedSearchCV
-> HyperOpt
-> joblib 모델 저장
-> 분류 평가지표
-> 회귀 평가지표
```

오늘의 핵심은 모델을 학습하는 것에서 끝나는 것이 아니라, 어떤 기준으로 좋은 모델을 고를지, 고른 모델을 어떻게 저장할지, 그리고 문제 유형에 맞는 지표를 어떻게 해석할지 확인하는 데 있었다.

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
08_model_performence/02_hyperparameter_tuning.ipynb
08_model_performence/03_hyperopt.ipynb
08_model_performence/04_model_persistence_joblib.ipynb
08_model_performence/models/wine_logistic_pipline.joblib
08_model_performence/models/wine_logistic_pipline_metadata.csv
09_evaluation_metrics/01_classification_evaluation_metrics.ipynb
09_evaluation_metrics/02_regression_evaluation_metrics.ipynb
09_evaluation_metrics/data/titanic_train.csv
.gitignore
```

파일 흐름을 보면 오늘은 모델 성능을 평가하고 관리하는 후반부 과정에 집중한 날이었다.

```text
Wine 데이터 기반 하이퍼파라미터 튜닝
-> GridSearchCV 결과 확인
-> RandomizedSearchCV 결과 비교
-> HyperOpt search space와 objective 함수 정의
-> joblib으로 최종 Pipeline 저장
-> Titanic 데이터 기반 분류 평가지표 정리
-> Diabetes 데이터 기반 회귀 평가지표 정리
```

## 오늘 커밋한 내용

오늘 날짜 기준으로 확인된 커밋은 다음과 같았다.

```text
e962670 회고록 업데이트
  - 2026-07-07 회고록 보완

8c85c66 하이퍼파라미터 튜닝
  - 08_model_performence/02_hyperparameter_tuning.ipynb 추가
  - GridSearchCV, RandomizedSearchCV 실습 정리

85844c7 하이퍼옵트 : HyperOpt는 하이퍼파라미터 후보를 더 효율적으로 찾기 위한 최적화 도구이다.
  - 08_model_performence/03_hyperopt.ipynb 추가
  - search space, objective, fmin, Trials 흐름 정리

a3ed309 joblib 업데이트
  - .gitignore 수정
  - 08_model_performence/04_model_persistence_joblib.ipynb 추가
  - 학습된 Pipeline 저장과 불러오기 정리

f1d90f9 분류평가지표 - 분류모델
  - 09_evaluation_metrics/01_classification_evaluation_metrics.ipynb 추가
  - 09_evaluation_metrics/data/titanic_train.csv 추가

9e72351 평가지표 - 회귀모델
  - 09_evaluation_metrics/02_regression_evaluation_metrics.ipynb 추가

e1662da 평가지표 - 회귀모델
  - 09_evaluation_metrics/02_regression_evaluation_metrics.ipynb 보완
```

커밋 흐름을 보면 오늘은 모델 평가와 관리 주제가 단계적으로 확장되었다.

```text
하이퍼파라미터 후보 탐색
-> 최적 모델 선택
-> 최종 모델 저장
-> 분류 지표 해석
-> 회귀 지표 해석
```

## 오늘 질문 흐름

오늘 직접 확인한 질문은 다음과 같았다.

```text
pprint('objective 함수 테스트', sample_result)를 실행하면 왜 에러가 나는가?
딕셔너리를 보기 좋게 출력하려면 어떤 함수를 써야 하는가?
```

질문의 핵심은 `print()`와 `pprint()`의 사용 방식 차이에 있었다.

```text
print() = 여러 값을 쉼표로 나열해서 출력할 수 있음
pprint() = 보기 좋게 출력할 객체 하나를 중심으로 사용함
```

처음 작성한 코드는 다음과 같았다.

```python
from pprint import pprint

sample_result = objective({
  'n_estimators': 100,
  'max_depth': 3,
  'min_samples_leaf': 2
})

pprint('objective 함수 테스트', sample_result)
```

이 코드는 `pprint()`가 두 번째 인자인 `sample_result`를 출력할 딕셔너리가 아니라 `stream` 인자로 해석하면서 문제가 생긴다.

따라서 제목과 딕셔너리를 나누어 출력하는 방식이 더 적절하다.

```python
print('objective 함수 테스트')
pprint(sample_result)
```

또는 제목까지 같이 묶고 싶다면 딕셔너리 형태로 감싸서 출력할 수 있다.

```python
pprint({
  'objective 함수 테스트': sample_result
})
```

오늘 이 질문을 통해 함수마다 인자를 받는 방식이 다르다는 점을 다시 확인했다.
특히 출력 함수처럼 간단해 보이는 함수도, `print()`처럼 여러 객체를 단순 출력하는 함수인지, `pprint()`처럼 하나의 객체를 구조적으로 출력하는 함수인지 구분해야 한다.

## 하이퍼파라미터 튜닝

오늘 먼저 정리한 내용은 하이퍼파라미터 튜닝이었다.

하이퍼파라미터는 모델이 학습으로 직접 찾는 값이 아니라, 사람이 후보를 정해두고 비교해야 하는 설정값이다.

```text
parameter = 모델이 학습 과정에서 찾는 값
hyperparameter = 사람이 먼저 정하는 모델 설정값
```

`GridSearchCV`는 사람이 정한 후보 조합을 모두 비교한다.

```text
GridSearchCV = 후보 조합을 전부 확인
RandomizedSearchCV = 후보 중 일부를 무작위로 확인
```

후보가 적을 때는 GridSearchCV가 이해하기 쉽지만, 후보가 많아지면 모든 조합을 확인해야 하므로 시간이 오래 걸린다.
이럴 때 RandomizedSearchCV는 탐색 횟수를 제한하면서 빠르게 좋은 후보를 찾을 수 있다.

오늘 중요한 점은 튜닝 결과를 단순히 `best_params_`만 보는 것이 아니라, `best_estimator_`, `cv_results_`, test 평가까지 함께 확인하는 흐름이었다.

```text
best_params_ = 가장 좋았던 하이퍼파라미터 조합
best_estimator_ = 그 조합으로 다시 학습된 최종 모델
cv_results_ = 후보별 교차검증 결과
```

## HyperOpt

HyperOpt에서는 탐색 공간과 목적 함수를 직접 정의했다.

```python
search_space = {
  'n_estimators': hp.choice('n_estimators', [50, 100, 150]),
  'max_depth': hp.choice('max_depth', [2, 3, 4, 5, None]),
  'min_samples_leaf': hp.choice('min_samples_leaf', [1, 2, 4]),
}
```

`objective()` 함수는 후보 파라미터를 받아 모델을 만들고, 교차검증 점수를 계산한 뒤, HyperOpt가 최소화할 `loss`를 반환한다.

```text
accuracy = 높을수록 좋음
loss = 낮을수록 좋음
```

그래서 accuracy를 그대로 반환하지 않고 음수로 바꾸어 반환했다.

```python
return {
  'loss': -mean_score,
  'mean_score': mean_score,
  'status': STATUS_OK,
  'params': params
}
```

정리하면 HyperOpt 흐름은 다음과 같다.

```text
search_space 정의
-> objective 함수 정의
-> fmin으로 반복 탐색
-> Trials에 결과 저장
-> best parameter 확인
```

## joblib 모델 저장

오늘은 학습된 모델을 파일로 저장하는 흐름도 정리했다.

`Pipeline`으로 전처리와 모델을 묶은 뒤, `GridSearchCV`로 가장 좋은 모델을 찾고, 그 결과인 `best_estimator_`를 `joblib.dump()`로 저장했다.

```text
Pipeline = 전처리와 모델을 하나의 흐름으로 묶음
joblib.dump() = Python 객체를 파일로 저장
joblib.load() = 저장된 객체를 다시 불러옴
```

이 과정에서 모델 파일과 메타데이터 파일을 함께 관리했다.

```text
wine_logistic_pipline.joblib = 학습된 Pipeline
wine_logistic_pipline_metadata.csv = 모델 설명 정보
```

모델 저장에서 중요한 점은 단순히 모델 객체만 저장하는 것이 아니라, 어떤 feature로 학습했는지, 어떤 target 이름을 사용하는지, 어떤 파라미터가 선택되었는지도 함께 남기는 것이다.

또한 `joblib.load()`는 내부적으로 pickle 계열 로딩을 사용하므로 신뢰할 수 없는 파일에는 사용하지 않아야 한다는 점도 확인했다.

## 분류 평가지표

분류 평가지표에서는 Titanic 데이터를 사용해 생존 여부를 예측하고, 여러 지표를 비교했다.

```text
accuracy = 전체 중 맞춘 비율
precision = Positive라고 예측한 것 중 실제 Positive 비율
recall = 실제 Positive 중 모델이 찾아낸 비율
f1-score = precision과 recall의 균형
ROC-AUC = 확률 기준으로 두 class를 구분하는 능력
```

특히 accuracy만 보면 모델을 잘못 해석할 수 있다는 점이 중요했다.
데이터가 불균형하거나 Positive class를 놓치면 안 되는 문제에서는 precision, recall, f1-score를 함께 봐야 한다.

혼동행렬은 예측 결과의 오류 패턴을 확인하는 데 사용했다.

```text
TN = 실제 Negative를 Negative로 예측
FP = 실제 Negative를 Positive로 예측
FN = 실제 Positive를 Negative로 예측
TP = 실제 Positive를 Positive로 예측
```

threshold를 0.3, 0.5, 0.7로 바꾸면서 precision과 recall이 서로 trade-off 관계를 가진다는 점도 확인했다.

```text
threshold 낮춤 = Positive 예측 증가, recall 증가 가능
threshold 높임 = Positive 예측 감소, precision 증가 가능
```

## 회귀 평가지표

회귀 평가지표에서는 Diabetes 데이터를 사용해 연속형 target 예측 결과를 평가했다.

```text
MAE = 오차 절댓값의 평균
MSE = 오차 제곱의 평균
RMSE = MSE의 제곱근
R2 = 평균 예측 대비 모델이 얼마나 설명력을 가지는지 보는 지표
```

MAE는 실제값과 예측값이 평균적으로 얼마나 떨어져 있는지 직관적으로 보기 좋다.
MSE와 RMSE는 큰 오차에 더 민감하다.

```text
MAE = 오차 크기를 그대로 평균
MSE = 큰 오차를 더 크게 반영
RMSE = target 단위에 가깝게 다시 해석
```

오늘은 작은 예제로 직접 residual을 계산하고, 같은 실제값에서 마지막 예측값 하나만 크게 틀렸을 때 MAE와 RMSE가 어떻게 달라지는지도 비교했다.

이 과정을 통해 회귀 지표는 단순히 숫자가 낮고 높은지만 보는 것이 아니라, 어떤 종류의 오차에 민감한 지표인지 같이 해석해야 한다는 점을 확인했다.

## 오늘 느낀 점

오늘은 모델을 만드는 단계에서 한 걸음 더 나아가, 모델을 고르고 저장하고 평가하는 전체 흐름을 정리한 날이었다.

하이퍼파라미터 튜닝에서는 좋은 후보를 찾는 방법을 봤고, HyperOpt에서는 탐색 문제를 `loss` 최소화 문제로 바꾸는 방식을 확인했다.
joblib에서는 학습된 모델을 나중에 다시 사용할 수 있도록 저장하는 흐름을 정리했다.
마지막으로 분류와 회귀 평가지표를 비교하면서 문제 유형에 따라 봐야 할 숫자가 달라진다는 점을 다시 확인했다.

오늘의 핵심 정리는 다음과 같다.

```text
모델 선택 = 교차검증과 튜닝으로 좋은 후보를 고름
모델 저장 = Pipeline과 메타데이터를 함께 관리함
분류 평가 = accuracy만 보지 말고 precision, recall, f1-score, ROC-AUC를 함께 봄
회귀 평가 = MAE, MSE, RMSE, R2의 해석 차이를 구분함
pprint = 딕셔너리 하나를 보기 좋게 출력할 때 사용함
```

머신러닝에서는 모델을 학습시키는 코드만큼이나, 결과를 어떻게 비교하고 해석할지 정하는 과정이 중요하다는 점을 다시 확인했다.
