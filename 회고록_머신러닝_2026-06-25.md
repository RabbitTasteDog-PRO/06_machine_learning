# 회고록: 머신러닝_2026-06-25

머신러닝 수업을 들으면서 처음에는 용어들이 낯설었다.
특히 `train`, `test`, `X_train`, `X_test`, `y_train`, `y_test`, `score`, `predict` 같은 단어들이 자주 나와서 헷갈렸다.

이번에는 머신러닝의 기본 흐름을 정리해보려고 한다.

## train과 test

머신러닝에서는 데이터를 보통 두 부분으로 나눈다.

```text
train = 학습용 데이터
test = 테스트용 데이터
```

쉽게 말하면 `train`은 모델이 공부할 때 사용하는 데이터이고, `test`는 공부가 끝난 뒤 시험을 볼 때 사용하는 데이터이다.

만약 모든 데이터를 모델에게 다 보여주면, 모델이 진짜로 배운 것인지 단순히 외운 것인지 알 수 없다.
그래서 일부 데이터는 공부용으로 사용하고, 일부 데이터는 시험용으로 따로 남겨둔다.

## X와 y

머신러닝에서 자주 나오는 `X`와 `y`는 다음과 같은 의미이다.

```text
X = 입력 데이터, 문제
y = 정답 데이터, 답
```

예를 들어 붓꽃 데이터를 사용한다면:

```text
X = 꽃잎 길이, 꽃잎 너비, 꽃받침 길이, 꽃받침 너비
y = 꽃의 종류
```

즉, `X`는 모델이 보고 판단해야 하는 정보이고, `y`는 그 정보에 대한 실제 정답이다.

## X_train

`X_train`은 학습용 입력 데이터이다.

```text
X_train = 모델이 공부할 문제
```

모델은 `X_train`을 보고 패턴을 배운다.

## y_train

`y_train`은 학습용 정답 데이터이다.

```text
y_train = 공부할 문제의 정답
```

모델은 `X_train`과 `y_train`을 함께 보면서 어떤 입력이 어떤 정답과 연결되는지 학습한다.

## X_test

`X_test`는 테스트용 입력 데이터이다.

```text
X_test = 모델이 시험 볼 문제
```

모델 입장에서는 처음 보는 데이터이다.
이 데이터를 이용해서 모델이 얼마나 잘 배웠는지 확인한다.

## y_test

`y_test`는 테스트용 정답 데이터이다.

```text
y_test = 시험 문제의 실제 정답
```

모델이 `X_test`를 보고 예측한 결과와 `y_test`를 비교해서 맞았는지 확인한다.

## train_test_split()

`train_test_split()`은 전체 데이터를 학습용과 테스트용으로 나누는 함수이다.

```python
from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.2,
    stratify=y,
    random_state=42
)
```

각 옵션의 의미는 다음과 같다.

```text
X = 입력 데이터
y = 정답 데이터
test_size=0.2 = 전체 데이터 중 20%를 테스트용으로 사용
stratify=y = 정답 비율을 비슷하게 유지하면서 나누기
random_state=42 = 실행할 때마다 같은 결과가 나오도록 고정
```

## 스케일링

스케일링은 쉽게 말하면 숫자의 크기를 비슷하게 맞추는 작업이다.

예를 들어 붓꽃 데이터에는 꽃잎 길이, 꽃잎 너비 같은 여러 숫자가 들어 있다.
만약 어떤 값은 숫자가 작고, 어떤 값은 숫자가 크면 모델이 큰 숫자를 더 중요하게 생각할 수 있다.

그래서 스케일링을 사용해서 숫자들을 공평하게 비교할 수 있도록 만든다.

```text
스케일링 = 숫자 크기 맞추기
```

특히 KNN처럼 가까운 이웃을 찾는 모델은 거리 계산을 하기 때문에 스케일링이 중요하다.
숫자 크기가 너무 다르면 거리 계산이 한쪽 값에 치우칠 수 있기 때문이다.

## 표준점수

표준점수는 평균에서 얼마나 떨어져 있는지를 나타내는 점수이다.

```text
표준점수 = (값 - 평균) / 표준편차
```

쉽게 말하면 다음과 같다.

```text
평균보다 크면 양수
평균보다 작으면 음수
평균과 비슷하면 0 근처
```

예를 들어 평균이 10이고 표준편차가 2일 때, 값이 14라면:

```text
(14 - 10) / 2 = 2
```

이 값은 평균보다 표준편차 2개만큼 크다는 뜻이다.

## StandardScaler

`StandardScaler`는 데이터를 표준점수로 바꿔주는 도구이다.

```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
scaler.fit(X_train)

X_train_scaled = scaler.transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

여기서 중요한 점은 `fit()`은 `X_train`에만 해야 한다는 것이다.

```text
X_train = 공부용 문제
X_test = 시험용 문제
```

시험 문제인 `X_test`를 기준으로 평균과 표준편차를 구하면, 시험지를 미리 보고 공부하는 것과 비슷해진다.
그래서 `X_train`으로만 기준을 만들고, 그 기준을 `X_test`에도 똑같이 적용한다.

## fit()

`fit()`은 모델을 학습시키는 함수이다.

```python
kn_clf.fit(X_train, y_train)
```

뜻은 다음과 같다.

```text
X_train 문제와 y_train 정답을 보고 공부해라
```

즉, 모델이 데이터를 보고 규칙을 배우는 과정이다.

## score()

`score()`는 모델의 점수를 확인하는 함수이다.

```python
kn_clf.score(X_test, y_test)
```

뜻은 다음과 같다.

```text
X_test 문제를 풀고, y_test 정답과 비교해서 점수를 확인한다
```

보통 테스트 데이터 점수가 더 중요하다.
학습 데이터 점수는 이미 공부했던 문제를 다시 푸는 것이고, 테스트 데이터 점수는 처음 보는 문제를 푸는 것이기 때문이다.

## predict()

`predict()`는 모델이 정답을 예측하는 함수이다.

```python
kn_clf.predict(X_test[:5])
```

뜻은 다음과 같다.

```text
테스트 문제 5개를 보고 모델이 정답을 예측한다
```

`predict()`는 정답을 넣지 않는다.
입력 데이터만 보고 모델이 스스로 판단한 결과를 출력한다.

## 전체 흐름 정리

머신러닝의 기본 흐름은 다음과 같다.

```text
1. X와 y 데이터를 준비한다.
2. train_test_split()으로 학습용과 테스트용으로 나눈다.
3. X_train을 기준으로 스케일링 기준을 만든다.
4. X_train과 X_test를 같은 기준으로 스케일링한다.
5. X_train, y_train으로 모델을 학습한다.
6. X_test로 예측한다.
7. y_test와 비교해서 점수를 확인한다.
```

코드로 보면 다음과 같다.

```python
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.neighbors import KNeighborsClassifier

# 데이터 나누기
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.2,
    stratify=y,
    random_state=42
)

# 스케일링
scaler = StandardScaler()
scaler.fit(X_train)

X_train_scaled = scaler.transform(X_train)
X_test_scaled = scaler.transform(X_test)

# 모델 만들기
kn_clf = KNeighborsClassifier(n_neighbors=3)

# 모델 학습
kn_clf.fit(X_train_scaled, y_train)

# 점수 확인
print("학습 데이터 점수:", kn_clf.score(X_train_scaled, y_train))
print("테스트 데이터 점수:", kn_clf.score(X_test_scaled, y_test))

# 예측
print("예측:", kn_clf.predict(X_test_scaled[:5]))
print("실제 정답:", y_test[:5])
```

## 오늘 배운 점

처음에는 `X_train`, `X_test`, `y_train`, `y_test`가 너무 비슷하게 생겨서 헷갈렸다.
하지만 공부용 문제, 시험용 문제, 공부용 정답, 시험용 정답이라고 생각하니 훨씬 이해하기 쉬웠다.

```text
X_train = 공부용 문제
y_train = 공부용 정답
X_test = 시험용 문제
y_test = 시험용 정답
```

그리고 `score()`와 `predict()`의 차이도 중요하다.

```text
score() = 모델이 얼마나 잘 맞혔는지 점수 확인
predict() = 모델이 생각한 정답 예측
```

스케일링은 숫자의 크기를 맞춰서 모델이 데이터를 공평하게 비교할 수 있게 도와준다.
특히 KNN처럼 가까운 이웃을 찾는 모델에서는 스케일링이 중요하다.

## 한 줄 정리

머신러닝은 데이터를 공부용과 시험용으로 나누고,
숫자 크기를 맞춘 뒤,
공부용 데이터로 모델을 학습시키고,
시험용 데이터로 모델이 얼마나 잘 배웠는지 확인하는 과정이다.
