# 회고록: 머신러닝_2026-07-07

오늘은 비지도학습 중에서도 `DBSCAN`을 중심으로, 밀도 기반 군집화 결과를 시각화하고 실제 그룹과 비교해서 해석하는 흐름을 정리했다.

이전까지는 `KMeans`처럼 군집 개수를 미리 정하는 방식에 익숙했다면, 오늘은 `eps`, `min_samples`에 따라 DBSCAN 결과가 어떻게 달라지는지 확인했다.

```text
KMeans = 군집 개수를 미리 정하고 중심점을 기준으로 나눔
DBSCAN = 데이터가 밀집된 영역을 기준으로 군집을 찾음
eps = 이웃으로 볼 반경
min_samples = 핵심 데이터로 판단하기 위한 최소 이웃 개수
```

오늘의 핵심은 단순히 모델을 실행하는 것이 아니라, 스케일링된 데이터의 컬럼 구조를 확인하고, 군집 결과를 실제 그룹과 비교해서 해석하는 데 있었다.

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
07_unsupervised_learning/01_kmeans.ipynb
07_unsupervised_learning/02_dbscan.ipynb
07_unsupervised_learning/practice/01_kmeans_practice.ipynb
07_unsupervised_learning/practice/02_dbscan_practice.ipynb
07_unsupervised_learning/03_rfm_customer_segmentation.ipynb
07_unsupervised_learning/data/OnlineRetail.xlsx
```

파일 흐름을 보면 오늘은 KMeans에서 시작해 DBSCAN으로 넘어가고, 원형 데이터와 달 모양 데이터처럼 비선형 구조를 가진 데이터에서 군집화 결과를 비교한 뒤, 온라인 리테일 데이터를 이용한 RFM 고객 세분화 전처리까지 이어간 날이었다.

```text
KMeans
-> DBSCAN
-> eps, min_samples 변화 비교
-> KMeans와 DBSCAN 결과 비교
-> 실제 그룹과 DBSCAN 결과 교차표 해석
-> OnlineRetail 데이터 로딩
-> 결측치 확인
-> 주문 취소행과 비정상 수량/단가 제거
-> RFM 고객 세분화 전처리 준비
```

## 오늘 커밋한 내용

오늘 날짜 기준으로 확인된 커밋은 없었다.

```text
오늘 커밋 없음
```

## 오늘 질문 흐름

오늘 직접 정리한 질문은 다음과 같았다.

```text
DBSCAN에서 eps, min_samples를 바꿔가며 결과를 비교하는 코드를 어떻게 수정해야 하는가?
plt.subplots(2, 2)에서 axes를 반복문에 넣을 때 왜 문제가 생기는가?
scatterplot에서 feature_1_scaled 컬럼을 찾지 못하는 오류는 왜 발생하는가?
스케일링된 데이터의 평균과 표준편차 출력은 어디에 넣어야 하는가?
KMeans와 DBSCAN 결과를 같은 원형 데이터에서 어떻게 비교하는가?
DBSCAN 결과와 true_group을 pd.crosstab()으로 어떻게 비교하는가?
교차표의 값은 무엇을 의미하는가?
OnlineRetail.xlsx 파일이 있는데 왜 pd.read_excel('/data/OnlineRetail.xlsx')에서 FileNotFoundError가 나는가?
info() 출력에서 결측치를 어떻게 판단하는가?
pandas에서 여러 컬럼을 팬시 인덱싱으로 가져올 때 문법은 어떻게 쓰는가?
InvoiceNo가 C로 시작하는 주문 취소행을 제거하는 조건식은 어떻게 작성하는가?
Quantity와 UnitPrice가 0 이하인 데이터를 제거할 때 조건식을 어떻게 작성해야 하는가?
```

오늘 질문의 공통점은 모델의 원리뿐 아니라, 결과를 데이터프레임으로 정리하고 시각화한 뒤 실제 정답과 비교해서 해석하는 과정에 있었다.
후반부에는 실제 엑셀 데이터를 불러와 고객 세분화에 사용할 수 있도록 결측치, 취소 주문, 비정상 수량과 단가를 정리하는 전처리 흐름을 함께 점검했다.

## DBSCAN 파라미터 비교

오늘 먼저 확인한 부분은 `eps`와 `min_samples` 변화에 따른 DBSCAN 결과 비교였다.

```python
param_candidates=[
  {'eps':0.18, 'min_samples':5},
  {'eps':0.30, 'min_samples':5},
  {'eps':0.45, 'min_samples':5},
  {'eps':0.30, 'min_samples':10},
]
```

`plt.subplots(2, 2)`를 사용하면 `axes`는 2차원 배열 형태로 만들어진다.
그래서 반복문에서 바로 사용하면 각 축 하나가 아니라 축 배열 한 줄이 들어갈 수 있다.

이 문제는 `axes.ravel()`을 사용해서 해결했다.

```python
for ax, params in zip(axes.ravel(), param_candidates):
  ...
```

정리하면 다음과 같다.

```text
axes = 2차원 축 배열
axes.ravel() = 1차원 축 배열로 펼친 것
```

## 컬럼명 오류 해결

산점도를 그릴 때 다음 오류도 확인했다.

```text
ValueError: Could not interpret value `feature_1_scaled` for `x`
```

이 오류는 `sns.scatterplot()`에서 지정한 `feature_1_scaled`, `feature_2_scaled` 컬럼이 실제 데이터프레임에 없어서 발생했다.

해결 방법은 스케일링된 배열을 기준으로 컬럼명을 직접 지정해 데이터프레임을 다시 만드는 것이었다.

```python
plot_df = pd.DataFrame(
  moon_X_scaled,
  columns=['feature_1_scaled', 'feature_2_scaled']
)
```

오늘 이 부분을 통해 그래프 함수의 `x`, `y`에는 실제 데이터프레임에 존재하는 컬럼명이 들어가야 한다는 점을 다시 확인했다.

## 원형 데이터 스케일링

원형 데이터에서는 `StandardScaler`를 사용해 데이터를 표준화했다.

```python
circle_scaler = StandardScaler()
circle_X_scaled = circle_scaler.fit_transform(circle_X)
```

스케일링이 제대로 되었는지 확인하기 위해 평균과 표준편차는 스케일링 직후에 출력했다.

```python
print('scaled mean:', circle_X_scaled.mean().round(1))
print('scaled std:', circle_X_scaled.std().round(1))
```

출력 결과는 다음과 같았다.

```text
scaled mean: 0.0
scaled std: 1.0
```

이는 스케일링 후 전체 데이터의 평균이 0에 가깝고, 표준편차가 1에 가깝게 변환되었다는 의미이다.

## KMeans와 DBSCAN 비교

같은 원형 데이터에 `KMeans`와 `DBSCAN`을 각각 적용했다.

```python
kmeans = KMeans(
  n_clusters=2,
  n_init=10,
  random_state=42
)
circle_kmeans_labels = kmeans.fit_predict(circle_X_scaled)

dbscan = DBSCAN(eps=0.32, min_samples=5)
circle_dbscan_labels = dbscan.fit_predict(circle_X_scaled)
```

결과는 `circle_compare_df`에 저장했다.

```python
circle_compare_df = pd.DataFrame(circle_X_scaled, columns=['feature_1', 'feature_2'])
circle_compare_df['kmeans_cluster'] = circle_kmeans_labels
circle_compare_df['dbscan_cluster'] = circle_dbscan_labels
```

이후 두 결과를 나란히 산점도로 시각화했다.

```text
KMeans on Circle Data
DBSCAN on Circle Data
```

원형 데이터처럼 비선형 구조를 가진 데이터에서는 KMeans보다 DBSCAN이 실제 데이터 구조를 더 잘 반영할 수 있다는 점을 확인했다.

## DBSCAN 결과와 실제 그룹 비교

마지막으로 `eps=0.32`, `min_samples=5`로 얻은 DBSCAN 결과를 실제 그룹인 `true_group`과 비교했다.

요구사항에 맞게 `pd.crosstab()`을 사용했다.

```python
circle_dbscan_table = pd.crosstab(
  index=circle_y,
  columns=circle_dbscan_labels,
  rownames=['true_group'],
  colnames=['dbscan_cluster']
)

circle_dbscan_table
```

교차표의 의미는 다음과 같다.

```text
행 = 실제 그룹
열 = DBSCAN이 예측한 군집 라벨
표 안의 값 = 실제 그룹과 DBSCAN 라벨이 동시에 해당되는 데이터 개수
```

예를 들어 다음과 같은 결과가 나오면,

```text
dbscan_cluster    0    1
true_group              
0               175    0
1                 0  175
```

다음처럼 해석할 수 있다.

```text
실제 그룹 0인 데이터 175개가 DBSCAN 라벨 0으로 분류됨
실제 그룹 1인 데이터 175개가 DBSCAN 라벨 1로 분류됨
서로 다른 그룹으로 섞인 데이터가 거의 없음
```

즉 이 결과에서는 DBSCAN이 실제 원형 데이터의 그룹 구조를 잘 찾아냈다고 볼 수 있다.

## OnlineRetail 데이터 로딩

후반부에는 `OnlineRetail.xlsx` 파일을 불러오면서 경로 문제를 확인했다.

처음 사용한 코드는 다음과 같았다.

```python
retail_df = pd.read_excel('/data/OnlineRetail.xlsx')
```

하지만 실제 파일 위치는 프로젝트 루트 기준으로 다음 경로에 있었다.

```text
07_unsupervised_learning/data/OnlineRetail.xlsx
```

`/data/OnlineRetail.xlsx`처럼 앞에 `/`가 붙으면 현재 프로젝트 안의 `data` 폴더가 아니라 컴퓨터 루트의 `/data` 폴더를 찾는다.
그래서 파일이 실제로 존재하더라도 경로가 맞지 않으면 `FileNotFoundError`가 발생한다.

수정한 경로는 다음과 같다.

```python
retail_df = pd.read_excel('./07_unsupervised_learning/data/OnlineRetail.xlsx')
```

만약 노트북이 `07_unsupervised_learning` 폴더 안에서 실행되고 있다면 다음처럼 쓸 수도 있다.

```python
retail_df = pd.read_excel('./data/OnlineRetail.xlsx')
```

정리하면 다음과 같다.

```text
/data/OnlineRetail.xlsx = 루트 디렉터리의 data 폴더
./data/OnlineRetail.xlsx = 현재 작업 위치 기준 data 폴더
```

## 결측치 확인

`retail_df.info()` 출력에서 원본 데이터의 크기는 다음과 같았다.

```text
원본데이터 쉐입 : (541909, 8)
```

이는 전체 행이 `541,909`개이고, 컬럼이 `8`개라는 의미이다.

`info()`의 `Non-Null Count`가 전체 행 개수인 `541,909`보다 작으면 그 차이만큼 결측치가 있다고 판단할 수 있다.

오늘 확인한 결측치는 다음과 같았다.

```text
Description
전체 541,909개 중 non-null 540,455개
결측치 = 541,909 - 540,455 = 1,454개

CustomerID
전체 541,909개 중 non-null 406,829개
결측치 = 541,909 - 406,829 = 135,080개
```

코드로는 다음처럼 확인할 수 있다.

```python
retail_df.isnull().sum()
```

비율까지 보려면 다음처럼 계산할 수 있다.

```python
retail_df.isnull().mean() * 100
```

## pandas 컬럼 선택 문법

`Quantity`와 `UnitPrice` 컬럼을 함께 가져오려다 다음 문법 오류도 확인했다.

```python
retail_df[['Quantity'],['UnitPrice']]
```

여러 컬럼을 선택할 때는 컬럼명 리스트 하나를 DataFrame에 전달해야 한다.

```python
retail_df[['Quantity', 'UnitPrice']]
```

정리하면 다음과 같다.

```text
df['컬럼명'] = 컬럼 하나 선택
df[['컬럼1', '컬럼2']] = 여러 컬럼 선택
```

## OnlineRetail 데이터 전처리

RFM 고객 세분화에 사용하기 위해 먼저 `CustomerID`가 없는 데이터를 제거했다.

```python
before_rows = len(retail_df)

retail_clean_df = retail_df.dropna(subset=['CustomerID']).copy()
after_customer_filter = len(retail_clean_df)
```

그 다음 주문 취소행을 제거했다.
온라인 리테일 데이터에서는 `InvoiceNo`가 `C`로 시작하면 취소 주문으로 볼 수 있다.

처음 작성한 코드는 대괄호가 한 겹 더 들어가 있었다.

```python
retail_clean_df = retail_clean_df[[~retail_clean_df['InvoiceNo'].astype(str).str.startswith('C')]]
```

DataFrame 필터링은 `df[조건식]` 형태로 작성해야 하므로 다음처럼 수정했다.

```python
retail_clean_df = retail_clean_df[
  ~retail_clean_df['InvoiceNo'].astype(str).str.startswith('C')
].copy()
```

마지막으로 수량과 단가가 0 이하인 데이터를 제거했다.
이때 pandas에서 여러 조건을 `&`로 연결할 때는 각 조건을 괄호로 감싸야 한다.

```python
retail_clean_df = retail_clean_df[
  (retail_clean_df['Quantity'] > 0) &
  (retail_clean_df['UnitPrice'] > 0)
].copy()
```

괄호가 없으면 연산자 우선순위 때문에 pandas가 조건식을 의도와 다르게 해석할 수 있다.

전처리 후에는 구매 금액을 나타내는 `SaleAmount` 컬럼을 추가했다.

```python
retail_clean_df['SaleAmount'] = retail_clean_df['Quantity'] * retail_clean_df['UnitPrice']
```

정리하면 오늘 전처리 흐름은 다음과 같다.

```text
CustomerID 결측치 제거
-> InvoiceNo가 C로 시작하는 취소 주문 제거
-> Quantity가 0 이하인 행 제거
-> UnitPrice가 0 이하인 행 제거
-> SaleAmount 생성
```

## 오늘 느낀 점

오늘은 비지도학습 결과를 해석하는 방법과 실제 데이터 전처리 흐름을 함께 익힌 날이었다.

군집화는 정답을 주고 학습하는 방식이 아니기 때문에, 모델이 만든 라벨 자체가 정답이라는 의미는 아니다.
그래서 실제 그룹이 있는 실습 데이터에서는 `pd.crosstab()`으로 모델의 군집 결과와 실제 그룹을 비교하는 과정이 중요했다.

오늘의 핵심 정리는 다음과 같다.

```text
KMeans = 중심 기반 군집화
DBSCAN = 밀도 기반 군집화
eps = 이웃으로 인정할 거리
min_samples = 핵심 데이터 판단 기준
crosstab 값 = 실제 그룹과 예측 라벨이 겹치는 데이터 개수
Non-Null Count가 전체 행 수보다 작으면 결측치가 있음
여러 조건 필터링은 (조건1) & (조건2) 형태로 작성
절대경로와 상대경로를 구분해서 파일을 읽어야 함
```

특히 오늘은 코드 오류를 해결하면서 데이터프레임 컬럼명, subplot 축 구조, 스케일링 결과 확인 위치처럼 실습에서 자주 막히는 부분도 함께 정리했다.
또한 실제 고객 데이터에서는 모델링보다 먼저 결측치, 취소 주문, 비정상 수량과 단가를 정리해야 분석 결과를 신뢰할 수 있다는 점도 확인했다.

모델을 실행하는 것에서 끝내지 않고, 결과를 저장하고 시각화하고 교차표로 해석하는 흐름까지 이어가는 것이 중요하다는 점을 확인했다.
