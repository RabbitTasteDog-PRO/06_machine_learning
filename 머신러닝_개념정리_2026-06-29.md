# 머신러닝 개념 정리

## 머신러닝의 기본 흐름

머신러닝은 데이터를 보고 규칙을 배우는 과정이다.

가장 기본적인 흐름은 다음과 같다.

```text
feature를 보고 target을 맞히는 규칙을 배운다
```

코드에서는 보통 feature를 `X`, target을 `y`라고 쓴다.

```python
model.fit(X_train, y_train)
```

이 코드는 다음 뜻이다.

```text
X_train 문제와 y_train 정답을 같이 보고 공부해라
```

## Feature와 Target

`feature`는 모델에게 입력으로 주는 정보이다.

`target`은 모델이 예측해야 하는 정답이다.

예를 들어 생선 종류를 맞히고 싶다면 다음과 같다.

```text
feature = 길이, 무게, 두께, 높이
target = 생선 종류
```

이때 모델의 질문은 다음과 같다.

```text
이 생선은 어떤 종류인가?
```

반대로 생선 길이를 맞히고 싶다면 target이 바뀐다.

```text
feature = 무게, 두께, 높이, 생선 종류
target = 길이
```

이때 모델의 질문은 다음과 같다.

```text
이 생선의 길이는 얼마인가?
```

즉 target은 고정된 컬럼이 아니다. 내가 이번에 무엇을 예측하고 싶은지에 따라 target이 정해진다.

주의할 점은 target으로 정한 값을 feature에 다시 넣으면 안 된다는 것이다.

```python
# 잘못된 예
X = df[['길이', '무게', '두께']]
y = df['길이']
```

이건 문제지에 정답을 같이 적어놓는 것과 같다.

## X_train, X_test, y_train, y_test

머신러닝에서는 데이터를 보통 학습용과 평가용으로 나눈다.

```text
X = feature, 문제 데이터
y = target, 정답 데이터

train = 학습용
test = 평가용
```

그래서 네 가지 이름은 다음 뜻이다.

```text
X_train = 학습용 문제
y_train = 학습용 정답

X_test = 평가용 문제
y_test = 평가용 정답
```

모델은 학습용 데이터로 공부한다.

```python
model.fit(X_train, y_train)
```

그리고 평가용 데이터로 실력을 확인한다.

```python
model.score(X_test, y_test)
```

전체 데이터를 나누는 코드는 보통 이렇게 쓴다.

```python
X_train, X_test, y_train, y_test = train_test_split(X, y)
```

데이터를 나누는 이유는 모델이 단순히 외운 것인지, 처음 보는 데이터도 잘 맞히는지 확인하기 위해서이다.

## 분류와 회귀

target이 어떤 값인지에 따라 문제 유형이 달라진다.

target이 종류나 이름처럼 카테고리이면 분류이다.

```text
생선 종류 예측
도미인지 빙어인지 예측
합격인지 불합격인지 예측
```

target이 숫자이면 회귀이다.

```text
집값 예측
생선 무게 예측
배달 시간 예측
상품 판매량 예측
```

정리하면 다음과 같다.

```text
target이 카테고리 -> 분류
target이 숫자 -> 회귀
```

## Scaler와 스케일링

`Scaler`는 feature들의 숫자 기준을 맞춰주는 도구이다.

예를 들어 다음과 같은 데이터가 있다고 하자.

```text
키: 150 ~ 190
연봉: 3000 ~ 10000
나이: 20 ~ 60
```

숫자 크기만 보면 연봉이 가장 크다. 그런데 KNN처럼 거리를 보는 모델은 숫자 크기가 큰 feature에 더 크게 영향을 받을 수 있다.

모델이 이렇게 착각할 수 있다.

```text
연봉 숫자가 크니까 연봉이 제일 중요한가 보다
```

그래서 스케일링을 한다.

```text
스케일링 = 서로 단위가 다른 feature들을 비슷한 숫자 기준으로 맞추는 것
```

`StandardScaler`는 각 feature를 평균 0, 표준편차 1 기준으로 바꾼다.

```python
scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

여기서 중요한 점은 `X_test`에는 `fit`을 다시 하면 안 된다는 것이다.

```text
fit = 기준을 배움
transform = 배운 기준으로 바꿈
fit_transform = 기준을 배우고 바로 바꿈
```

`StandardScaler`에서 `fit`은 학습 데이터의 평균과 표준편차를 계산하는 것이다. 테스트 데이터는 처음 보는 데이터처럼 남겨야 하므로, 학습 데이터에서 배운 기준만 적용해야 한다.

## KNN

KNN은 가까운 이웃을 보고 판단하는 모델이다.

분류에서는 새 데이터 주변에 어떤 종류의 데이터가 많은지 보고 예측한다.

```text
새 생선 주변에 도미가 많다 -> 도미라고 예측
새 생선 주변에 빙어가 많다 -> 빙어라고 예측
```

회귀에서는 가까운 이웃들의 숫자 값을 참고해서 예측한다.

```text
비슷한 생선들의 무게 평균을 보고 새 생선 무게 예측
```

KNN은 거리 기반 모델이므로 스케일링이 중요하다. feature의 숫자 범위가 다르면 거리 계산이 한쪽 feature에 치우칠 수 있다.

## 선형회귀

선형회귀는 직선 관계를 이용해서 숫자를 예측하는 회귀 모델이다.

기본 식은 다음과 같다.

```text
y = w1 * x + b
```

feature가 여러 개이면 다음처럼 볼 수 있다.

```text
예측값 = w1 * feature1 + w2 * feature2 + w3 * feature3 + b
```

여기서 `w`는 회귀계수 또는 가중치이고, `b`는 절편이다.

모델이 학습한다는 것은 좋은 `w`와 `b`를 찾는다는 뜻이다.

## 다항회귀

다항회귀는 기존 feature를 제곱항이나 곱셈항으로 확장해서 더 복잡한 관계를 표현하는 방법이다.

예를 들어 feature가 `x1`, `x2`라면 2차 다항 feature는 다음처럼 늘어날 수 있다.

```text
x1, x2, x1^2, x1*x2, x2^2
```

직선으로는 표현하기 어려운 곡선 관계를 더 잘 표현할 수 있다.

하지만 feature가 많아지고 모델이 복잡해지면 과적합이 생길 수 있다.

## 과적합

과적합은 모델이 학습 데이터를 너무 외워서 새로운 데이터에는 약해지는 상태이다.

예를 들면 다음과 같다.

```text
train 점수는 매우 높음
test 점수는 낮음
```

이 경우 모델이 학습 데이터에는 잘 맞지만, 처음 보는 데이터에는 잘 못 맞히는 것이다.

## Ridge, Lasso, ElasticNet

Ridge, Lasso, ElasticNet은 규제를 사용하는 선형 회귀 모델이다.

규제는 모델의 회귀계수가 너무 커지지 않도록 제한하는 방법이다.

```text
규제 = 모델이 너무 복잡하게 외우지 못하도록 계수에 제한을 거는 것
```

Ridge는 L2 규제를 사용한다.

```text
Ridge = 모든 계수를 전체적으로 작게 만드는 방향
```

Lasso는 L1 규제를 사용한다.

```text
Lasso = 중요하지 않은 feature의 계수를 0으로 만들 수 있음
```

ElasticNet은 L1과 L2를 함께 사용한다.

```text
ElasticNet = Ridge와 Lasso를 섞은 모델
```

규제 모델은 계수 크기에 벌점을 주기 때문에 feature 스케일이 중요하다. 그래서 보통 `StandardScaler`와 함께 사용한다.

## Score와 오차 지표

`score()`의 의미는 모델 종류에 따라 다르다.

회귀 모델에서 `score()`는 보통 R2 결정계수이다.

```text
R2 = 1에 가까울수록 좋음
R2 = 0이면 평균값만 예측하는 수준
R2 < 0이면 평균 예측보다도 못함
```

예시는 다음과 같다.

```text
R2 = 0.95 -> 매우 좋음
R2 = 0.75 -> 꽤 괜찮음
R2 = 0.30 -> 설명력이 낮음
R2 = 0.00 -> 평균으로 찍는 것과 비슷함
R2 = -1.00 -> 평균으로 찍는 것보다 못함
```

반대로 MSE, MAE, RMSE는 오차 지표이다.

```text
MSE, MAE, RMSE = 0에 가까울수록 좋음
```

정리하면 다음과 같다.

```text
회귀 score, R2 -> 1에 가까울수록 좋음
MSE, MAE, RMSE -> 0에 가까울수록 좋음
분류 score, accuracy -> 1에 가까울수록 좋음
```

## 경사하강법

경사하강법은 모델이 정답에 가까워지도록 숫자들을 조금씩 조정하는 방법이다.

선형회귀 모델을 예로 들면 다음과 같다.

```text
y = w1 * x + b
```

여기서 모델이 찾아야 하는 값은 `w1`과 `b`이다.

경사하강법은 이 값을 한 번에 맞히는 것이 아니라, 예측 오차가 줄어드는 방향으로 조금씩 바꾼다.

흐름은 다음과 같다.

```text
현재 계수로 예측
-> 실제 정답과 비교
-> 오차 계산
-> 오차가 줄어드는 방향으로 계수 수정
-> 다시 예측
-> 다시 오차 계산
-> 반복
```

우리가 쓰는 `fit()` 안에서도 이런 최적화 과정이 들어간다.

```python
model.fit(X_train, y_train)
```

이 한 줄 안에서 모델은 예측하고, 틀린 정도를 계산하고, 계수를 수정한다.

## MSE와 Learning Rate

MSE는 오차를 제곱해서 평균낸 값이다.

```text
MSE = 오차^2의 평균
```

MSE가 작을수록 예측이 실제 정답에 가깝다는 뜻이다.

Learning Rate는 한 번에 얼마나 이동할지 정하는 값이다.

```text
lr이 너무 작음 -> 학습이 너무 느림
lr이 너무 큼 -> 정답 근처를 지나칠 수 있음
```

경사하강법에서는 보통 다음처럼 파라미터를 업데이트한다.

```python
w0 -= lr * w0_grad
w1 -= lr * w1_grad
```

`gradient`는 오차가 커지는 방향을 알려준다. 그래서 오차를 줄이려면 그 반대 방향으로 이동한다.

## Batch, Mini-Batch, SGD

경사하강법은 데이터를 얼마나 사용해서 한 번 업데이트하는지에 따라 나눌 수 있다.

Batch Gradient Descent는 전체 데이터를 한 번에 사용해서 업데이트한다.

```text
장점: 안정적
단점: 데이터가 많으면 느림
```

Stochastic Gradient Descent, 즉 SGD는 데이터 한 개씩 사용해서 업데이트한다.

```text
장점: 빠르게 자주 업데이트함
단점: 움직임이 흔들릴 수 있음
```

Mini-Batch Gradient Descent는 데이터를 작은 묶음으로 나누어 업데이트한다.

```text
장점: Batch와 SGD의 중간 방식
실무와 딥러닝에서 많이 사용
```

## 실무 데이터로 이해하기

예를 들어 배달 시간을 예측한다고 하자.

```text
feature = 거리, 주문량, 비 오는 정도, 조리시간
target = 실제 배달 시간
```

모델은 다음과 같은 식을 학습하려고 한다.

```text
예상 배달 시간 =
w1 * 거리
+ w2 * 주문량
+ w3 * 비 오는 정도
+ w4 * 조리시간
+ b
```

처음에는 예측이 많이 틀릴 수 있다.

```text
예측: 25분
실제: 40분
오차: 15분
```

그러면 모델은 학습하면서 계수를 조정한다.

```text
거리가 멀면 배달 시간이 늘어난다 -> 거리 가중치 조정
비가 오면 배달 시간이 늘어난다 -> 날씨 가중치 조정
주문량이 많으면 지연된다 -> 주문량 가중치 조정
```

이렇게 오차를 줄이도록 가중치를 바꾸는 원리가 경사하강법이다.

## 지금까지 배운 내용의 연결

지금까지의 내용은 따로 떨어진 개념이 아니라 하나의 흐름으로 이어진다.

```text
머신러닝 기본 개념
-> 데이터 준비와 전처리
-> KNN으로 가까운 이웃 기반 예측 이해
-> 회귀로 숫자 예측 이해
-> 선형회귀와 다항회귀
-> Ridge, Lasso, ElasticNet으로 과적합 제어
-> 경사하강법으로 모델이 학습되는 원리 이해
```

한 줄로 정리하면 다음과 같다.

```text
전처리로 데이터를 정리하고,
feature를 모델에 넣어 target을 맞히게 하고,
평가지표로 성능을 확인하고,
규제와 경사하강법으로 학습 원리를 이해하는 과정
```
