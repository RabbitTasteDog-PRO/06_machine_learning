# 회고록: 머신러닝_2026-07-09

오늘은 추천 시스템 파트로 넘어가서 회귀 기반 추천, 벡터와 코사인 유사도, 콘텐츠 기반 영화 추천 시스템까지 이어서 정리했다.

이전까지는 모델을 어떻게 학습하고 평가할지에 집중했다면, 오늘은 학습된 모델이나 벡터화된 feature를 이용해 사용자가 좋아할 만한 대상을 어떻게 추천할 수 있는지 확인했다.

```text
회귀 기반 추천
-> 여행 만족도 예측
-> CatBoostRegressor
-> 벡터와 코사인 유사도
-> 텍스트 feature 벡터화
-> 장르 기반 영화 유사도 계산
-> Weighted Rating을 반영한 콘텐츠 기반 추천
```

오늘의 핵심은 추천 시스템이 완전히 새로운 알고리즘 하나라기보다는, 지금까지 배운 회귀 모델, 전처리, 벡터화, 유사도 계산을 목적에 맞게 조합하는 방식이라는 점이었다.

---

## 오늘 생성 및 수정된 파일

오늘 작업공간에서 확인된 주요 파일은 다음과 같다.

```text
09_evaluation_metrics/02_regression_evaluation_metrics.ipynb
회고록_머신러닝_2026-07-08.md
10_recommendation_system/01_regression_based_recommendation.ipynb
10_recommendation_system/02_vector_cosine_similarity_basics.ipynb
10_recommendation_system/03_content_based_filtering.ipynb
10_recommendation_system/data/tmdb_5000_movies.csv
10_recommendation_system/data/travel.csv
```

추천 시스템 쪽 파일 흐름은 다음과 같았다.

```text
travel.csv
-> 여행 만족도 예측용 회귀 추천 예제
-> CatBoostRegressor 학습
-> 사용자 조건별 방문지 추천 함수 생성

tmdb_5000_movies.csv
-> 영화 장르/키워드 전처리
-> CountVectorizer로 텍스트 벡터화
-> cosine_similarity로 영화 간 유사도 계산
-> 장르 유사도와 가중평점을 함께 반영한 추천 함수 생성
```

오늘 추가된 데이터셋도 추천 시스템 목적에 맞게 나뉘었다.

```text
travel.csv = 사용자 조건과 여행 만족도를 이용한 회귀 기반 추천 실습 데이터
tmdb_5000_movies.csv = 영화 metadata를 이용한 콘텐츠 기반 추천 실습 데이터
```

## 오늘 커밋한 내용

오늘 날짜 기준으로 확인된 커밋은 다음과 같았다.

```text
22c0c69 회고록 업데이트
  - 2026-07-08 회고록 파일 추가

3c788fe 회귀평가지표
  - 09_evaluation_metrics/02_regression_evaluation_metrics.ipynb 보완

a24f355 - record - 회기기반 추천 시스템
  - 10_recommendation_system/01_regression_based_recommendation.ipynb 추가
  - 여행 데이터 기반 회귀 추천 시스템 정리

0536cb3 - record - 백터 변환 후 백터의 코사인 유사도를 이용한 계산
  - 10_recommendation_system/02_vector_cosine_similarity_basics.ipynb 추가
  - 벡터의 크기, 방향, 내적, 코사인 유사도 계산 정리

5c92632 - record - 영화 추천 시스템 - 코사인 백터화 + 회기 분석 이용하여 학습 후 개발
  - 10_recommendation_system/03_content_based_filtering.ipynb 추가
  - 영화 장르 기반 콘텐츠 추천 흐름 생성

b6ff278 - record - 관련 파일 업데이트
  - 10_recommendation_system/data/tmdb_5000_movies.csv 추가
  - 10_recommendation_system/data/travel.csv 추가

1a3a366 - record - 영화관련 추천 시스템
  - 10_recommendation_system/03_content_based_filtering.ipynb 수정
  - 콘텐츠 기반 추천 노트북 보완
```

커밋 흐름을 보면 오늘은 추천 시스템의 두 가지 접근을 모두 확인했다.

```text
회귀 기반 추천 = 사용자의 조건을 넣고 만족도나 평점을 예측
콘텐츠 기반 추천 = 아이템의 장르, 키워드 같은 속성을 벡터화해서 비슷한 아이템 추천
```

## 오늘 질문 흐름

오늘 직접 정리한 질문은 다음과 같았다.

```text
pandas apply와 lambda로 딕셔너리 리스트에서 name만 꺼내는 코드는 C# 반복문으로 어떻게 이해할 수 있는가?
movie_df['genres'] = ...처럼 기존 컬럼을 다시 수정하는 것은 pandas 특징인가, Python 특징인가?
희소 행렬과 원핫 인코딩은 둘 다 0이 많은데 비슷한 개념으로 봐도 되는가?
cosine_similarity(movie_genres_mat, movie_genres_mat)에서 NameError가 나는 이유는 무엇인가?
영화 제목으로 인덱스를 찾은 뒤 추천 결과를 뽑을 때 인덱스 에러가 나는 이유는 무엇인가?
추천 결과를 만들 때 movie_df 자체를 덮어쓰지 않고 별도 변수로 저장해야 하는 이유는 무엇인가?
```

질문의 공통점은 추천 시스템 코드에서 데이터 구조가 계속 바뀌기 때문에, 지금 변수에 들어 있는 값이 "원본 문자열", "딕셔너리 리스트", "텍스트로 합쳐진 문자열", "희소 행렬", "유사도 행렬", "정렬된 인덱스" 중 무엇인지 구분하는 데 있었다.

특히 오늘은 pandas 문법이 익숙하지 않아 보이는 부분이 많았다.
하지만 실제로는 문법이 복잡하다기보다 한 줄 안에 다음 과정이 압축되어 있었다.

```text
컬럼 선택
-> 각 행에 함수 적용
-> 딕셔너리에서 name 값 추출
-> 공백 문자열로 합치기
-> 같은 컬럼에 다시 저장
```

## 딕셔너리 리스트 전처리

TMDB 영화 데이터의 `genres`, `keywords` 컬럼은 처음에는 일반 텍스트처럼 보이지만, 실제 내용은 딕셔너리 리스트 구조였다.

예를 들면 다음과 같은 형태이다.

```python
[
  {'id': 28, 'name': 'Action'},
  {'id': 12, 'name': 'Adventure'}
]
```

추천 시스템에서는 `id`보다 장르 이름이 필요했기 때문에 `name` 값만 꺼내서 하나의 문자열로 합쳤다.

```python
movie_df['genres'] = movie_df['genres'].apply(
  lambda x: ' '.join([genre['name'] for genre in x])
)
```

이 코드는 C# 스타일로 생각하면 다음 흐름과 비슷하다.

```text
genres 컬럼의 각 행을 하나씩 꺼냄
-> 행 안의 딕셔너리들을 반복
-> 각 딕셔너리에서 name 값을 꺼냄
-> 리스트에 추가
-> string.Join(" ", 리스트)로 하나의 문자열 생성
```

즉 결과는 다음처럼 바뀐다.

```text
[{'id': 28, 'name': 'Action'}, {'id': 12, 'name': 'Adventure'}]
-> "Action Adventure"
```

오늘 확인한 중요한 점은 다음이다.

```text
movie_df['genres'] = ...
```

이 코드는 pandas가 자동으로 원본을 바꾸는 것이 아니라, 왼쪽에 같은 컬럼명을 다시 썼기 때문에 기존 `genres` 컬럼을 덮어쓰는 것이다.
기존 컬럼을 보존하고 싶다면 새 컬럼에 저장하면 된다.

```python
movie_df['genres_text'] = movie_df['genres'].apply(
  lambda x: ' '.join([genre['name'] for genre in x])
)
```

## 희소 행렬과 원핫 인코딩

오늘은 희소 행렬과 원핫 인코딩의 관계도 다시 확인했다.

원핫 인코딩은 범주형 값을 0과 1로 표현하는 방법이다.

```text
Action  -> [1, 0, 0, 0]
Comedy  -> [0, 1, 0, 0]
Romance -> [0, 0, 1, 0]
```

범주의 개수가 많아지면 대부분의 값이 0이 되기 때문에 원핫 인코딩 결과는 희소한 데이터가 되기 쉽다.

```text
원핫 인코딩 = 범주형 데이터를 숫자 벡터로 바꾸는 방법
희소 행렬 = 대부분의 값이 0인 행렬 또는 그것을 효율적으로 저장하는 방식
```

따라서 두 개념은 완전히 같은 말은 아니지만, 실제 머신러닝 전처리에서는 자주 연결된다.
`CountVectorizer`도 텍스트를 단어별 숫자 행렬로 바꾸는데, 영화 하나가 모든 장르와 모든 키워드를 다 가지는 것은 아니므로 결과가 희소 행렬이 된다.

## 코사인 유사도 계산

오늘 영화 추천에서는 `CountVectorizer`로 장르 텍스트를 벡터화한 뒤, 코사인 유사도를 계산했다.

```python
from sklearn.feature_extraction.text import CountVectorizer

cnt_vectorizer = CountVectorizer(min_df=0, ngram_range=(1, 2))
movie_genres_mat = cnt_vectorizer.fit_transform(movie_df['genres'])
```

그 다음 영화 간 장르 유사도를 계산했다.

```python
from sklearn.metrics.pairwise import cosine_similarity

genre_sim = cosine_similarity(movie_genres_mat, movie_genres_mat)
```

이때 `movie_genres_mat`이 정의되지 않은 상태에서 `cosine_similarity()`를 실행하면 `NameError`가 발생한다.

```text
NameError = 파이썬이 해당 변수 이름을 모른다는 뜻
```

즉 문제는 `cosine_similarity` 함수 자체가 아니라, 그 함수에 넣을 재료인 `movie_genres_mat`을 앞 셀에서 먼저 만들지 않았거나 변수명을 다르게 작성한 데 있었다.

계산 결과의 모양은 다음과 같다.

```text
genre_sim.shape = (영화 개수, 영화 개수)
```

이는 모든 영화와 모든 영화 사이의 장르 유사도를 계산했다는 뜻이다.

```text
genre_sim[0, 1] = 0번 영화와 1번 영화의 장르 유사도
genre_sim[10, 20] = 10번 영화와 20번 영화의 장르 유사도
```

## 추천 인덱스 오류

오늘 가장 중요한 디버깅 포인트는 추천 기준 영화의 인덱스를 찾는 부분이었다.

처음에는 다음처럼 작성하면 문제가 생길 수 있었다.

```python
index = movie_df[movie_df['title'] == title].values[0]
```

이 코드는 영화의 행 번호를 가져오는 것이 아니라, 조건에 맞는 영화의 행 전체 값을 가져온다.
그 결과 `index`에는 정수 하나가 아니라 배열 형태의 데이터가 들어가고, 이 값을 유사도 행렬의 인덱스로 쓰면 오류가 발생한다.

올바른 흐름은 다음과 같다.

```python
# title : 추천 기준이 되는 영화 제목
title = "The Godfather"

# movie_df의 기존 인덱스가 중간에 비어 있으면
# cosine_similarity 결과의 행 번호와 맞지 않을 수 있으므로 인덱스를 초기화
movie_df = movie_df.reset_index(drop=True)

# 영화 목록(movie_df)에서 title이 일치하는 행의 인덱스 찾기
# index는 추천 기준 영화가 movie_df에서 몇 번째 행인지 나타내는 정수값
index = movie_df[movie_df['title'] == title].index.values[0]

# 기준 영화와 장르 유사도가 높은 영화들의 인덱스를 상위 20개까지 조회
recommended_idx = movie_idx_by_genre_sim_desc[index, :20]

# 추천 영화 목록 조회
# 원본 movie_df를 덮어쓰지 않고 recommended_movies라는 새 변수에 저장
recommended_movies = movie_df.iloc[recommended_idx]

# 추천 영화 상위 20개 출력
recommended_movies.head(20)
```

여기서 특히 조심해야 할 코드는 다음이다.

```python
movie_df = movie_df.iloc[movie_idx_by_genre_sim_desc[index, :20]]
```

이렇게 작성하면 원본 `movie_df`가 추천 결과 20개짜리 데이터프레임으로 덮어써진다.
그러면 다음 추천을 다시 실행할 때 전체 영화 목록이 사라져서 인덱스가 꼬일 수 있다.

따라서 추천 결과는 별도 변수에 저장하는 편이 안전하다.

```python
recommended_movies = movie_df.iloc[recommended_idx]
```

## Weighted Rating

콘텐츠 기반 추천에서는 단순히 장르가 비슷한 영화만 보여주면 문제가 생길 수 있다.
평점이 높더라도 평가 수가 너무 적은 영화는 신뢰도가 낮을 수 있기 때문이다.

그래서 오늘 노트북에서는 평가 수를 반영한 가중평점도 함께 계산했다.

```text
vote_average = 단순 평균 평점
vote_count = 평가 수
weighted_vote = 평가 수까지 고려한 보정 평점
```

이 방식은 장르 유사도가 높은 후보 중에서도 평가 수와 평균 평점을 함께 고려해 조금 더 안정적인 추천 결과를 만들기 위한 장치였다.

정리하면 최종 추천 흐름은 다음과 같다.

```text
기준 영화 제목 입력
-> 해당 영화의 행 인덱스 찾기
-> 장르 유사도 높은 영화 후보 추출
-> 후보 영화의 vote_count, weighted_vote 기준 정렬
-> 최종 추천 목록 반환
```

## 오늘 정리

오늘은 추천 시스템을 구현하면서 pandas 전처리와 scikit-learn 벡터화 결과가 어떻게 이어지는지 확인했다.

특히 다음 개념이 중요했다.

```text
apply = Series의 각 값에 함수를 적용
lambda = 짧은 익명 함수
literal_eval = 문자열처럼 저장된 리스트/딕셔너리를 실제 Python 객체로 변환
CountVectorizer = 텍스트를 단어 빈도 기반 벡터로 변환
sparse matrix = 0이 대부분인 행렬
cosine_similarity = 벡터 방향이 얼마나 비슷한지 계산
argsort = 유사도 값을 기준으로 정렬된 인덱스 생성
iloc = 행 번호 기반으로 데이터프레임 조회
```

오늘 막혔던 부분은 대부분 "변수 안에 지금 무엇이 들어 있는가"를 놓쳐서 생긴 문제였다.
추천 시스템에서는 데이터가 여러 단계를 지나면서 형태가 계속 바뀐다.

```text
문자열
-> 리스트/딕셔너리
-> 전처리된 텍스트
-> 희소 행렬
-> 유사도 행렬
-> 정렬된 인덱스
-> 추천 결과 데이터프레임
```

앞으로는 에러가 났을 때 바로 함수만 의심하기보다, 그 함수에 들어가는 변수의 타입과 shape를 먼저 확인해야겠다.

```python
print(type(movie_df['genres'].iloc[0]))
print(movie_genres_mat.shape)
print(genre_sim.shape)
print(type(index), index)
```

오늘 학습한 추천 시스템은 단순한 장르 기반 추천이지만, 전처리, 벡터화, 유사도 계산, 정렬, 평점 보정까지 들어가 있어 실제 추천 시스템의 기본 구조를 이해하기에 좋은 예제였다.
