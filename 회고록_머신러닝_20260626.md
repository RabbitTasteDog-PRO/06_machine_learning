# 회고록: 머신러닝_2026-06-26

학습은 : fit
결과는 :predict
평가점수는 : :score

오늘은 데이터 전처리에서 시작해서 KNN 회귀, 선형 회귀, 다항 회귀, 회귀 평가 지표까지 이어서 정리했다.
어제는 머신러닝의 기본 흐름과 `train`, `test`, `X`, `y`를 익히는 데 집중했다면, 오늘은 실제 회귀 문제에서 입력값을 어떻게 준비하고 모델이 무엇을 학습하는지 더 구체적으로 다뤘다.

## 오늘 커밋한 내용

```text
daab168 - 데이터 전처리
- 결측치 처리 및 이상치 탐지 예제 추가
- 범주형 데이터 인코딩 실습 추가
- 표준화, 정규화, 로버스트 스케일링 예제 추가

1699872 - 회귀분석 update

27d3b11 - 회귀분석
- 최근접 이웃 회귀
- 근접한 이웃을 찾아 수치를 예측하는 흐름 정리

5e08d22 - 회귀분석
- 최근접 모델과 선형 모델 비교
- 농어 데이터와 perch_full.csv 추가

6cef67d - 회귀분석
- 선형회귀 실습 추가
- 보스턴 주택가격 데이터 추가

1a7afbc - 회귀분석
- 선형회귀 내용 보완
```

## 데이터 전처리

전처리는 모델이 데이터를 제대로 학습할 수 있도록 입력값을 정리하는 과정이다.
오늘은 `SimpleImputer`, `IsolationForest`, `LabelEncoder`, `OneHotEncoder`, `StandardScaler`, `MinMaxScaler`, `RobustScaler`를 실습했다.

`SimpleImputer`는 비어 있는 값을 평균, 중앙값, 최빈값 등으로 채워준다.
`IsolationForest`는 다른 데이터와 패턴이 많이 다른 값을 이상치로 찾아낸다.
`LabelEncoder`와 `OneHotEncoder`는 문자 범주를 숫자로 바꿔서 모델이 계산할 수 있게 해준다.

스케일링도 다시 중요하게 봤다.
KNN처럼 거리 기반으로 판단하는 모델은 숫자 크기가 큰 feature에 쉽게 끌릴 수 있다.
그래서 `StandardScaler`, `MinMaxScaler`, `RobustScaler`를 사용해서 feature의 숫자 범위를 맞춘다.

## KNN 회귀

KNN 회귀에서 근접한 이웃은 예측하려는 데이터와 feature 값이 비슷한 기존 데이터이다.
예를 들어 농어의 길이를 보고 무게를 예측한다면, 길이가 비슷한 농어들이 가까운 이웃이 된다.

`n_neighbors`는 몇 개의 이웃을 참고할지 정하는 값이다.

```text
n_neighbors가 작으면 가까운 데이터에 민감하게 반응한다.
n_neighbors가 크면 여러 데이터를 평균내기 때문에 더 안정적이지만 둔해질 수 있다.
```

그래서 이웃의 수를 바꿔보면서 너무 예민하지도, 너무 둔하지도 않은 값을 찾는다.

KNN의 한계도 확인했다.
학습 데이터 범위 밖의 값을 넣으면 가장 가까운 이웃들의 평균만 사용하기 때문에, 큰 입력값에 대해서도 예측값이 충분히 커지지 못할 수 있다.
이것이 KNN의 외삽 문제이다.

## 선형 회귀

선형 회귀는 입력값 `X`와 정답 `y` 사이의 관계를 가장 잘 설명하는 직선을 찾는 모델이다.

feature가 1개라면 다음과 같이 볼 수 있다.

```text
y = w * x + b
```

여기서 `w`는 기울기이고, `b`는 절편이다.
`x`가 1 증가할 때 `y`가 얼마나 변하는지를 기울기 `w`가 나타낸다.

feature가 여러 개라면 다중 선형 회귀가 된다.

```text
y = (w1 * x1) + (w2 * x2) + (w3 * x3) + b
```

농어 데이터에서는 `length`, `height`, `width` 같은 여러 feature를 사용해서 `weight`를 예측했다.
각 feature마다 가중치가 따로 생기고, 모델은 이 가중치를 학습해서 최종 예측값을 계산한다.

`lin_reg`는 선형대수 자체를 뜻하는 것이 아니라, `LinearRegression()` 모델을 담은 변수 이름이다.
다만 선형 회귀 내부에서는 벡터와 행렬 계산이 사용되므로 선형대수 개념과 연결된다.

## 다항 회귀

선형 회귀는 기본적으로 직선 관계를 학습한다.
하지만 현실의 데이터는 항상 직선처럼 움직이지 않는다.
농어의 길이와 무게 관계도 길이가 커질수록 무게가 더 빠르게 증가할 수 있다.

그래서 `PolynomialFeatures`를 사용해 기존 feature를 다항 feature로 확장했다.

```text
degree=2이면 x를 x, x^2 형태로 확장한다.
```

학습 데이터에는 `fit_transform()`을 사용했다.
테스트 데이터에는 `transform()`만 사용했다.

```text
X_train은 변환 기준을 학습해도 된다.
X_test는 모델이 처음 보는 데이터처럼 남겨야 하므로 fit하지 않는다.
```

테스트 데이터에 `fit()`을 하면 시험지를 미리 보고 공부하는 것과 비슷해진다.
이런 문제를 데이터 누수라고 부른다.

## 보스턴 주택가격 회귀

`03_linear_regression.ipynb`에서는 보스턴 주택가격 데이터를 사용해 회귀 분석 흐름을 정리했다.

`MEDV`는 예측해야 하는 집값이고, 나머지 컬럼들은 feature이다.

```text
feature = 모델이 예측할 때 참고하는 입력 정보
target = 모델이 맞춰야 하는 정답
```

예를 들어 집값 예측에서는 범죄율, 방 개수, 세금, 저소득층 비율 같은 정보가 feature가 되고, 실제 집값인 `MEDV`가 target이 된다.

13개의 feature와 `MEDV`의 관계를 `sns.regplot()`으로 확인했다.
이때 `plt.subplot()`이 아니라 `plt.subplots()`를 써야 여러 그래프 영역을 만들 수 있다는 점도 확인했다.

## 회귀 평가 지표

선형 회귀 모델을 평가할 때는 `score()`뿐 아니라 여러 오차 지표를 함께 봤다.

```text
R^2  = 모델이 target의 변화를 얼마나 잘 설명하는지 보는 지표
MSE  = 오차를 제곱해서 평균낸 값
MAE  = 오차의 절댓값을 평균낸 값
RMSE = MSE에 제곱근을 씌운 값
```

`R^2`는 높을수록 좋고, `MSE`, `MAE`, `RMSE`는 낮을수록 좋다.
특히 `MAE`와 `RMSE`는 target과 같은 단위로 해석하기 쉬워서 실제 예측 오차를 이해하는 데 도움이 된다.

오늘 보스턴 주택가격 선형 회귀에서는 학습셋과 평가셋 점수를 함께 비교했다.
학습셋 점수만 보면 모델이 이미 본 문제를 잘 맞춘 것인지, 처음 보는 문제도 잘 맞추는지 구분하기 어렵다.
그래서 항상 테스트 데이터 기준의 평가가 중요하다.

## 오늘 자주 만난 실수

오늘 실습하면서 나온 에러들은 대부분 변수명, 컬럼명, 함수명에서 발생했다.

```text
load_iris      -> load_iris()로 실행해야 함
x_test         -> X_test와 다른 변수임. 파이썬은 대소문자를 구분함
plt.subplot    -> plt.subplots가 맞음
' height'      -> 컬럼명 앞 공백 때문에 KeyError 발생 가능
```

특히 pandas에서 `KeyError`가 나면 먼저 컬럼명을 확인해야 한다.

```python
print(perch_df.columns)
```

컬럼명 앞뒤 공백은 다음처럼 정리할 수 있다.

```python
perch_df.columns = perch_df.columns.str.strip()
```

## 오늘 정리

오늘은 회귀 문제를 풀기 위한 전체 흐름을 더 명확히 이해했다.

```text
1. 데이터를 불러온다.
2. feature와 target을 나눈다.
3. train/test로 분리한다.
4. 필요한 전처리를 한다.
5. 모델을 학습한다.
6. 예측한다.
7. 평가 지표로 성능을 확인한다.
```

KNN 회귀는 가까운 이웃을 기준으로 예측하고, 선형 회귀는 입력값과 정답 사이의 직선 관계를 학습한다.
다항 회귀는 선형 회귀의 구조를 유지하면서 feature를 확장해 곡선적인 관계를 표현할 수 있게 한다.

오늘의 핵심은 feature, target, fit, transform, predict, score가 서로 어떻게 이어지는지 이해한 것이다.
다음에는 회귀 모델의 성능을 더 좋게 만들기 위해 과대적합과 과소적합, 그리고 규제 모델을 이어서 정리하면 좋겠다.
